# MEASUREMENTS — 測定と作業の記録

**この文書は追記専用。** 一度書いた記録は消さない。伸びるのが正常な挙動で、
**ここを圧縮すると、この実装を信用する根拠そのものが消える。**

現在地は [STATUS.md](STATUS.md)、設計判断と罠は [HANDOVER.md](HANDOVER.md)。

> ## ⚠ 測定結果には世代が3つある。各エントリ直下の条件行を見ること
>
> `eval/arib_vtt.py` に**採点側の欠陥**が見つかり修正した（[#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた)）。
> 参照キューの表示時間を発話時間として使っていたため、主題歌の無音が採点対象に入り、
> 歌を書き起こす設定が不当に罰せられていた。**#1〜#18 の数値は現在の指標では再現しない。**
>
> | 世代 | エントリ | 採点指標 | 歌唱区間 |
> |------|---------|---------|---------|
> | 1 | #1〜#15 | pre-#19（欠陥あり） | 採点に含む |
> | 2 | #16〜#18 | pre-#19（欠陥あり） | `--exclude-songs` |
> | 3 | **#19〜#22** | **修正後** | `--exclude-songs` |
>
> **各測定エントリの直下に出典レポートと採点条件を1行で書いてあるので、
> 引用するときはこの表を覚える必要はなくそこを見れば足りる。**
> 条件行は `_eval/score-*.txt` が自分で出力している `[score] ... file(s) scored | ...`
> をそのまま引いたもので、レポートを開けば照合できる。
>
> 参照を使わない指標（ループ字数・30秒超・thin）はどの世代でも有効。
> 現行の基準値は [STATUS.md](STATUS.md)。
>
> **訂正（2026-08-23）。** この節は長らく「#15〜#18 が `--exclude-songs` 世代」と
> 書いていたが、**#15 の出典 `_eval/score-cond-prev.txt` の条件行に歌唱除外が無い。**
> 正しい境界は #16（`--exclude-songs` を導入した節）で、上の表はそれに直してある。

| 節 | 内容 |
|----|------|
| [測定結果](#測定結果) | #1〜#22 と現行の基準値の測定。**数値を引用するならここ** |
| [完了した着手の記録](#完了した着手の記録fgiheacb-と運用メモ) | 決着した A〜I と運用メモ |
| [作業記録](#作業記録) | 実装の経緯と踏んだ罠 |
| [検証済みのこと](#検証済みのこと)・[未検証のこと](#未検証のこと) | 何を確かめて何を確かめていないか |
| [解決済み](#解決済み旧版からの変更点)・[テスト結果サマリ](#テスト結果サマリ) | 直したものと実行した条件 |
| [ビルド検証](#ビルド検証済み5回)・[配布アーカイブ](#配布アーカイブ-091単一ファイルになった2026-08-23) | 各版のビルドとパックの記録 |

---

### このセッションで変えたもの

**コードは版番号1行と spec の1行だけ。挙動は触っていない。**
残りはドキュメントの再編と配布作業。

| ファイル | 変更 |
|---------|------|
| `whisp_carrier.py` | **`VERSION = "0.1.0"` → `"0.9.0"` の1行のみ。** バナーに出るので再ビルドが必要（[理由](HANDOVER.md#なぜ-10-ではなく-090-なのか)） |
| `whisp_carrier.spec` | post-COLLECT のコピー対象に **`README.md` を追加**。配布アーカイブに導入手順が1つも入っていなかったため |
| `README.md`（旧 `README_ja.md`） | **`git mv` で日本語をトップに。** exe 版のマニュアルとして再構成し、**スクリプト版のコマンドを全部削除**（`pip install`・`python whisp_carrier.py`・`.bat`・変換レシピ）。「exe版とスクリプト版の違い」表を新設。beam の「推奨: 10」・特徴の VAD 行・フィルターの原因説明・ステータスを修正。参考プロジェクト表に **TEN VAD ほか5件とライセンス列**を追加。anime-whisper の数値を[#22](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持)に差し替え。[XXL が 50番台で落ちる節](#実運用での裏付けamatsukaze-から素の-xxl-を呼ぶと落ちる2026-08-23)を新設 |
| `README_en.md`（旧 `README.md`） | 同じ方針に追従。言語切り替えリンク、Architecture の最新化（**英語側にだけ意図的に残した重複**）、`What the exe build cannot do` に exe/script 列 |
| `THIRD-PARTY-NOTICES.md` | **PyInstaller ブートローダーを追加**（節2・節5・再生成手順の3箇所）。同梱物で唯一の GPL 由来コードで、`_internal/` にも PYZ にも現れないため再生成手順では拾えない |
| `HANDOVER.md` | [ドキュメント構成](HANDOVER.md#ドキュメント構成readme-は日本語がトップ開発版は-handover-に寄せた)・[スクリプト版の実務](HANDOVER.md#スクリプト版の実務exe版との差分)・[再パックの発動条件](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)・[版番号とタグの運用](HANDOVER.md#版番号とタグの運用スクリプト版だけの修正では版を上げない)・[配布アーカイブ](#配布アーカイブ2分割で作った2026-08-23)・[#22](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持)・[XXL の実運用失敗](#実運用での裏付けamatsukaze-から素の-xxl-を呼ぶと落ちる2026-08-23)・[第三者検証と訂正](HANDOVER.md#訂正して伝える必要がある説明sm_120-フォールバック説)の各節。既存のデッドアンカー6本も修正 |

**ドキュメントの分け方を決めた理由。** 詳細は
[ドキュメント構成](HANDOVER.md#ドキュメント構成readme-は日本語がトップ開発版は-handover-に寄せた)にあるが、
要点は **「exe版マニュアルとスクリプト版マニュアルを2本作らない」**。
共通を README に1つ置き、スクリプト版で違うところだけ HANDOVER に書く。
**重複が測定値を腐らせる**からで、実際に旧 README は否定済みの beam 10 を
全7例に残していた。

### ビルド検証（済み・5回）

**配布した exe は5回目のビルド**（2026-08-23 13:44:55、`VERSION = "0.9.0"`）。
1〜3回目が 08-22、4〜5回目が 08-23。

1. **計測ログを入れて再ビルド** — 連携テストに必要だった
2. **torchaudio の警告抑止** — 連携テストのログにノイズが出ていたため
3. **turbo の注意書きと huggingface_hub の警告抑止** — `自動` = turbo が判明したため
4. **README.md の同梱**（12:09）— spec の post-COLLECT コピー対象に足した。
   **Python コードは無変更**（[ドキュメント構成](HANDOVER.md#ドキュメント構成readme-は日本語がトップ開発版は-handover-に寄せた)）
5. **版番号を 0.9.0 に**（13:44）— バナーに出るので再ビルドが必要だった。
   **回帰確認と `[spec]` ログは[配布アーカイブの節](#配布アーカイブ2分割で作った2026-08-23)にある**
   （字幕出力の MD5 は5回すべて記録値と一致）

#### 4回目の検証（2026-08-23）

**ターミナルの出力が取れない状態で回したので、ログ行ではなく成果物で判定した。**
ビルド中と exe 実行中に stdout が空で返り、直後の別コマンドの結果として
遅れて出てくる挙動があった。**この節の冒頭に書いてある罠と同じ形**なので、
テキストを読まずに済む確認だけで固めた。

| 確認項目 | 結果 |
|---------|------|
| exe が更新されたか | `LastWriteTime` 1:57:55 → **12:09:31** |
| README が入ったか | `dist/whisp-carrier/README.md` が出現し、**ビルド時点のソースと MD5 一致**。その後 README を[さらに削った](HANDOVER.md#ドキュメント構成readme-は日本語がトップ開発版は-handover-に寄せた)ので dist 側へコピーし直した（現在は両方 `FCA4B1114FE963CFCD6B7F142E6CE634`）。**次回ビルドでは spec が同じことをする**ので手当ては不要 |
| 字幕出力の回帰 | `test_speech.wav` を `-m tiny -f srt vtt --no_config` で処理し、**記録値と MD5 完全一致**（SRT `AE21834629D4762F0BCD2405E102D1DB` / VTT `6A6791FD97DE025C91A202223185C868`） |
| 生きた設定の保全 | `whisp-carrier.yaml` の MD5 が前後で不変（`399D11649E65FF7102DAE603ECBD7ECD`）。spec の退避・復元が4回目も効いた |
| ffmpeg | 下記 |

**ffmpeg は同梱された実物の configure 行を読んで確かめた。**
`[spec] ffmpeg verified` の行は上記の理由で目視できていないが、
**成果物を直接見るほうが強い確認**になる（ビルド時のメッセージを信じる必要がない）。

```
dist\whisp-carrier\_internal\ffmpeg.exe -hide_banner -version
ffmpeg version n8.1.2-44-g7c533d0f86-20260820
configuration: ... --enable-version3 ... （--enable-gpl も --enable-nonfree も無い）
  さらに --disable-libx264 --disable-libx265 --disable-libxvid --disable-libfdk-aac
        --disable-libdavs2 --disable-libxavs2 --disable-libvidstab
        --disable-librubberband --disable-avisynth --disable-libdvdread
        --disable-libdvdnav --disable-frei0r
EXIT=0
```

**GPL / non-free を要求する構成要素が名指しで disable されている**のが LGPL ビルドの
署名で、`--enable-version3` は LGPL v3 を意味する（GPL 化はしない）。
SHA-256 も記録値と一致（`8ee152edc79f7ba99969b7fb590cfde438b46cb383952dec87e754db83788572`、
[該当節](#ffmpeg-を-gpl-v3-から公式-lgpl-v3-に差し替えた)）。
`dist/whisp-carrier/LICENSE.ffmpeg.txt` も **LGPL v3 の本文**であることを確認した
（1行目が `GNU LESSER GENERAL PUBLIC LICENSE`）。
そして **GPL ビルドを掴んだ場合は spec が COLLECT 前に `SystemExit` で止まる**ので、
新しい exe が生成されている事実自体がガードを通過した証拠でもある。

> **出力が取れないときの回し方。** ビルドを
> `python -m PyInstaller whisp_carrier.spec --noconfirm > build.log 2>&1` にして
> **後からファイルを読む**のが確実。ターミナルの取りこぼしと無関係になる。
> ただし判定は exe のタイムスタンプで行うこと（ログが古い可能性を消せない）。

**3回とも字幕出力の MD5 が記録値と一致**
（SRT `AE21834629D4762F0BCD2405E102D1DB` / VTT `6A6791FD97DE025C91A202223185C868`）。
3回目では `-m large-v3-turbo` で `[MODEL] NOTE:` の3行が出て、
`-m tiny` では出ないことも exe で確認した。

> **ビルドの成否は exe のタイムスタンプで確認すること。**
> 3回目で一度失敗している。既存ターミナルを再利用して `python -m PyInstaller` を投げ、
> `BUILD_EXIT=0` の行を読んで成功と判断したが、**それは前回のビルドの出力が
> 残っていたもので、ビルド自体は起動していなかった。**
> 気付いたのは exe が turbo の注意書きを出さなかったからで、
> `Get-Item dist\whisp-carrier\whisp-carrier.exe` の `LastWriteTime` を見たら
> 44分前のままだった。**新しいターミナルで実行し、タイムスタンプで確認する。**

以下は1回目の記録（内容は2回目でも同じ）。凍結環境で TEN が動き、新しいログ行も出る。

```
[VAD] threshold 0.75 (default for ten)
  [VAD] Running ten...
  [VAD] ten: 1 regions | 7.1s of speech | detected in 0.3s
  [VAD] 1 segments via clip_timestamps (clips are zero-padded to 30s)
  [STT] Done in 1.0s | dur=7.1s | lang=ja (96.53%) | 3 segments
```

**字幕出力は不変。** `test_speech.wav` を `-m tiny -f srt vtt` で処理して
MD5 が記録値と完全一致した（SRT `AE21834629D4762F0BCD2405E102D1DB` /
VTT `6A6791FD97DE025C91A202223185C868`）。**print だけの変更なので、
すでに生成済みの字幕を作り直す必要はない。**

**ライセンス同梱も自動化が効いている。** 前回は手で補っていた TEN の
Apache-2.0 ライセンスが、今回は spec が自分で配置した。

```
[spec] ten_vad dll: ...\ten_vad\lib\Windows\x64\ten_vad.dll -> ten_vad/lib/Windows/x64
[spec] ten-vad licence: ...\ten_vad-1.0.6.8.dist-info\licenses\LICENSE
[spec] ten-vad licence: ...\ten_vad-1.0.6.8.dist-info\licenses\NOTICES
```

**一度踏んだ罠（解決済み）。** ライセンス探索の glob が外れて、初回ビルドは DLL だけ入って
Apache-2.0 の LICENSE が付かない成果物になった。この wheel は
`dist-info/licenses/` 配下に置く新しめのレイアウトで、`dist-info` 直下を見る
`glob()` では見つからない。**`rglob()` に直し、見つからない場合は警告ではなく
`SystemExit` にした**（DLL は無条件に同梱されるので、これは警告で済ませてよい
ものではない）。上のログはその修正が効いていることの確認でもある。

しきい値は推測ではなく実測から決めた。15本 23,848 キューで保持時間は
**中央値 0.24 秒/字、p95 1.17 秒/字**、20秒以上保持は 88 件（0.37%）。
`SPEECH_SECONDS_PER_CHAR = 1.2`（p95）、`MIN = 2.0`、`MAX = 20.0`（`min_gap` と同値）。
分布は `_eval/_cue_length.txt`。

### 1.0.0のビルドと配布アーカイブ（2026-09-03）

**2回ビルドしている。1回目は[既定の実行が全部落ちた](#100の初回ビルドが既定の実行で全部落ちたpathnone2026-09-03)**
（`Path(None)`）。**以下は2回目＝配布するぶんの記録。**
コード状態は `998798a`。

| | 値 |
|---|---|
| exe | **13,899,921 バイト**（0.9.2 は 13,887,994。**落ちた1回目のビルドは 13,899,906 で別物**） |
| payload | **`_internal` 816ファイル・2226 MB**／`whisp-carrier` 全体 2239 MB。`_internal/torch` は無し |
| アーカイブ | **`whisp-carrier-1.0.0.7z`・1,055,059,470 バイト（1007 MiB・単一ファイル）** |
| **SHA-256** | **`52E84E784751EA1009F6701D0BA70CE2598C4D9161303DFFBD29E67C7F89AE9B`** |
| アーカイブ内 | 169 folders / 824 files / 展開後 2,348,067,641 バイト。`7z t` が `Everything is Ok` |
| 同梱 yaml | **`.example`（10,967 バイト）だけ。生きた設定は入っていない** |
| 同梱 docs | `README.md` 53,304 バイト・`THIRD-PARTY-NOTICES.md` 17,403 バイト。**どちらもソースとバイト一致** |

**パックは2回している。** 1回目は README の1行を直す前のもので、
`1,055,056,170` バイト・SHA-256 `6494C638…`。**公開していないので破棄した**
（[経緯](#実況系の推奨が2箇所に残っていた100を再パックした2026-09-03)）。
**exe は両方とも同一**（13,899,921 バイト）。

#### リリース前チェックリスト16項目の結果

**全項目通った。** 項目14の後半（リリース後にアセットの digest と照合）も
**公開後に確認して一致した**ので、**16項目すべてが閉じている。**

| # | 項目 | 結果 |
|---|------|------|
| 1 | ビルドログ | `BUILD_EXIT=0` / `[spec] default (slim)` / `ffmpeg verified (known LGPL build): n8.1.2-44-g7c533d0f86-20260820` / `keeping 13 CUDA libraries (1870 MB), dropping 2403 MB of torch` / `dropped 3 duplicate torch/lib binaries (766 MB)` |
| 2 | `--version` | `whisp-carrier 1.0.0 \| ctranslate2 4.8.1 \| torch not bundled \| CUDA: True` |
| 3 | payload / `_internal/torch` | 2226 MB / 無し |
| **4** | **出力の回帰** | **SRT `AE21834629D4762F0BCD2405E102D1DB` / VTT `6A6791FD97DE025C91A202223185C868` が記録値と完全一致。この項目が `Path(None)` を捕まえた** |
| 5 | PATH から ffmpeg を抜いて AAC | exit 0・字幕生成・**stderr 空**（抜いたのは `...\WinGet\Links`） |
| 6 | 偽 `CUDA_PATH` | exit 0・`[CUDA] ignoring CUDA_PATH`・転写完了 |
| 7 | 同上 `=off` | **exit 1・`Library cublas64_12.dll is not found or cannot be loaded`**（N と同一） |
| 7b | 同上 `=preload` | exit 0・`ignoring CUDA_PATH` は出ない（層2単独で救えることの確認） |
| 8 | ロードされた DLL のフルパス | **cuBLAS 2本とも `_internal` 由来**（181モジュール、0.9.2 と同数）。cuDNN は `_internal\ctranslate2\cudnn64_9.dll`（266KB のディスパッチャ）だけ |
| 9 | `--device cpu` | exit 0・**cuBLAS が1つもロードされない**（ピン留めを払っていない） |
| 10 | `silero_v5` / `silero_v5_fw` | 前者 exit 1 で代替名を表示、後者 exit 0 |
| 11 | 生きた設定の保全 | MD5 `399D11649E65FF7102DAE603ECBD7ECD` がビルド前後・パック前後で不変 |
| 12 | アーカイブ | `7z t` が `Everything is Ok`。**一覧の yaml は `.example` 1件だけ** |
| 13 | 同梱 docs | README / THIRD-PARTY-NOTICES がソースとバイト一致 |
| 14 | SHA-256 | 上記。**公開後にリリースのアセット digest と一致を確認済み**（`sha256:52e84e78…`。GitHub 側は小文字表記） |
| 15 | `--help` | exit 0・15,905 バイト・`--filler_filter` と `--vad_method` が出る |
| 16 | `-m kotoba-v2` | exit 0・**`[MODEL] word timestamps disabled: alignment heads name decoder layer 25 but this model has 2.`** が出て転写も完了（2セグメント） |

#### 項目7が1回「通ってしまった」（テスト側の不備・チェックリストを直した）

**`CUDA_PATH` を偽装しただけでは項目7が exit 0 になる。**
**この開発機は PATH に `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.8\bin` があり**、
`CUDA_PATH` が的外れで探索が PATH まで落ちたときに
**本物の `cublas64_12.dll` がそこで見つかる。**

**[N の表](#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正)の
「PATH の toolkit」列が失敗行で「なし」なのはこのため**だが、
**チェックリスト側に書いていなかった。**
[項目6に追記し、項目7bも表に足した](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)。

**教訓は項目7の読み方。あれは「失敗できることの確認」で、
成功したら直った証拠ではなく、測定になっていない証拠。**
**否定的確認を項目として持つなら、通ったときの意味を項目自身に書いておくこと。**

### 上流の Pro も TEN VAD を既定にしていた（2026-09-03 に確認）

**推測ではなく上流のリリースノートで確認した。**
Faster-Whisper-XXL **Pro `r3.256.1`（2025-11-08）**に次の項目がある。

```
New VAD models: silero_v6_fw, silero_v6 [patched], nemo_v2, ten [its threshold is offset by +0.2]
Change: --vad_method default is set to ten
Change: --compute_type defaults to default if CUDA, else auto
Updated: torch to 2.8.0+cu128 [to support 50xx GPUs on torch models]
```

そして [#456](https://github.com/Purfview/whisper-standalone-win/discussions/456) の本文が
**「寄付額 £50 以上の寄付者向け非公開版」**と明記している。

#### 分かったこと4つ

**(1) TEN を既定にしたのは向こうが先。** あちらが **2025-11**、
こちらは [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) が **2026-08**。
**独立に同じ結論に着いた。** 15本の CER で選んだこちらと、
別の理由で選んだあちらが一致したので、**TEN が妥当だという傍証としては
こちらの測定より強い。**

**(2) 閾値の扱いが違う。** あちらは **`ten` の閾値を +0.2 オフセット**している
（＝利用者から見た既定値を据え置いて内部でずらす）。
こちらは[バックエンドごとに意味が違うので既定のままにしろ](../README.md)と書き、
**`ten` の 0.75 を3閾値の実測から選んだ**（[#23 の表](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) に 0.60/0.75/0.90 がある）。
**同じモデルを載せても運用点の決め方は別問題**という例。

**(3) 50番台対応は Pro に入っている。** `torch 2.8.0+cu128` を
「to support 50xx GPUs on torch models」として取り込み、
**`--compute_type` の既定も変えている。**
[無料版が 5090 で float16 のまま落ちる件](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施)の
対策はあちら側にあるということで、**README の経緯の記述は事実として正しかった。**
`£50` も正しい。

**(4) こちらの測定の射程は変わらない。** 相手は**無料版 r245.4** で、
そう明記してある。**ただし [#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) の
「別実装でも同じ3割を落とす」は XXL が silero を使っていた前提**なので、
**TEN 既定の Pro には当てはまらない。** あの節の結論は
「Whisper 側の限界」ではなく「silero の限界」だったと [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) で
訂正済みなので向きは合っているが、**根拠として XXL を引くときは版に注意すること。**

> **調べ方の教訓。** front page と Discussions の一覧だけ見て
> 「公開情報からは裏が取れない」と書きかけた。**実際は Pro のリリースノートにあった。**
> GitHub の Discussions は本文が JS で描画されるので機械的なフェッチでは
> **コメント1件しか取れず、それを「本文にはない」と読み違えた。**
> **リリース一覧は別経路で見に行くべきだった。**

### 実況系の推奨が2箇所に残っていた（1.0.0を再パックした・2026-09-03）

**[#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) で取り下げたはずの
`--ff_loudnorm --ff_lowhighpass` の推奨が、README と HANDOVER に1行ずつ残っていた。**

> レース実況などノイズの多い素材では `--ff_loudnorm --ff_lowhighpass` が比較的安定します。

**README は同じファイルの440行あとで、まさにこの組み合わせを明示的に取り下げている。**

> ```
> # 以前ここに「ノイズの多い素材向け」として書いていた組み合わせ。取り下げます
> --ff_loudnorm --ff_lowhighpass
> ```
> **この組み合わせは実測で +2.4pt 悪化し、ループが 0件 → 5件に増えました。**

**つまり1つのファイルが自分と矛盾していた。** 実測は
**+2.4pt 悪化・ループ 0件 → 5件・`loudnorm` の後に `lowhighpass` を通すと
ピークが 0.0 dB（クリップ寸前）**。**そして実況系は素材15本に1本も入っていない**（全部TVアニメ）ので、
あの一文はどの測定にも基づいていなかった。

**なぜ残ったか。** #23 で README を書き直したとき、
**音声フィルターの節と `--ff_*` のオプション表は直したが、
別の節（anime-whisper の注意書きの並び）にある1行を見落とした。**
HANDOVER 側も同じ文がボーカル抽出の節に残っていた。
**「同じ主張が何箇所にあるか」を数えずに直したのが原因。**

#### 再パックの判定

**[発動条件の表](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)では
「README の本文だけ → 再パックしない」に当たる。それでも固め直した。**

| 判断材料 | 内容 |
|---------|------|
| 表の原則 | README 本文だけなら再パックしない（0.9.0 は 298 バイトのずれを承知で出した） |
| **今回の性質** | **精度の数値のずれではなく、「併用しないでください」と書いた組み合わせを同じファイルで勧めている状態。** 読者を実際に誤らせる |
| **費用** | **`7z a` が68秒。exe は無変更なので再ビルドは不要**（spec が次のビルドでやるコピーを手でやるだけ） |
| 公開状況 | **まだ公開していない。** リリースを作り直す費用がゼロ |

**「判定に迷ったら配布物の中身を正しく説明しているかで切る」**に従った形。
**0.9.0 でずれを許容したのは公開後だったから**で、公開前なら払う額が桁違いに安い。

**手順は exe を触らない。** `README.md` を `dist/whisp-carrier/README.md` へコピーし、
生きた設定を退避して固め直すだけ。**再ビルドすると exe のバイト列が変わって
チェックリストを全部やり直すことになるので、やってはいけない。**

**教訓が2つ。**

- **同じ主張が複数箇所にある文書では、直す前に全部数えること。**
  `--ff_loudnorm --ff_lowhighpass` は README に3箇所（注意書き・オプション表・取り下げ節）、
  HANDOVER に1箇所あった。**#23 は2箇所しか直していない**
- **再パックは思っていたより安い。** 分割アーカイブ時代の「6〜10分」が頭に残っていたが、
  [単一ファイルになってから](#配布アーカイブ-091単一ファイルになった2026-08-23)は**約1分。**
  **費用の見積もりが古いと、直すべきものを直さない判断に傾く**

### 配布アーカイブ 0.9.1（単一ファイルになった・2026-08-23）

**[K](#k-torch-を外して配布サイズを削る2026-08-23) で torch を外した結果、分割が不要になった。**

| | 0.9.0 | **0.9.1** |
|---|---|---|
| ファイル数 | 2（`.7z.001` + `.7z.002`） | **1（`whisp-carrier-0.9.1.7z`）** |
| ダウンロード量 | 1999 MiB | **1006 MiB** |
| 展開後 | 4900 MiB | **2240 MiB** |
| アーカイブ内 | 522 folders / 3029 files | **169 folders / 824 files** |
| 圧縮時間 | 6〜10分 | **約1分** |

```
whisp-carrier-0.9.1.7z   1,055,048,158 バイト（1006 MiB）
SHA-256  91BAB9EABEEEDFC766007882A85646CACEBDC85A544B8D755D05E7079D69B0A5
```

`7z t` で整合性を確認済み（`Everything is Ok`、169 folders / 824 files、
展開後 2,348,048,153 バイト）。**アーカイブ内の `whisp-carrier.yaml*` は
`.example`（10,849 バイト）だけで、生きた設定は入っていない。**
開発機の設定は元に戻してある（MD5 `399D11649E65FF7102DAE603ECBD7ECD` で確認）。

**圧縮率は 0.449 で、0.9.0 の 0.408 より悪い。** 落とした torch の CUDA DLL が
平均より圧縮の効く部分だったということ。**次に容量を見積もるときは 0.45 を使う。**

**`-v` は付けていない。** 単一ファイルで 2GB 上限を大きく下回るので分割の理由が無く、
`.001` という拡張子を利用者に見せる必要も無くなった。
**再び 2GB に近づいたときだけ `-v1900m` に戻すこと。**

**未公開。** コミットもタグもリリースもまだ。

---

### 配布アーカイブ（2分割で作った・2026-08-23）

> **これは 0.9.0 の記録。** 0.9.1 は[単一ファイル](#配布アーカイブ-091単一ファイルになった2026-08-23)になった。
> **見出しは変えていない** — 既存の参照が4箇所あり、GitHub のアンカーは見出しの
> 文字列から作られるので、括弧を1つ足すだけで全部切れる。

**4900 MiB → 1999 MiB の2分割。両方とも GitHub の1ファイル上限 2GB 未満。**

| ファイル | サイズ | SHA-256 |
|---------|--------|---------|
| `whisp-carrier-0.9.0.7z.001` | 1,992,294,400（1900 MiB） | `488C2E31A5A5304AC131CA82A6F266645AB3D0E374553A94FDEB7CAA369D490D` |
| `whisp-carrier-0.9.0.7z.002` | 103,770,855（99 MiB） | `5C8BF3994BCE368DB473B509B0BCCB691E24E5597A0AEDAE8C70137E0C6AF30D` |

`7z t` で整合性を確認済み（`Everything is Ok`、522 folders / 3029 files、
展開後 5,137,515,654 バイト、合計 2,096,065,255 バイト）。
**アーカイブ内も確認した**（`whisp-carrier.exe` は 13:44:55 の 0.9.0 ビルド、
`README.md` 44,842 バイト / `THIRD-PARTY-NOTICES.md` 15,087 バイト = 修正後の版）。
**`whisp-carrier.yaml` は入っておらず `.example` だけが入っている。**
開発機の設定は元の場所に戻してある（MD5 `399D11649E65FF7102DAE603ECBD7ECD` で確認）。

**3回固めている。** 最初の2回は破棄した。
1回目は古い docs を含んでいたため、2回目（`0.1.0` 名義）は
[版を 0.9.0 に上げた](HANDOVER.md#なぜ-10-ではなく-090-なのか)ため。
**版を変えると exe のバナーが変わるので再ビルドが必要**で、
アーカイブ名も SHA-256 も変わる。**公開前に版を決めきること。**

**0.9.0 の exe の回帰確認。** `test_speech.wav` を `-m tiny -f srt vtt --no_config` で
処理して**字幕出力の MD5 が記録値と完全一致**
（SRT `AE21834629D4762F0BCD2405E102D1DB` / VTT `6A6791FD97DE025C91A202223185C868`）。
バナーは `whisp-carrier 0.9.0 | torch 2.8.0+cu128 | CUDA: True | GPU: NVIDIA GeForce RTX 5090`。
**版の文字列だけの変更なので出力は不変。**
ビルドログには `[spec] ffmpeg verified (known LGPL build)` /
`[spec] copied README.md to:` / `[spec] restored config:` / `BUILD_EXIT=0` が出ている。

### 測定で出た結論の一覧

> **この表の数値の多くは #1〜#18 由来なので参考情報。**
> 結論の向き（どちらが良いか）はおおむね維持されているが、
> **pt 差を引用するときは[修正後の9本](#修正後の9本2026-08-22)を見ること。**

| 問い | 答え |
|------|------|
| **採点指標は信用できるのか** | **一度壊れていた。** 参照キューの表示時間を発話時間として使っていたため、主題歌の無音が採点対象に入り、歌を書き起こす設定が不当に罰せられていた。修正済み。**#1〜#18 の数値は再現しない。** [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) |
| **TEN VAD を既定にすべきか** | **すべき。既定にした。** 9本で 8勝0敗1分・合計 −3.2pt、子供向け4本で 4勝0敗・−9.2pt、一挙2本でも勝ち。閾値は 0.75。**「15本すべてで silero 以上」は定型句フィルタなしの世代の話で、両側にフィルタを当てると 12勝1敗**（[#28](#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。**群の合計での結論は変わらない。**[修正後の9本](#修正後の9本2026-08-22)・[追加4本](#追加4本の-ten-測定2026-08-22決着)・[一挙2本](#一挙2本の-ten-測定2026-08-22決着) |
| **TEN は長尺で遅くないのか** | **遅くない。** 5h22m で VAD 103.9秒（全体の8.6%）、総時間は silero と +1.5% の差。音声長の 0.54% で線形に伸びるだけ。[一挙2本](#一挙2本の-ten-測定2026-08-22決着) |
| **子供向けの取りこぼしは Whisper の限界か** | **違った。silero の限界だった。** TEN で4本合計 31.1% → 21.9%、カバレッジ 70.3% → 82.1%。[追加4本](#追加4本の-ten-測定2026-08-22決着) |
| **定型句フィルタは入れるべきか** | **入れない。** 0.6pt しか得られず、作中の放送・配信を締める台詞を消す危険が残る。確信度で見分ける案は失敗（定型句のほうが自信を持って出る）。**ループ抑制では弾けないので残存する。本家XXLも同じものを出す。** [#20](#20-定型句フィルタは-06pt-効くが見送った)・[判断記録](#g-定型句フィルタは見送り判断記録) |
| **歌唱・OP/ED をフィルタで落とすべきか** | **落とさない。** 落とすと悪化する（15.5% → 16.1%）。本家も拾う。「対象外だが拾う」と明示する方針。[該当節](#歌唱oped-の扱い対象外だが拾う) |
| **歌検出に INASS を転用できるか** | **できない。** 確定台詞の44%を speech と認識しないので台詞が消える。[#21](#21-inass-を歌検出に転用するのは危険否定的結果) |
| 30秒超セグメント | **既定にあった既存バグ。修正済み**（370s → 18s、全設定で0）。[#1](#1-既定経路に30秒超セグメントの欠陥があった既存バグ修正済み) |
| `collect` と `clip` の差 | **一挙で分離した。clip の勝ち**（全文CER 20.5% vs 22.5%、ループ文字は collect が3.3倍）。**既定は `clip`**。[#6](#6-一挙放送で-clip-が勝ち既定を-clip-に戻した) |
| 外部VAD vs 内蔵VAD | **外部が3pt良い**（clip 24.3% vs 内蔵 27.1%）。内蔵は発話を2割落とす。[#3](#3-外部vadは内蔵vadよりcerで3pt良い想定外) |
| anime-whisper vs large-v3 | **large-v3 の勝ち**（41.8% vs 24.3%、9本全敗）。モデルカードと逆。[#4](#4-anime-whisper-は-large-v3-に負けたモデルカードと逆) |
| VADは何を買っているのか | **カバレッジ4.6ptを売って精度8.7ptを買う交換**。差引でCER 4.4pt改善。入れる判断は正しい。[#7](#7-vadはカバレッジを売って精度を買う交換だったnovad対照) |
| VADの取りこぼしは直せるか | **直せない。パラメータ4つすべて振り終えた**（threshold / min_silence / speech_pad / neg_threshold）。既定が全条件で最良。[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果)・[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)・[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) |
| 死亡遊戯の取りこぼしは何の失敗か | **検出（開始）側。** ヒステリシスを下限まで緩めても区間数が動かない＝落ちている音声は silero が発話と見なしていない。[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) |
| ではVADを替えれば直るのか | **直る。** TEN VAD（Apache-2.0）で死亡遊戯 42.4% → **30.5%**、アイプリ 40.9% → **33.2%**、4本合計で既定 −3.1pt / XXL −3.5pt。**「Whisper側の限界」は silero の限界だった。** [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) |
| 残っている誤差の正体 | **取りこぼしが主。** 参照の20%を落とし、出した文字の87.7%は参照に一致。**間違えているのではなく出していない** |
| **その取りこぼしは何なのか** | **分解した。VAD の外は参照の 0.56% しかなく、9割は区間の中。** 重なり発話は9本で0件。**VAD を外した `novad` でも別実装の XXL でも一致しない文字が参照の 9.15%** で、中身は ARIB が字幕化する呻き・笑い・叫び。[#29](#29-取りこぼしを分解した現行パイプラインの伸びしろは2pt未満否定的結果4件2026-09-05) |
| **正規化を直せば改善するか** | **しない。** 長音・どもり・語尾促音を両側に吸収して **0.28pt**。**床は表記の不一致ではなく、その発話が一度も文字になっていないこと。** 族外（qwen）もドメイン適応（anime-whisper、7系統中最下位）も既定を超えない。**実装・モデルで買える差の合計が 1.6% 程度。** [#29](#29-取りこぼしを分解した現行パイプラインの伸びしろは2pt未満否定的結果4件2026-09-05) |
| 本家XXLと比べてどうか | **健全6本は6勝0敗**。負けるのは病的な2本で、ほぼニンジャラ1本のループ。[#10](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施) |
| ループ抑制は効いたのか | **上限と同じ22.0%に到達。健全6本の出力は1文字も変わらない（誤爆ゼロ）。** [#11](#11-ループを潰した場合の上限既定に載せるだけで本家を抜く)・[#12](#12-実装した検出器は上限にそのまま到達した誤爆ゼロで) |
| WAVで測った数字は実運用でも同じか | **同じ。MKV直渡しの出力がバイト一致した**（[コンテナ等価性](#コンテナ等価性mkv直渡しでバイト一致)） |
| Amatsukazeはどこで呼ぶのか | **CMカット後・出力ファイル単位の中間AAC。** 参照VTTと同じ時間軸。[該当節](#amatsukazeがどこで呼ぶかログで確定) |
| Amatsukazeが渡す beam 10 は良いのか | **悪い。9本中8本で同等または悪化**（合計 22.0% → 22.3%）。時間は+2.8%しか変わらない。**yaml で 5 に戻した。** [#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い) |
| `--language ja` は効くのか | **出力を1セグメントも変えない**（9本すべて一致）。自動判定が元々 ja 92〜100%。[#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い) |
| 取りこぼしは実装の問題か | **違う。Whisper側の限界。** 子供向け4本で XXL のカバレッジ 69.1% 対こちら 67.7%。**別実装でも同じ3割を落とす。** [#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) |
| XXLの `condition_on_previous_text=true` を借りられるか | **借りられない。** 死亡遊戯でカバレッジ 59.7% → **21.3%**、推論5.9倍、末尾12分が消える。`loop_filter` も無力。[#15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定) |
| 死亡遊戯で XXL に負けたままか | **もう負けていない。** `novad` + ループ抑制が CER **30.9%** / カバレッジ **71.6%** で XXL（35.5% / 69.1%）を全指標で上回る。残るのは発動条件だけ。[#15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定) |
| 「問題の3本」は特別なのか | **違った。子供向け番組全般の性質。** 新規のぷにる・プリキュアも同じ故障モード。アイプリは別話でもカバレッジ47%。[#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) |
| 長尺は本当に大丈夫か | **2本目で裏付けた。** マジルミエ4h52mが **17.3%** で全測定中最良、30秒超0件。[#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) |
| 推奨設定同士で本家と比べたら | **未測定。** 既定同士しか比べていない（[今後やるなら](HANDOVER.md#今後やるなら)） |

**合計CERだけを見て判断しないこと。** ファイル別の分散が9本で8.8%〜54.4%あり、
悪い側は「間違えている」のではなく「出していない」（カバレッジ47〜60%に対し
精度85〜87%）。歌唱区間が参照に丸ごと入っている作品もあるので、
到達不能な誤差も混ざっている。

指標の設計で3回、自分の数字に騙されて誤った結論を出しかけた。
[指標の設計欠陥（記録）](#指標の設計欠陥記録)と測定結果 #4・#6 に残してある。
**要点は5つ。CERは全文とブロックを両方見る。無字幕域の数字は解釈しない。
全文CERは `UPPER BOUND` が付いていないことを確認してから読む。
CERを見たらカバレッジと精度も見る（同じCERでも原因が逆のことがある）。
そして合計値だけで判断しない**（本家との比較は合計で同点、ファイル別で6勝0敗）。

---

### J. 同梱 ffmpeg が使われていなかった（2026-08-23）

**`vad.py` の3箇所が bare `"ffmpeg"` を渡していたので、同梱した ffmpeg ではなく
PATH の ffmpeg を探していた。** README は「ffmpeg も不要（同梱）」と書いているので、
**PATH に ffmpeg が無い環境では既定VAD（ten）が非WAV入力で落ちる。**

配布中の 0.9.0 exe で再現させた。PATH から `WinGet\Links` を1つ抜くだけで、
モデル・VAD・入力を同一にしたまま結果が変わる。

| 条件 | 旧 0.9.0 exe | 修正後（dist-slim） |
|------|-------------|-------------------|
| AAC 入力・PATH に ffmpeg あり | exit 0 / 字幕生成 | exit 0 / 字幕生成 |
| AAC 入力・**PATH に ffmpeg なし** | **`[WinError 2]` / exit 1 / 字幕なし** | **exit 0 / 字幕生成** |

**Amatsukaze から見た壊れ方が問題。** 終了コード1と `[ERROR]` は正しく出るが、
Amatsukaze は字幕生成の失敗を警告に落としてエンコードを続行するので、
**録画は完成して字幕だけ付かない。** README で XXL の欠点として書いているのと同じ形。

影響するのは `vad.py` を通る経路（既定の `ten`、`silero_v3/v4/v5`、`precomputed`）。
内蔵VAD経路（`silero_v5_fw` など）は PyAV でデコードするので無関係。
入力が既に16kHzモノラルWAVなら変換分岐に入らないので、これも無関係。

**修正は `audio_filter.get_ffmpeg_path()` に一本化した**（`_MEIPASS` を先に見る）。
`vad.py` に `_ffmpeg_exe()` を置いて3箇所から呼ぶ形で、実装の重複はしていない。

**[再パックの発動条件](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)に該当する**（コード変更・利用者が踏む不具合）。

### K. torch を外して配布サイズを削る（2026-08-23）

> **採用した。0.9.1 から既定ビルドが torch なしになった。**
> 当初 `WHISP_CARRIER_SLIM=1` の opt-in にしていたが、**向きが逆だった** —
> 環境変数を付け忘れると黙って 4.8GB のアーカイブができる。
> **既定を slim にし、torch を戻すほうを opt-in にした。**
>
> | 環境変数 | 挙動 |
> |---------|------|
> | （なし） | **既定。torch なし。これが配布物** |
> | `WHISP_CARRIER_WITH_TORCH=1` | torch を同梱。**silero バックエンドを凍結ビルドで測り直すとき用** |
> | `WHISP_CARRIER_FULL=1` | 上を含む + stable-ts（`--realign`）。stable-ts が torch を要るため |

動機は配布サイズが利用者側の最大の障壁だったこと。0.9.0 は payload 4.78 GB で、
**そのうち torch が 4273 MB（CUDA系が 4004 MB）。** しかも
**torch は既定経路に無い** — 推論は CTranslate2（59 MB）、既定VADは ctypes で叩く
ネイティブライブラリ（1.3 MB）。torch を使っていたのは
バナー・`--checkcuda`・silero バックエンドの3つだけで、
**silero は15本すべてで TEN に負けている。**

| | 0.9.0 | SLIM |
|---|------|------|
| payload | 4780 MB | **2240 MB** |
| torch | 4273 MB | 0 |
| 同梱 CUDA ライブラリ | torch/lib 経由 4004 MB | **13ファイル 1870 MB**（cuBLAS・cuDNN・nvrtc・nvJitLink） |
| 圧縮後（実績比 0.41） | 1999 MiB / **2分割** | **約 920 MiB / 単一ファイル** |

**分割アーカイブが不要になる**のが実質的な利点。

検証（`dist-slim`、`WHISP_CARRIER_SLIM=1`）。

| 確認項目 | 結果 |
|---------|------|
| CUDA 推論 | **通る。** `ctranslate2 4.8.1 \| device=cuda \| compute=float16` |
| 字幕出力の回帰 | **16kHzモノラルWAVで SRT/VTT の MD5 が記録値と完全一致** |
| `--version` / `--checkcuda` | 動く（`ctranslate2.get_cuda_device_count()` に切り替えた） |
| 内蔵VAD（`silero_v5_fw`） | **動く**（onnxruntime なので torch 不要） |
| `--vad_method silero_v5` | **exit 1 で理由を表示**（`_missing_backend` 経由） |
| J の修正 | 同時に検証済み（上の表） |

**失うのは `vad.py` 側の silero バックエンド3つ**（`silero_v3/v4/v5`）。
代替は内蔵VAD（`silero_v5_fw`）で、これは torch なしで動く。
**記録されている silero の測定値は再現できなくなる**ので、
測り直す必要が出たらスクリプト版を使うか、SLIM を外してビルドする。

**`silero_v3/v4/v5` を exe から落とすことは受け入れた**（2026-08-23）。
測り直す必要が出たら `WHISP_CARRIER_WITH_TORCH=1` でビルドするか、スクリプト版を使う。

**残っている選択肢。** 落とさずにサイズを削るなら `vad.py` の silero を
onnxruntime 経由に書き換える（`segments_from_probabilities()` はモデル非依存に
作ってあるので確率の供給元を差し替えるだけ）。**未実装で、優先度は低い** —
既定の TEN に15本すべてで負けている経路のために書く価値が薄い。

**波及した先。** README.md（特徴・導入・違いの表・VADの節・参考プロジェクト表・
テスト環境）、README_en.md（同）、THIRD-PARTY-NOTICES.md
（**scope の容量、節2の CUDA 一覧から cuFFT/cuRAND/cuSOLVER/cuSPARSE を削除、
節4に「これは環境の一覧で配布物ではない」の注記、節6に silero バックエンドの項、
再生成手順の `torch/lib` 参照**）。
**帰属と同梱物の一覧に関わるので、[再パックの発動条件](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)では「ドキュメントだけ」に該当しない。**

**踏んだ罠。** `excludes` に `torch` を入れても**torch の DLL は落ちない。**
PyInstaller が追加した CUDA ライブラリの依存関係を辿って `torch/lib` に再収集するので、
初回の SLIM ビルドは `cublasLt64_12.dll`（643 MB）と `cudnn_ops64_9.dll`（121 MB）を
**二重に同梱した**（766 MB の無駄）。`a.binaries` から `torch/` 宛のエントリを
落として解決している。

### N. CUDA_PATH が同梱の cuBLAS を隠していた（2026-08-24 報告・0.9.2 で修正）

**第三者環境からの最初の報告で、最初の報告が不具合だった。** 0.9.1 exe を
Amatsukaze から呼んで落ちている。

```
2026-08-24 22:01:39 [ERROR] F:\temp\Amatsukaze\amt9191378\a0-0-0-0-main.aac:
  Library cublas64_12.dll is not found or cannot be loaded
```

**原因。`CUDA_PATH` が設定されていると、CTranslate2 は cuBLAS を
`%CUDA_PATH%\bin` から読もうとし、同梱ぶん（`_internal`）が検索対象から外れる。**
`ctranslate2.dll` の文字列に `CUDA_PATH` と `SetDllDirectory` の両方がある。
CUDA 13 は `cublas64_13.dll`、CUDA 11 は `cublas64_11.dll`、アンインストール後は
そもそもディレクトリが無い。**どれも `cublas64_12.dll` を持たないので、
同梱しているのに「無い」と言われて停止する。**

**0.9.1 exe で再現させた。** `CUDA_PATH` を弄るだけで、モデル・入力・PATH を
同一にしたまま結果が変わる。**PATH は関係ない**（壊れた cublas を PATH の先頭に
置いても影響しなかった。CT2 の経路は PATH を見ていない）。

| `CUDA_PATH` | PATH の toolkit | 0.9.1 の結果 | 実際に読まれた cuBLAS |
|-------------|----------------|-------------|---------------------|
| v12.8（この開発機） | あり | exit 0 | **toolkit の `v12.8\bin`**（同梱ぶんではない） |
| v12.8 | なし | exit 0 | toolkit の `v12.8\bin` |
| 未設定 | なし | exit 0 | `_internal`（同梱ぶん） |
| 未設定 | あり | exit 0 | `_internal`（同梱ぶん） |
| **`bin\cublas64_13.dll` だけの偽 toolkit** | なし | **報告と同一のエラーで exit 1** | — |
| **存在しないパス** | なし | **同上** | — |

**この開発機では出ない。CUDA Toolkit 12.8 が入っていて `CUDA_PATH` がそれを指すので、
同梱ぶんが一度も使われないまま toolkit の cuBLAS で動いていた。**
[J](#j-同梱-ffmpeg-が使われていなかった2026-08-23)と同じ構図で、
**0.9.1 の回帰テスト6項目が全部通ったのはそのため。**

**修正（0.9.2）は2層。どちらも凍結ビルド限定**（スクリプト版は同梱ぶんが無く、
torch が自分のディレクトリを登録して解決している）。

| 層 | 実装 | 効くところ |
|----|------|-----------|
| **1. 原因を消す** | 起動時に `CUDA_PATH` / `CUDA_HOME` を自プロセスから落とす（`_use_bundled_cuda()`）。`os.environ.pop` は CRT 側しか書き換えないので `SetEnvironmentVariableW` も呼ぶ。あわせて `os.add_dll_directory(sys._MEIPASS)` | CT2 が探索先を差し替えなくなるので、通常の探索順で `_internal` が見つかる |
| **2. 探索に頼らない** | 同梱の `cublasLt64_12.dll` → `cublas64_12.dll` を**絶対パスで先に読む**（`preload_bundled_cuda()`）。**`device=cuda` のときだけ** | 探索先が何かに潰されても、**名前で要求された時点で既にロード済み**なので 0ms で解決される |

**層2を「PATH に足す」ではなく絶対パスにした理由。** PATH 方式は
**環境変数を書き換えるので子プロセスにも波及し、同名 DLL が PATH のどこかにあれば
そちらが勝つ余地が残る。** 絶対パスなら**同梱した実体そのものを名指しできる。**

**順序が要る。** `cublas64_12.dll` は `cublasLt64_12.dll` を import するので、
Lt を先に読む。そうすれば依存も名前解決で満たされ、探索が走らない。

**`device=cuda` に限定した理由。** 2ファイルのマップに約1秒かかる
（`cublasLt` 751ms + `cublas` 230ms、実測）。**GPU 経路では追加コストにならない**
（CT2 が直後に同じファイルをマップするので、払う時期が前倒しになるだけ）が、
**CPU 経路では丸損。** 実測でも CPU 実行では cublas が1つもロードされない。

**絶対パスの先読みで救えない場合もある**（実測）。**存在しない絶対パスへの
`LoadLibrary` は、同名モジュールがロード済みでも失敗する。**
つまり CT2 が `%CUDA_PATH%\bin\cublas64_12.dll` を**絶対パスで**開いていたら層2は効かない。
**効いたので、CT2 側は名前指定**だと確定した（報告者の PATH 回避策が効いた事実とも一致）。

**層ごとの検証用に環境変数を1つ用意した。** `WHISP_CARRIER_CUDA_FIX`
（`preload` = 層2のみ、`off` = 両方とも無効 = 0.9.1 相当）。**README には書かない。**
**これで「配布する exe そのもので不具合を再現できる」**状態になり、
回帰テストが本当にこの不具合を突いているかを毎回確認できる。

**副作用が1つある。この開発機でも同梱ぶんが読まれるようになった**
（`cublas64_12.dll` は toolkit 6,14,11,1283 → 同梱 6,14,11,1284）。
**公女殿下 #10 で A/B を取った結果、テキストは完全に同一**、
差は**401キューのうち13キューの時刻だけ**（+10ms〜920ms、2箇所に集中）。
**全文CERはテキストだけの指標なので不変。** 時刻を使う指標
（per-block CER・coverage）には微差が出る可能性があるが、再採点はしていない。

| | A（同梱 = 0.9.2 の既定） | B（toolkit = 0.9.1 の実質） |
|---|---|---|
| セグメント数 | 400 | 400 |
| キュー数 | 401 | 401 |
| SRT サイズ | 31,261 バイト | 31,261 バイト |
| SRT MD5 | `BD6603BA…` | `B27376DB…` |
| 差分 | — | **時刻行13本のみ。テキストの差分は0** |
| 所要 | 130.9s | 144.4s |

**0.9.2 の回帰テスト（12項目、すべて exe で実測。終了コードとロード元のフルパスまで見た）。**

| 項目 | 結果 |
|------|------|
| `--version` | exit 0・`whisp-carrier 0.9.2 \| ctranslate2 4.8.1 \| torch not bundled \| CUDA: True` |
| **偽 toolkit（`bin\cublas64_13.dll` だけ）** | **exit 0・`_internal` の cuBLAS・`[CUDA] ignoring CUDA_PATH`** |
| **同上・層2のみ**（`WHISP_CARRIER_CUDA_FIX=preload`） | **exit 0・`_internal` の cuBLAS。ピン留めだけで直ることの確認** |
| **同上・両層とも無効**（`=off`） | **exit 1・報告と同一のエラー。回帰テストが本当に不具合を突いていることの確認** |
| 両層とも無効・`CUDA_PATH` なし | exit 0（0.9.1 が素の環境では動いていたことの裏返し） |
| この開発機のまま（v12.8 あり） | exit 0・**`_internal` の cuBLAS**（0.9.1 は toolkit を読んでいた） |
| `--verbose true` | `[CUDA] pinned bundled cublasLt64_12.dll, cublas64_12.dll` が出る |
| **`--device cpu`** | exit 0・**cublas が1つもロードされない**（ピン留めを払っていない） |
| **出力の回帰**（16kHzモノラルWAV・tiny） | **SRT `AE218346…` / VTT `6A6791FD…` が記録値と完全一致** |
| J の回帰（AAC・PATH に ffmpeg なし） | exit 0・字幕生成・stderr 空 |
| `--vad_method silero_v5` | exit 1・理由と代替を表示（想定どおり） |
| `silero_v5_fw` | exit 0 |
| 生きた設定の保全 | `whisp-carrier.yaml` の MD5 が前後で不変（`399D1164…`） |

#### 報告者の環境と、報告者が見つけた回避策（機構の裏付けになった）

**報告は RTX 4080。**[旧スクリプト版を検証した Amatsukaze の作者](HANDOVER.md#第三者環境での動作報告)で、
**`_internal` を PATH の先頭に足す .bat を exe の隣に置いたら正常動作した**と報告があった。

```bat
@echo off
setlocal
set "PATH=%~dp0_internal;%PATH%"
"%~dp0whisp-carrier.exe" %*
exit /b %ERRORLEVEL%
```

**これで探索順が確定した。** `LoadLibrary` の順は
**exe のディレクトリ → `SetDllDirectory` の枠（CT2 が `%CUDA_PATH%\bin` に差し替える）
→ System32 → PATH**。**PATH は最後だが、探索はされる。**

- 枠が生きているとき（`CUDA_PATH` なし）は、**壊れた cublas を PATH の先頭に置いても
  `_internal` が勝つ**（上の表の4行目と、PATH 汚染の実測）
- 枠が潰れているとき（`CUDA_PATH` が的外れ）は、**PATH まで落ちてくるので
  `_internal` を PATH に入れれば救える**（報告者の .bat）

**2つの実測は矛盾していない。** そして **PyInstaller の枠を CT2 が上書きしている**という
読みは、この2つを同時に説明できる唯一の形。

**0.9.2 の修正との違い。** 報告者の .bat は**枠を潰したまま PATH で救う**ので、
toolkit 側に `cublas64_12.dll` があればそちらが使われる。
**0.9.2 は `CUDA_PATH` を捨てるので、常に同梱ぶんが使われる**（決定的になる）。
どちらも症状は消えるが、**再現性が要るのは後者。**

**副産物として、4080 で exe の残り全部が動いていることが分かった** —
モデル取得・TEN VAD・推論・Amatsukaze 連携・字幕出力。
**詰まっていたのは cuBLAS の解決1点だけ。**
**RTX 2070 での確認が控えているので、それは 0.9.2 で流してもらうのが望ましい。**

#### 決着：0.9.2 が 4080 と 2070 の両方で回避策なしに動いた（2026-08-27 報告）

**[1.0 の条件](HANDOVER.md#なぜ-10-ではなく-090-なのか)（現行 exe が他人の環境で1回動く）が満たされた。**
報告は作者のブログのコメント欄、rigaya 氏から。

| 日付 | 環境 | 版 | 結果 |
|------|------|----|------|
| 2026-08-24 | RTX 4080（`CUDA_PATH` = CUDA 11） | 0.9.1 | **停止。** `Library cublas64_12.dll is not found or cannot be loaded`。`_internal` を PATH の先頭に足す `.bat` で動作 |
| 2026-08-25 | RTX 2070（**`CUDA_PATH` 未設定**） | 0.9.1 | 正常動作（この環境は不具合の発火条件を満たさない） |
| **2026-08-27** | **RTX 4080 と RTX 2070 の両方** | **0.9.2** | **exe を直接実行して問題なく動作。`.bat` は不要になった** |

**4080 が効く1台。** `CUDA_PATH` が CUDA 11 を指しているので
**`%CUDA_PATH%\bin` に `cublas64_12.dll` が無い** — 0.9.1 で停止していたケースそのもの。
**そこで回避策なしに動いたので、修正が実環境で効いたことの確認になっている。**
**2070 は不具合と無関係な確認**（`CUDA_PATH` 未設定なので 0.9.1 でも動く環境）だが、
**sm_75 で exe 全体が通ることの確認としては有効**
（[既知の未確認 d](HANDOVER.md#5-宣言した動作環境の内側で落ちる経路イシュー待ちにした2026-08-23) の
「残る未確認は sm_75」がこれで埋まる）。

**あわせて Amatsukaze の紹介ページが書き換えられた。**
[sm_120 フォールバック説の訂正](HANDOVER.md#訂正して伝える必要がある説明sm_120-フォールバック説)は
2026-08-23 に送付済みで、**反映されたかは未確認と書いていたが、反映された。**
exe 版の導入手順に差し替わっている。

**この報告が価値を持った理由を1つ記録しておく。`.bat` が原因特定の決め手だった。**
「PATH に `_internal` を足すと動く」という事実から
**CT2 が DLL を名前で探していると確定**でき、
[層2（絶対パスでの先読み）](#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正)が
効くと判断できた。**存在しない絶対パスへの `LoadLibrary` は
同名モジュールがロード済みでも失敗する**ので、CT2 が絶対パスで開いていたら
層2は無効だった。**回避策の報告は、症状の報告より情報量が多いことがある。**

#### 他の DLL の探索も点検した（2026-08-25）

**方法。** 通常実行と、**探索先を潰したまま cuBLAS だけをピン留めした状態**
（`WHISP_CARRIER_CUDA_FIX=preload` + 偽 `CUDA_PATH`）の2回、
`Get-Process().Modules` を 80ms 間隔で全件記録して突き合わせた。
**DLL は FreeLibrary されない限り一覧に残るので、1回でも読まれたものは必ず出る。**

**結果。両方とも 181 モジュール、フルパスまで完全一致。**
**探索順に依存していたのは cuBLAS だけだった。**

| 分類 | 解決元 | 探索順への依存 |
|------|--------|---------------|
| PyAV の `av.libs` 25本・`ten_vad.dll`・`libsndfile`・openblas・VCランタイム・`python311.dll` | すべて `_internal` 配下 | **無い**（静的インポートか絶対パスの ctypes） |
| `ctranslate2.dll` / `libiomp5md.dll` / `cudnn64_9.dll` | `_internal\ctranslate2\` | **無い**（pyd の隣として解決される） |
| `cublas64_12.dll` / `cublasLt64_12.dll` | `_internal`（**0.9.2 でピン留め**） | **あった。ここだけが今回の不具合** |
| `nvcuda.dll` + DriverStore の `nvcuda64.dll` | System32 | ドライバ側。**同梱してはいけないもので、正しい挙動** |

**副産物。cuDNN の実体が1つもロードされていない。**
載るのは `_internal\ctranslate2\cudnn64_9.dll`（266KB のディスパッチャ）だけで、
`cudnn_ops` / `cudnn_adv` / `cudnn_engines_precompiled`（514MB）/ `cudnn_graph` /
`cudnn_cnn` / `cudnn_heuristic` / `cudnn_engines_runtime_compiled` は未使用。
`cudart64_12.dll` / `nvrtc64_120_0.dll` / `nvJitLink_120_0.dll` も未使用。
**同梱 CUDA 1870MB のうち、この経路で触られたのは cuBLAS の 752MB だけ。**

> **落とす判断はしない。** 測ったのは **RTX 5090 / float16 / clip 経路の1本だけ**で、
> **別世代の GPU では nvrtc / nvJitLink の実行時コンパイルや cuDNN エンジンが
> 必要になりうる**（[既知の未確認 d](HANDOVER.md#5-宣言した動作環境の内側で落ちる経路イシュー待ちにした2026-08-23)）。
> **2070 / 4080 の動作報告が揃うまでは触らない。**
> サイズ削減の候補としてだけ記録する（[K](#k-torch-を外して配布サイズを削る2026-08-23) の続きになる）。
>
> **もし cuDNN を呼ぶ経路が実在するなら**（別 compute_type・`--batched` など）、
> エンジン群は**名前で**ロードされるので**層1が効いている必要がある。
> 層2は cuBLAS しかピン留めしていない。**

**アプリ外から1つ入っていた。**
`C:\Program Files (x86)\ASUS\GPUTweakIII\GTIII-OSD\x64\GTIII-OSD64-VK.dll`。
ASUS のオーバーレイが自分のプロセスに注入されている。害は出ていないが、
**この種の注入 DLL（RTSS・Afterburner・Discord など）は凍結アプリの
原因不明のクラッシュの定番**なので、説明のつかない報告が来たら候補に入れる。

**教訓。「同梱した」と「同梱ぶんが使われた」は別のことで、後者は
ロードされた DLL のフルパスを見るまで確認できていない。**
`Get-Process` の `.Modules` で見れば1回で分かる。
[L（回帰テストの穴）](STATUS.md#l-回帰テストの穴を塞ぐ)に
「`CUDA_PATH` を cublas 不在のディレクトリに向けて実行」を追加した。

## 完了した着手の記録（F・G・I・H・E・A・C・B と運用メモ）

**以下は決着済み。読む必要があるのは、同じことを再検討するときだけ。**
各節の先頭に結論が入っている。

### F. TEN VAD を既定にする（完了）

数字は[修正後の9本](#修正後の9本2026-08-22)で確定している。触る先は5箇所。

| 項目 | 内容 |
|------|------|
| **`whisp_carrier.spec`** | **最大の未知。** `ten_vad` の記述が1つもない。wheel は `lib/Windows/x64/ten_vad.dll` を持つ形（Linux `.so` と macOS framework も同梱）なので、`collect_dynamic_libs` か `binaries` で明示しないと凍結ビルドで落ちる。ONNX 経由に切り替える選択肢もある |
| `requirements.txt` | `ten-vad==1.0.6.8` を追加 |
| `THIRD-PARTY-NOTICES.md` | Apache-2.0 の表記を追加 |
| `whisp-carrier.yaml` / `.example` | `vad_method: ten` と `vad_threshold: 0.75` |
| `whisp_carrier.py` | 既定値の変更（現在は `--vad_method ten --vad_threshold 0.75` の明示が必要） |

**閾値は 0.75。** 0.90 は4本で測って否定的（合計 +1.3pt 悪化。クレバテスの退行を
1.4pt 戻す代わりにアイプリで 4.6pt 払う）。レポートは `_eval/score-ten4-t090.txt`。
**なお 0.90 の検証は修正前の指標で行ったので、必要なら 9本で測り直すこと**
（推論は `_eval/hyp/ten-t090/` に4本ぶん済み）。

### G. 定型句フィルタは見送り（判断記録）

**実装しないことを決めた。** 数字は出ていて、既定（silero）で −0.3pt、TEN で −0.6pt。
[#20](#20-定型句フィルタは-06pt-効くが見送った) に実測と安全側の設計案がある。

**見送りの理由。** 得られるのは 0.6pt で、代わりに
**「作中のテレビ放送や配信を締める台詞が完全一致したら字幕1行を消す」機構**を
主経路に持つことになる。手持ち18本の参照には該当例が無いので誤爆は観測されて
いないが、**アニメでアナウンサーや配信者キャラが言う可能性は消えない。**
0.6pt との釣り合いが悪い。

**あわせて確認した2つの事実。**

- **ループ抑制では弾けない。** これは見送りの根拠ではなく、
  **見送ると残り続けるという意味の代償。** `loop_filter` はセグメント内の
  繰り返し検出なので、1回だけの非繰り返し出力に手がかりが無い。実測でも
  9本の TEN 出力でループ検出が拾った5件（35字）に定型句は1件も含まれない。
  部分的に効くのは `whisp_carrier.is_hallucination()` の `MAX_DUPES = 2` だけで、
  これは**連続**3回目以降しか落とさない。実測の分布は散発
  （クレバテスは 04:17 / 06:37 / 10:59 / 13:44 / 23:20 / 27:27）なので発動しない
- **本家も同じものを出す。** 9本で XXL 11件、旧既定 15件、TEN 43件。
  XXL の終末ツーリング 00:26 は
  「ご視聴ありがとうございます高評価とチャンネル登録よろしくお願いします」。
  つまり Whisper 系共通の残存欠陥で、こちらだけの弱点ではない

**代替案も潰れている。** 「無音区間でカットオフする」案は音のレベルを測って否定した。
定型句は通常の発話と同じレベル（中央値 −28.7 対 −28.2 dBFS）に出ていて、
**無音への幻覚ではなく「音のある非音声」への幻覚**なのでエネルギーでは触れない。
[#20](#20-定型句フィルタは-06pt-効くが見送った) の該当節。**つまり選択肢は「定型句か、何もしないか」の二択だった。**

**そして定型句側はジャンルを広げると弱くなる。** バラエティは締めに本当に
「ご視聴ありがとうございました」と言い、ドラマの配信者役は「チャンネル登録」と言う。
**最も発火する場所で最も誤爆する不安定なルール**で、
Amatsukaze の利用者が映画やドラマも録るなら実運用で効いてくる。

**再検討する条件。** 0.6pt では動かないが、次のどれかが起きたら見直す価値がある。

- 実運用で目に付くほど頻発する（TEN は silero の3倍出すので、素材次第ではありうる）
- **`no_speech_prob` に動作点が見つかる。** 唯一手つかずの信号で、
  出力に1フィールド足して分布を測るだけなら誤爆リスクはゼロ（[#20](#20-定型句フィルタは-06pt-効くが見送った) の該当節）
- 別の理由で後段フィルタを作ることになり、相乗りできる

そのときは `_eval/_strip_filler.py` の `FILLER` と `_eval/_filler_risk.txt`、
`_eval/_filler_energy.txt` から始められる。

### I. 一挙の時間内訳を取る（閾値を振る前に）

**一挙の所要時間を削りたいなら、まずどこに時間が行っているかを分ける。**
既存ログから分かることは分かってしまったので、残っているのは1点だけ。

#### 分かっていること：閾値は時間に効くが、無料ではない

`_eval/_threshold_cost.txt`（既存ログの `[VAD] N segments` と `[STT] Done in Xs`）。

| 素材 | クリップ数 | 出力セグメント | 推論時間 |
|------|-----------|--------------|---------|
| クレバテス 0.75→0.90 | 38 → **65** | 407 → 378 | 97.8 → 97.6s |
| 終末ツーリング 0.75→0.90 | 51 → **70** | 345 → 323 | 81.4 → 80.2s |
| 死亡遊戯 0.75→0.90 | 74 → 81 | 262 → 227 | 80.3 → **68.5s** |
| アイプリ 0.75→0.90 | 50 → **73** | 362 → **266** | 82.0 → **64.4s** |
| **合計** | **+76** | **−182** | **−30.8s（−9%）** |

**コストはクリップ数ではなく出力トークン量で決まる。** クリップ数は増えているのに
時間が減っていて、削減幅は出力減少幅に追随する（アイプリ −96セグメントで −17.6秒が最大）。
`clip_timestamps` は各クリップを30秒にゼロパディングするので
「クリップ数がコストを決める」と予測したが、**外れた。** RTX 5090 では encoder が速く、
**自己回帰デコードが支配的**。

**したがって閾値による高速化は速度と精度のトレードそのもので、独立した軸ではない。**
0.90 は4本で 9% 速いが +1.3pt 悪化する（旧指標）。同じ形が `collect` にもあり、
一挙で 13% 速いが +2.3pt（[該当節](#一挙と追加素材の再採点2026-08-22)）。どちらも既に却下したトレード。

#### 分かっていないこと：TEN 自体の実行時間

**閾値は VAD の計算時間には一切効かない。** TEN は 256サンプルごとにモデルを回し、
閾値は出てきた確率に当てるだけなので、どう振っても VAD の所要時間は同じ。

そして**そこだけが精度コストなしで削れる可能性のある時間**で、しかも未測定。
フレーム単位の Python ループなので、5時間の素材では silero より不利になりうる。
24分ものでは `[VAD] Running ten...` から `[VAD] N segments` までが体感で問題にならない
範囲だが、一挙で線形に伸びるなら効いてくる。

```powershell
# 一挙1本を TEN で通し、ログの [VAD] 行の時刻差を見る。
# [VAD] Running ten... から [VAD] N segments までが VAD、残りがデコード。
python eval/run.py --config ten-clip -m large-v3 --tag ten-long `
                   --only 202503231430000102
```

**判断の分岐。**

- **VAD が支配的だった場合**：閾値は打つ手にならない。効くのはホップ長
  （`TenVad(hop_size=256)` は 16ms 相当）を粗くするか、確率計算をベクトル化するか、
  ONNX 経由に替えるか。**いずれも精度に影響しうるので測ってから**
- **デコードが支配的だった場合**：閾値か `collect` しか無く、どちらも精度を払う。
  その場合は「一挙は遅い」を受け入れるのが筋（現状 clip で 1195.7s ≒ 実時間の 3.7% で、
  すでに実用範囲）

### H. 一挙2本の再採点（推論不要）

> **再採点は済んだ**（[結果](#一挙と追加素材の再採点2026-08-22)）。保持キューが最多の2本で、
> 参照の発話時間と画面時間が34分・42分ずれていた。ハクメイ 20.5% → 19.2%、
> マジルミエ 17.3% → 16.0%。**残っているのは一挙での TEN 測定だけ。**

**一挙は silero のままで、TEN では未測定。** 24分ものでの利得は素材で桁違いに違い
（会話劇 0.1〜1.4pt、子供向け 13〜18pt）、一挙2本はどちらも会話劇寄りなので
**利得は小さい側に出る見込み**。それでも測る価値があるのは、
[I](#i-一挙の時間内訳を取る閾値を振る前に) の時間内訳が同じ実行で取れるため。

```powershell
python eval/run.py --config ten-clip -m large-v3 --tag ten-long `
                   --only 202503231430000102 --only 202607111500000202
python eval/score.py --hyp _eval\hyp\clip-long --hyp _eval\hyp\ten-long `
                     --exclude-songs --report _eval\score-long-ten.txt
```

**採点は24分ものと分けること。** 帯状DPのコストが桁違いで、
一挙は `--only` で明示しないと事故る。

### （済み）E. TEN VAD の検証を広げる

> **完了。** 9本で決着し（[修正後の9本](#修正後の9本2026-08-22)）、既定も差し替えて
> exe も作り直した（[F](#f-ten-vad-を既定にする完了)）。
> **以下はこの節が最優先だった時点の記録で、残タスク一覧はもう有効ではない。**
> 唯一残っているのは一挙での TEN 測定（[H](#h-一挙2本の再採点推論不要)）。

[#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) で4本を測って既定を上回った。**既定を差し替える前に必要なもの。**

```powershell
# 残り5本（狙い2本＋健全2本は済み）
python eval/run.py --config ten-clip -m large-v3 --tag ten-t075 `
                   --extra=--vad_threshold --extra=0.75 --exclude 2025032314
# クレバテスの退行を戻せるか
python eval/run.py --config ten-clip -m large-v3 --tag ten-t090 `
                   --extra=--vad_threshold --extra=0.90 --exclude 2025032314
python eval/score.py --hyp _eval\hyp\clip-prodloop --hyp _eval\hyp\ten-t075 `
                     --hyp _eval\hyp\ten-t090 --hyp _eval\hyp\xxl `
                     --exclude-songs --report _eval\score-ten9.txt
```

| 項目 | なぜ必要か |
|------|-----------|
| 残り5本 | 4本は狙い2本＋健全2本で、**選び方が結論に有利に働いていないかを確認していない** |
| `ten 0.90` | クレバテスが唯一の退行（+4.5pt）。死亡遊戯では 0.90 で精度が silero 並みに戻った |
| 一挙（4〜5時間） | 未測定。**フレーム単位の Python ループなので VAD 自体が silero より遅い可能性がある** |
| exe 同梱 | wheel に prebuilt な `ten_vad.dll` が入る形。PyInstaller が拾うか未確認。ONNX 経由に切り替える選択肢もある |
| `THIRD-PARTY-NOTICES.md` | Apache-2.0 の表記追加 |
| `requirements.txt` | まだ入れていない。既定にするなら必須 |

済んだもの（この節の後半に記録として残してある）。

- **exe の再ビルドと検証** — UTF-8化を入れて再ビルド済み
- **yaml を beam 5 に変更** — `override: true` で Amatsukaze の 10 を上書きすることを実機確認
- **素材の追加と採点** — 追加5本を測り終えた（[#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった)）
- **実運用設定の検証** — beam 10 は悪い、`language ja` は無影響（[#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)）
- **C の仮説検証** — `condition_on_previous_text=true` は破壊的だった（[#15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定)）
- **実運用条件7本の再採点** — ループ抑制後で 18.4% → **15.6%**
  （測定結果の「実運用条件（アイプリ・死亡遊戯を除く7本）」節）

### （不要になった）C. 死亡遊戯の取りこぼし

> **着手不要。** TEN VAD が死亡遊戯を 42.4% → **30.5%** にし、XXL（35.1%）も
> 上回った。この節が設計しようとしていた「素材依存で `novad` に切り替える
> 発動条件」は要らない（[修正後の9本](#修正後の9本2026-08-22)）。
> **以下は記録。発話率トリガーの検討過程として読む価値はあるが、
> 数値は旧指標なので参考情報。**

**取りこぼしは全体としては Whisper の限界だが、1本だけ例外がある。**

| 作品 | うち（clip） | XXL | 差 |
|------|-------------|-----|-----|
| 死亡遊戯 #09 | 59.7% | **69.1%** | **XXL が 9.4pt 良い** |
| アイプリ | 47.3% | 49.3% | ほぼ同じ |
| ニンジャラ | 75.4% | 75.3% | ほぼ同じ |
| 子供向け4本（追加素材） | 67.7% | 69.1% | ほぼ同じ |

**この節の仮説（`condition_on_previous_text=true`）は測って否定された。**
経緯と数字は[測定結果 #15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定)。
要点は3つ。

- カバレッジが 59.7% → **21.3%** に落ちた。仮説は 69.1% への接近を予測していたので**逆向き**
- 推論時間が 65.4s → **384.2s（5.9倍）**。各窓がトークン上限までデコードしている
- 出力が **18:16 で止まる**（既定は 27:05 まで出す。尺は30:00）
- **`loop_filter` は1件も落とさなかった。** 故障の形が
  「セグメント内の繰り返し」ではなく**セグメントをまたぐ縮退**なので検出器の対象外

**しかし改善策そのものは既に手元にある。`novad` が XXL を全指標で上回っている。**
`_eval/score-noloop.txt` に既に入っていた数字（推論不要で読める）。

| 設定 | CER全文 | coverage | precision | per-block |
|------|--------|----------|-----------|-----------|
| clip（現在の既定） | 42.4% | 59.7% | 87.3% | 42.8% |
| **novad + ループ抑制** | **30.9%** | **71.6%** | 86.4% | **32.5%** |
| XXL | 35.5% | 69.1% | 78.4% | 41.7% |
| cond-prev（今回・否定） | 79.4% | 21.3% | 87.6% | 83.3% |

**つまり「XXL に 9.4pt 負けている」という差はもう存在しない。**
`novad` に切り替えれば CER 30.9% / カバレッジ 71.6% で XXL（35.5% / 69.1%）を抜く。
ループ抑制を通すことが前提で、素の novad は精度64.5%まで落ちるので単独では使えない。

**したがって C の残りは精度の問題ではなく発動条件の問題になった。**

#### 健全素材に novad を当てたときのコスト（実測）

**「全ファイルで novad に切り替える」は選べない。** 健全6本を production 検出器つきで
直接採点した（`_eval/score-novad-clean6.txt`、参照 25,698字、推論なし。
`novad-prodloop` は `strip_loops.py --detector production` で生成）。

| | clip（現在の既定） | novad | 差 |
|---|---|---|---|
| 全文CER | **13.2%** | 15.5% | **+2.3pt 悪化** |
| coverage | 88.5% | **89.3%** | +0.8pt 改善 |
| precision | **92.3%** | 89.6% | **−2.7pt** |
| per-block plain | **14.6%** | 15.9% | +1.3pt 悪化 |
| 長さ比 | 0.96 | 1.00 | — |
| 30秒超 | 0 | 0 | — |
| thin | **0** | 3 | — |
| 無字幕域への出力 | 181.0s / 1039字 | **486.1s / 1580字** | **2.7倍** |

**交換の向きは [#7](#7-vadはカバレッジを売って精度を買う交換だったnovad対照) と同じで、健全素材では割に合わない。**
カバレッジは 0.8pt 上がるが精度が 2.7pt 落ちて、差引で悪化する。

**ファイル別。損失は2本に集中していて、残り4本はむしろ改善する。**

| 作品 | 発話率 | clip CER | novad CER | 差 | novad の内訳 |
|------|-------|---------|-----------|-----|-------------|
| LIAR GAME #10 | 0.58 | 11.1% | **9.5%** | **−1.6** | cov 89.5→91.1 / prec 94.2→94.6 |
| Summer Pockets #26 | 0.51 | 14.0% | **12.2%** | **−1.8** | cov 87.4→88.8 / prec 92.3→93.2 |
| 桃源暗鬼 | 0.60 | 12.6% | **12.4%** | −0.2 | ほぼ同一 |
| 公女殿下 #10 | 0.62 | **8.8%** | 10.3% | +1.5 | cov 92.5→93.9 / **prec 95.2→92.8** |
| クレバテス #06 | 0.50 | **16.7%** | 23.3% | **+6.6** | **cov 84.8→82.6** / prec 90.4→86.5 |
| 終末ツーリング #12 | 0.40 | **18.3%** | 30.5% | **+12.2** | cov 85.8→**89.0** / **prec 90.0→77.3** |

**壊れ方が2本で違う。**

- **終末ツーリングは「拾いすぎ」。** カバレッジは 3.2pt **上がる**のに精度が
  12.7pt 落ちて、長さ比 1.15、判定は `INVENTION dominates`。
  **そして production 検出器はこの1本から1件も落とせない**
  （`strip_loops` の出力で dropped 0）。[既知の問題 1](HANDOVER.md#1-ハルシネーションループは解決残るのはループ形をしない余剰出力) の
  「ループ形をしない余剰出力」がそのまま出ている
- **クレバテスは「取りこぼしも増える」。** カバレッジが 2.2pt **下がる**。
  こちらは検出器が1114字（18%）落としていて、
  素の novad 43.2% → 23.3% まで回復させたうえで**なお 6.6pt 負けている**

**発動条件の設計に効く事実が1つ。損失最大の終末ツーリングは、
健全側で最も発話率が低い（0.40）＝トリガー境界の隣にいる。**
狙う2本は 0.23（死亡遊戯）と 0.32（アイプリ）なので、
**0.32 と 0.40 の間に線を引くことになる。余裕は 0.08 しかない。**
ここが薄いので、[素材の項](HANDOVER.md#素材を足すべきか判断済み)のとおり
低発話率の素材を足さずに閾値を決めるのは危険。
（発話率は `clip-prodloop` の出力占有／尺で出した近似値。
**VADの検出発話そのものは clip 経路がログに出していない**ので、
トリガーを作るならまずそこを直すこと。）

#### novad が増やしているのは「静かな台詞」ではなく歌唱帯（要注意）

**C の仮説はもう1つ間違っていた可能性がある。** 死亡遊戯の取りこぼしを
「静かな台詞」と説明してきたが、**novad の増分の最も大きいスパンは
3本すべてで歌唱区間だった。** `paired diff` の上位がそれを示している。

```
死亡遊戯       novad のみ 27:44-28:05  翼からくてもこの呪文がある / 僕たちならば昨日より高く飛べる
死亡遊戯       novad のみ 28:53-29:15  つないだ手と手は永遠の証 / 力は絶対誰よりも高く飛べる
アイプリ        novad のみ 19:20-20:19  大好きぴょんぴょんカバサクソ / バズリウムチェンジドピョン
アイプリ        novad のみ 01:08-01:30  笑顔でジャンプ / メラメラ / 信じるハートが
終末ツーリング   novad のみ 20:02-20:27  ハイウェイ止めたまたバレたままの / ワンウェイ指さした心が動く
終末ツーリング   novad のみ 22:15-22:39  淡い期待零れずっと突き刺したくて / 掴んでみたご飯はない
```

歌唱帯を時間で切って文字数を数えた（`_eval/_novad_where.py` / `.txt`）。

| 素材 | 全体の増分 | うち歌唱帯 | 歌唱帯の clip 側 |
|------|-----------|-----------|-----------------|
| 死亡遊戯 #09 | +636字 | **+255字（40%）** | **0字** |
| アイプリ | +2008字 | **+556字（28%）** | **0字** |
| 終末ツーリング #12 | +730字 | **+233字（30%）** | 18字 |

**VADは歌唱区間を完全に落としている**（clip 側がほぼ0字）。
novad はそこを埋めるが、**同じ挙動が素材によって得にも損にもなる。**
ARIB字幕は歌に字幕を付けるので参照側にテキストがあり、
**Whisperの歌唱書き起こしが当たれば coverage が上がり、外れれば precision が落ちる。**
アイプリは当たった側（CER 54.4% → 39.4%）、終末ツーリングは外れた側
（18.3% → 30.5%、`掴んでみたご飯はない` のような聞き間違い）。

**これは発動条件の設計に決定的に効く。** 分けたい2群の違いは
「発話率」ではなく**「その素材の歌をWhisperがどれだけ正しく歌詞として書き起こせるか」**で、
**実行時には知りようがない。** 発話率トリガーが偶然この2群を分けているだけの
可能性があるので、**閾値を決める前に「歌唱帯を除いた本編だけで novad が勝つか」を
確かめること。** 上の表の「上記以外（本編）」も novad のほうが多い
（死亡遊戯 +381字）が、それが参照に一致しているかは未測定。

**切り分けの方法。** `score.py` に時間範囲の絞り込みが無いので、
歌唱帯のセグメントを落としたコピーを作って採点するのがいちばん安い
（`strip_loops.py` と同じ作り。推論不要）。**これをやるまで
「novad は死亡遊戯型の取りこぼしを直す」と書かないこと。**

なお字幕用途としての望ましさも別問題で、**歌詞を誤って書き起こすのは
出さないより悪い**という判断もありうる。CERは前者を報酬にする。

利得が出るのは発話率の低い2本（死亡遊戯 −11.5pt、アイプリ −15pt）。
**[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果) で否定されたのは
「発話率で `--vad_threshold` を動かす」ルールで、「発話率で VAD を外す」は別物。**
否定の根拠だったニンジャラ（発話率49%で最大の利得）は
ループ抑制で解決済みなので、**発話率トリガーが効かせたい相手と効く相手の食い違いも
解消している。** ここは[今後やるなら 3](HANDOVER.md#3-novad-を活かす道とvadログの不足)と同じ話になったので、
着手するならそちらに合流させること。

**先に必要な部品が1つある。clip 経路は発話秒数をログに出していないので、
トリガーに使う発話率が実運用ログから読めない**（collect 側は出す）。

### （済み）yaml を beam 5 に戻した

`dist/whisp-carrier/whisp-carrier.yaml` と `whisp-carrier.yaml.example` の
両方で `beam_size: 5` / `best_of: 5` にした。根拠は
[測定結果 #13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)。

**whisper-option 欄に 10 が入っている条件で実機確認済み。**

> **訂正（2026-08-22）。この記録は当初「Amatsukaze が `--beam_size 10 --best_of 10` を
> 渡してくる」とソフトの挙動として書いていたが、実際は Amatsukaze の
> プロファイル画面の whisper-option 欄に入っていた文字列**で、
> **以前の実験の名残だった。** つまり欄を空にすれば既定の 5 が入る。
> **上書き機構で殴り返す必要はなく、欄を直すのが素直。**
> `-m large-v3` のほうは whisper-model ドロップダウンから来る本物の Amatsukaze 由来。
>
> **モデル欄には `未指定` がある**（候補は `自動` / `未指定` / `small` / `medium` /
> `large-v1` / `large-v2` / `large-v3` / `large-v3-turbo`）。
> **`未指定` を選ぶと `-m` が渡らない**ので、こちらの既定（`large-v3`）が入り、
> **設定ファイルの `model:` が `override` なしで効く。**
> 「Amatsukaze は `-m` を渡してくるからモデル変更には `override: true` が必要」
> という記述はこれで回避できる。
> **そして `自動` は `-m large-v3-turbo` を渡す**（[該当節](#モデル欄の-自動-は-large-v3-turbo-になる)）。

```
[CONFIG]   beam_size = 5  (overrides CLI 10)
[CONFIG]   best_of = 5  (overrides CLI 10)
```

`override: true` が効いているので **Amatsukaze 側の追加オプション欄は触っていない。**
設定ファイル機構を足した動機どおりの使い方になった。

### （済み）exe の再ビルドと検証

UTF-8化を入れて再ビルドし、`dist/whisp-carrier/whisp-carrier.exe` は現行。
`BUILD_EXIT=0`、`[spec] ffmpeg verified (known LGPL build)` と
`[spec] restored config:` も出ている。確認した3点。

| 確認項目 | 結果 |
|---------|------|
| 凍結ビルドで `reconfigure()` が効くか | **効く。** exe の stdout/stderr のバイト列を直接取って UTF-8 として無エラーでデコードできた（`raw bytes: 1392`、非ASCII行なし） |
| `[CONFIG]` の自動検出 | **動作。** `beam_size = 10` / `best_of = 10` / `language = 'ja'` / `standard_asia = True` / `vad_method = 'silero_v5'` すべて反映、`[VAD] via clip_timestamps` |
| **字幕出力が再ビルドで変わらないか** | **SRT / VTT ともに MD5 完全一致**（`AE21834629D4762F0BCD2405E102D1DB` / `6A6791FD97DE025C91A202223185C868`）。`test_speech.wav` を `-m tiny -f srt vtt` で処理して前後比較 |

**3つめが実務上いちばん重要。** 今回の変更はコンソール出力のエンコーディングだけで、
字幕ライタは `open(..., encoding="utf-8")` を明示しているため出力は不変。
**すでに生成済みの VTT を作り直す必要はない。**

**ビルドが途中で死んだ場合の見分け方**（一度踏んだ）。`dist/whisp-carrier/` に
`whisp-carrier.yaml` が残っているかを見る。spec は COLLECT の後で書き戻すので、
**config が残っていれば COLLECT は通っている。** Analysis 段（開始から2分ほど）で
死んだ場合は `dist/` に手が付かないので、古い exe がそのまま残る＝壊れない代わりに
**再ビルドしたつもりで古い exe を使う危険がある。** exe のタイムスタンプを見ること。

### A. Amatsukaze連携テストの再実行（フィルターを外した条件）

> **先に exe を再ビルドすること。** `whisp_carrier.py` に VAD の所要時間と
> 発話秒数のログを足した（[一挙2本](#一挙2本の-ten-測定2026-08-22決着)の測定に必要だった）ので、
> `dist/whisp-carrier/whisp-carrier.exe` は現行ソースより古い。
> 変更は print 文だけなので字幕出力は変わらないが、
> **`[VAD] ten: N regions | Xs of speech | detected in Ys` が実運用ログに出るのは
> 再ビルド後**で、それが連携テストで見たい行の1つになる。
> **再ビルドは済んでいる**（[ビルド検証](#ビルド検証済み5回)）。手順と確認項目は
> [（済み）exe の再ビルドと検証](#済みexe-の再ビルドと検証)にある。

### ✅ 完了。実運用と測定が同一素材で一致した（2026-08-22）

**フィルターを外した条件で再実行し、4つの確認項目すべてが通った。**
しかも回した素材が**測定素材と同じ回**（ニンジャラ「花火が開く夜空に」、
`202410260730010102`）だったので、**実運用ログと eval ログを直接突き合わせられた。**

| 確認項目 | 結果 |
|---------|------|
| `[CONFIG]` の override | `beam_size = 5 (overrides CLI 10)` / `best_of = 5` / `language = 'ja'` / `standard_asia = True` |
| TEN が既定で入るか | `[VAD] threshold 0.75 (default for ten)` |
| 再ビルドの反映 | `[VAD] ten: 30 regions \| 1246.7s of speech \| detected in 8.8s` |
| `[LOOP]` の日本語 | `ヤンヤンヤンヤンヤンヤンヤンヤン` が読める（`?` 羅列も `\uXXXX` も無し） |
| 整形 | `[FMT] width=16 lines=2 sentence=True gap=3.0 \| 411 -> 431 cues` |
| 時間 | `[STT] Done in 101.8s \| dur=1434.9s`、Amatsukaze の字幕生成全体で 110.49秒 |

**同一素材での一致（下の「測定条件と実運用条件は同じではない」を覆した）。**

| | eval（`_eval/hyp/ten-new/`） | Amatsukaze（実運用） |
|---|---|---|
| VAD 区間数 | 30 | **30** |
| 発話秒数 | 1246.7s | **1246.7s** |
| 音声長 | 1434.9s | **1434.9s** |
| セグメント数 | 412 | **412** |
| ループ抑制 | 1件16字（00:22:37） | **同じ1件16字・同じ時刻・同じテキスト** |
| 推論時間 | 172.1s（**2並列で実行**） | 101.8s（単独） |

**VAD 区間・発話秒数・セグメント数・落としたループが完全一致。**
これは記録上「成立しない」と書いていた比較で、成立した理由は3つ。

1. **yaml で beam を5に戻した**ので、whisper-option 欄の 10 が測定と揃った（[#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)）。
   **欄を空にしても同じ結果になる**（既定が 5 なので）
2. **`--language ja` は出力を1セグメントも変えない**と確認済み（同上）
3. **フィルターを使わない方針で決着した**ので、両者ともフィルターなし

**残る差は字幕整形（`standard_asia`）だけで、それは全文CERを動かさない**
（採点時に空白を完全除去するため。[該当節](#追加素材を測るときの手順素材は配置済み)）。
**したがって配布している 16.1% は、この経路で実際に出てくる品質そのもの。**

時間の差（172.1s 対 101.8s）は eval 側を**2プロセス並列**で回したためで、
単独実行の 101.8s が本来の値。24分ものが**字幕生成込みで110秒**。

**唯一のノイズも消した。** 本番ログに torchaudio の `UserWarning`
（2.9 で `load_with_torchcodec` に変わる予告）が混ざっていた。動作に影響は無いが
**利用者が「エラーが出た」と誤解しうる**ので、`vad.py` の先頭で
メッセージ指定の `warnings.filterwarnings` を1つ入れて抑えた。
**カテゴリ全体ではなくメッセージで絞ってある**ので、torchaudio の本物の警告は通る。

---

<details>
<summary>（記録・覆った）測定条件と実運用条件は同じではない（時間比較は成立しない）</summary>

> **この節は上の一致測定で覆った。** 当時は beam が 10 対 5 でずれていて、
> 時間の一致を根拠にできないという話だった。**yaml で 5 に戻した後は
> 同一素材で VAD 区間・セグメント数・ループまで一致する。**
> 以下は「時間の一致を実運用＝測定の根拠に使うな」という教訓として残す。

**`eval/run.py` と Amatsukaze はデコード設定が違った。** これに気付かず
「フィルターを外せば131秒に戻る、一致すれば実運用＝測定」と書いていたが、
**フィルターとは無関係に一致しない。**

| | `eval/run.py`（測定） | Amatsukaze（実運用） |
|---|---|---|
| `--beam_size` | **5**（既定） | **10** |
| `--best_of` | **5**（既定） | **10** |
| `--language` | **None＝自動判定** | `ja`（yaml） |
| 字幕整形 | **なし** | `standard_asia`（yaml） |
| 設定ファイル | `--no_config` で無効 | 有効（`override: on`） |
| 出力 | `-f json` | `-f srt vtt` |

`eval/run.py` が渡すのは `-f json --no_config --beep_off` と VAD 系だけで、
`CONFIGS` に beam / language は入っていない。**つまり記録されている全CER値
（22.0% / 18.4% ほか）は beam_size 5・自動言語判定・整形なしの数字。**

**当初ここに「beam 5 → 10 でデコード量が約2倍、それが実運用238.3秒と測定131.3秒の
差」と書いたが、実測で否定された。** 同一ファイル（クレバテス #06、同じWAV、
VADも同じ19区間）で比べると、

| | beam 5（記録 `clip-fixed`） | beam 10 | 差 |
|---|---|---|---|
| 推論時間 | 131.3s | **135.0s** | **+2.8%** |
| セグメント数 | 355 | **344** | −11 |

**beam_size のコストは約3%で、時間差の説明にはならない。** GPU ではビーム幅が
バッチ内で処理されるので、ほぼ無料に近い。`--temperature` の既定が 0 なので
ビーム探索が使われ、`best_of` は温度フォールバック時のサンプリング用。

**実運用の238.3秒がなぜ長いのかは未解明。** 残っている候補は、
**素材が別作品であること**（あの実行は別番組で、`[VAD] 35 segments` と
測定の19より多い＝デコード対象の発話が多い）と、
**温度フォールバックの再試行**（`best_of 10` は再試行時のサンプリング数を倍にする。
幻覚が出やすい素材ほど再試行が増える）。**同一素材で測らないと切り分けられない。**

**いずれにせよ時間の一致を「実運用＝測定」の根拠に使うのは筋が悪い。**
精度で比べるべきで、そのために条件を寄せる。

- 測定を実運用に寄せる → `eval/run.py --extra=--beam_size --extra=10
  --extra=--best_of --extra=10 --extra=--language --extra=ja`
- 実運用を測定に寄せる → yaml の `beam_size` / `best_of` を外す

**どちらが良いかは測って決着した。beam 5 が良い**
（[#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)）。
yaml を 5 に戻したので、**現在は実運用と測定のデコード設定が揃っている。**
残る違いは字幕整形だけで、[整形は全文CERに影響しない](#追加素材を測るときの手順素材は配置済み)。

</details>

### モデル欄の `自動` は large-v3-turbo になる

**Amatsukaze の whisper-model を `自動` にすると `-m large-v3-turbo` が渡ってくる**
（実機で確認）。候補は `自動` / `未指定` / `small` / `medium` / `large-v1` /
`large-v2` / `large-v3` / `large-v3-turbo`。

**これは黙って別モデルで走るという意味。** `large-v3-turbo` も `turbo` も
`FALLBACK_BUILTIN_MODELS` に入っているので普通に解決してしまい、
ログの `[MODEL]` 行を読まないと気付けなかった。

**turbo が構造的に危ういと考える理由。** turbo は large-v3 の encoder をそのまま使い、
**decoder を 32層 → 4層に削ったもの**（[OpenAI のモデルカード](https://huggingface.co/openai/whisper-large-v3-turbo)・
[リリース議論](https://github.com/openai/whisper/discussions/2363)。速いが品質はわずかに落ちる、という説明。
※内容は要約している）。**そしてこのプロジェクトの優位は全部 decoder 側にある。**

- ハルシネーションループは自己回帰デコードの失敗モード
- 30秒超セグメントも thin セグメントも、キューを置くタイムスタンプトークンの生成＝decoder の仕事
- `condition_on_previous_text=False` なので、**文脈で立て直す余地も無い**
- 勝ちの中身は VAD（切り出し）＋ループ抑制＋サニタイズで、**decoder が弱ると抑制側の負荷が上がる**

**測っていない。そして測らない判断をした。** 理由は、
**数字が出ても推奨（`large-v3` に固定）が変わらない**から。
得られるのは警告文の精度だけで、`自動` = turbo という事実のほうが読者にとって有用。
model 軸を開けると medium / distil と続いてしまうのもある。
**（参考にならない観測が1つ。7秒のテスト音声で large-v3 が3セグメント、turbo は1セグメント。
尺が短すぎて証拠にはならないが、粗くなる方向ではある。）**

**採った対処は実行時の注意書き。** `whisp_models._reduced_decoder_notice()` が
名前に `turbo` / `distil` を含むモデルに対して `[MODEL]` の直後に3行出す。

```
[MODEL] built-in Whisper model: large-v3-turbo
[MODEL] NOTE: 'large-v3-turbo' is a reduced-decoder Whisper variant (turbo runs 4 decoder layers against large-v3's 32).
[MODEL]   The accuracy figures this project publishes are large-v3. This model is not measured against the reference set, and loop hallucinations and over-long segments are decoder-side failures.
[MODEL]   Amatsukaze's whisper-model 'auto' resolves to large-v3-turbo; pick large-v3 explicitly to match the published numbers.
```

**モデル名をホワイトリストで弾く案は却下した。** `--model` はローカルディレクトリと
Hugging Face のリポジトリIDも受ける設計なので、閉じると `-m anime-whisper` や
自前ファインチューンの経路まで死ぬ。**警告して選択は利用者に残す**のが筋。

**設定ファイルの使い道が1つ増えた。** `自動` のままでも
`model: large-v3` + `override: true` を書けば打ち消せる。
[配布は `.example` だけ](#b-配布アーカイブサイズ上限に引っかかる)という判断への唯一の反例なので、
`.example` 側にその用途を書いてある。

### （済み）素材の追加と採点

追加5本は測り終えた。結果は[測定結果 #14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった)。
以下は**次に素材を足すときの手順**として残す。

#### 追加素材を測るときの手順（素材は配置済み）

**MKV と VTT は stem（拡張子を除くファイル名）が一致していれば置き場所は自由。**
`prep.py` が入力ツリーを再帰的に探して突き合わせる。既存の `sample/mkv/` と
`sample/short vtt/` が別フォルダなのもそのため。

```powershell
# WAV抽出。既存のWAVはスキップされるので何度でも回せる
python eval/prep.py "sample/complete-test" --out _eval\wav

# 記録値と揃える条件（beam 5・言語自動・整形なし・ループ抑制あり）
python eval/run.py --config ext-clip -m large-v3 --tag <tag> --only <識別子>

# 24分ものと一挙は分けて採点する（帯状DPのコストが桁違い）
python eval/score.py --hyp _eval\hyp\<tag> --exclude <一挙の識別子> --report _eval\score-<tag>.txt
python eval/score.py --hyp _eval\hyp\<tag> --only <一挙の識別子> --report _eval\score-<tag>-long.txt
```

**`_eval/wav` にファイルが増えると `--exclude` だけでは絞れなくなる。**
一度これを踏んで14本を回し始めた。**`--only` で明示するほうが安全。**

**字幕整形（`standard_asia`）を条件に入れる必要はない。** `format_segments()` は
`write_outputs()` の前に `segments` を書き換えるので JSON には整形後のキューが
入るが、`eval/normalize.py` の `collapse_space()` が全レベルで `\s+` を
完全除去するため、**全文CERへの影響はゼロ**（整形が足すのは改行とキュー分割だけで
テキストは保持される）。per-block だけは中点の割り当てが変わるので動く。

**本家XXLと比べるとき**は `-ct float32` が必須（RTX 5090 で float16 は落ちる）。

```powershell
$exe = "C:\Users\Owner1\Downloads\Faster-Whisper-XXL_r245.4_windows\Faster-Whisper-XXL\faster-whisper-xxl.exe"
& $exe <wav> -m large-v3 -ct float32 -f json -o _eval\hyp-xxl-new --language ja --beep_off
python eval/xxl_convert.py --src _eval\hyp-xxl-new --dst _eval\hyp\xxl-new --duration-from _eval\hyp\<tag>
```

**素材が揃った時点で詰まる箇所が1つある。`score.py` は
`_eval/hyp/<tag>/*.json` を読む前提で、SRT/VTT パーサを持っていない。**
つまり、

- **参照側（新規ARIBVTT）** は `arib_vtt.py` が読むので問題ない
- **仮説側は MKV から `eval/run.py` で作り直す必要がある。** Amatsukaze や
  whisp-carrier が出した SRT/VTT をそのまま採点する経路は存在しない
- VTT を直接採点したいなら **VTT→JSON コンバータを書く**
  （`eval/xxl_convert.py` が本家XXLのJSONに対してやっているのと同じ役割）。
  これが現状いちばんはっきり足りていない部品

Amatsukaze 経由の出力は連携テストの目視確認に使い、精度の数字は
`eval/run.py` で改めて回すのが既存のやり方。

既存3本の現行版出力は**回帰確認**になる。`_eval/hyp/clip-fixed` は
ループ抑制導入前の記録なので、**健全な素材なら1文字も変わらないはず**で、
そこが確認ポイント（[測定結果 #12](#12-実装した検出器は上限にそのまま到達した誤爆ゼロで)で誤爆ゼロを確認済み）。

新素材を入れるときに **MKVのメタデータで局を確認する**価値がある。
「問題の3本に局や番組属性の共通項があるか」が未確認のまま残っている
（[該当節](HANDOVER.md#解決済み問題の3本は局ではなく内容だった)）。`prep.py` は局を記録していない。

### B. 配布アーカイブ（サイズ上限に引っかかる）

**通常ビルドは 4.78GB あり、GitHub Releases の1ファイル上限 2GB を超える。**
分割アーカイブ（7-Zip の `-v1900m` など）か別のホスティングが必要。

同梱物の準備は済んでいる。exe の隣に `LICENSE` / `LICENSE.ffmpeg.txt` /
`THIRD-PARTY-NOTICES.md` / `whisp-carrier.yaml.example` が並ぶので、
LGPL の要求（ffmpeg のライセンス文がバイナリと一緒に配布される）は満たす。

**未決定が1つ。`whisp-carrier.yaml`（ユーザー設定）を配布物に入れるか。**
いまは開発機の設定が `dist/` に置かれている（[運用上の注意](HANDOVER.md#運用上の注意)）。
配るなら `.example` だけにして、利用者にリネームさせるのが筋。

`dist-full/`（5.06GB）と `build-full/` は `--realign` を配らないなら不要。
配布は通常ビルドで決着しているので削除できるが、**`--realign` が動くことを
確認した唯一の成果物**でもある（再ビルドは約2分40秒）。

### D. フィルター失敗の残り（優先度低）

「例外が上がったときに終了コードへ反映する」までは入っている。
**ffmpeg が returncode 0 を返しつつ無音や途中切れの出力を書くケースは、
段ごとの計測で検出できるようになったが、「`--ff_lowhighpass` で後半が消える」
の再現自体が取れていない。** 7秒のテスト音声では起きないので実素材が必要。
再発したらどの段で peak か duration が壊れるかログに残る。

**優先度は低い。** 実運用は `--ff_*` を使わない方針で決着したので、
この経路は誰も通らない。詳細は[既知の問題 2](HANDOVER.md#2-音声フィルターが実用レベルに達していない)。

### 保留：機能面の穴は要望駆動で判断する

話者分離（XXLにはある）、音声フィルターの実用化、`--realign` の安定化は、
**要望が来てから着手する**方針。XXLにあるからという理由で pytorch-lightning や
speechbrain を引き込むと、すでに4.78GBあるexeがさらに膨らんで、いま優位に
立っている主経路のビルドが重くなるだけ。**未着手のタスクではなく、意図的な保留。**

---

## 測定結果

素材15本を測り終えた。生データは `_eval/` に残してある。

> **#1〜#18 は参考情報。** [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) で採点指標そのものの欠陥が見つかったので、
> **これらの数値は現在の指標では再現しない。** なぜそう作ったかという経緯としては
> 有効なので残してあるが、**数値を引用しないこと。**
> 現行の基準は[修正後の9本](#修正後の9本2026-08-22)。
>
> 参照を使わない指標（ループ字数・30秒超・thin）は影響を受けないので、
> #1・#11・#12 のその部分は今も有効。
>
> さらに #1〜#14 は silero + 歌唱を含めて採点した世代、#15〜#18 は `--exclude-songs` 世代
> という違いもある。**要するに #1〜#18 の間でも横に並べられない。**

### 索引（#1〜#29）

**「それは測ってあるか」を引くための表。** この文書は 5,700行あって
線形に読む前提ではないので、番号から本文へ飛べる入口を1つ置いてある。
**題に結論の向きを入れてあるので、開く前に当たりかどうかが分かる。**
上の世代の注意は **#1〜#18 に効く**（数値を引用しないこと）。

| | 題 |
|---|---|
| [#1](#1-既定経路に30秒超セグメントの欠陥があった既存バグ修正済み) | 既定経路に30秒超セグメントの欠陥があった（既存バグ・修正済み） |
| [#2](#2-24分ものでは-collect-と-clip-に差が無かった一挙で決着) | 24分ものでは collect と clip に差が無かった（一挙で決着） |
| [#3](#3-外部vadは内蔵vadよりcerで3pt良い想定外) | 外部VADは内蔵VADよりCERで3pt良い（想定外） |
| [#4](#4-anime-whisper-は-large-v3-に負けたモデルカードと逆) | anime-whisper は large-v3 に負けた（モデルカードと逆） |
| [#5](#5-cer-24-の読み方合計値に意味は薄い) | CER 24% の読み方（合計値に意味は薄い） |
| [#6](#6-一挙放送で-clip-が勝ち既定を-clip-に戻した) | 一挙放送で clip が勝ち、既定を clip に戻した |
| [#7](#7-vadはカバレッジを売って精度を買う交換だったnovad対照) | VADは「カバレッジを売って精度を買う」交換だった（novad対照） |
| [#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果) | `--vad_threshold` は取りこぼしの knob ではなかった（否定的結果） |
| [#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果) | VADパラメータ空間は打ち止め。既定が最良だった（否定的結果） |
| [#10](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施) | Faster-Whisper-XXL との比較（既定同士。推奨設定では未実施） |
| [#11](#11-ループを潰した場合の上限既定に載せるだけで本家を抜く) | ループを潰した場合の上限。既定に載せるだけで本家を抜く |
| [#12](#12-実装した検出器は上限にそのまま到達した誤爆ゼロで) | 実装した検出器は上限にそのまま到達した（誤爆ゼロで） |
| [#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い) | 実運用設定（beam 10）は測定設定（beam 5）よりわずかに悪い |
| [#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) | 追加素材5本。**取りこぼしは「問題の3本」ではなく子供向け番組の性質だった** |
| [#15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定) | `condition_on_previous_text=true` は死亡遊戯を破壊した（仮説の否定） |
| [#16](#16-歌唱区間を除外したら構図が変わったexclude-songs) | 歌唱区間を除外したら構図が変わった（exclude-songs） |
| [#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) | `--vad_neg_threshold` も knob ではなかった。VAD空間は本当に打ち止め（否定的結果） |
| [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) | TEN VAD が silero を置き換えた。**取りこぼしは実装で直せる** |
| [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) | 採点指標に欠陥があった。参照キューの表示時間を発話時間として使っていた |
| [#20](#20-定型句フィルタは-06pt-効くが見送った) | 定型句フィルタは 0.6pt 効くが、見送った |
| [#21](#21-inass-を歌検出に転用するのは危険否定的結果) | INASS を歌検出に転用するのは危険（否定的結果） |
| [#22](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持) | anime-whisper を現行パイプラインで再検証した（#4 の結論は維持） |
| [#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) | 音声フィルターを全部測った（`--ff_lowhighpass` だけが効く・2026-08-25） |
| [#24](#24-ボーカル抽出は3つの不具合で動いていなかった順序を直して測った2026-08-29) | ボーカル抽出は3つの不具合で「動いていなかった」（順序を直して測った・2026-08-29） |
| [#25](#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02) | ニワトリ・ファイターは言語自動判定の失敗だった（`-l ja` で解決・2026-09-02） |
| [#26](#26-定型句フィルタを実装した20-の見送りを撤回2026-09-02) | 定型句フィルタを実装した（#20 の見送りを撤回・2026-09-02） |
| [#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) | モデル軸を4候補で閉じた（large-v3 が全勝・2026-09-03） |
| [#28](#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03) | 定型句フィルタを残り6本にも当てて、15本を現行既定で採り直した（2026-09-03） |
| [#29](#29-取りこぼしを分解した現行パイプラインの伸びしろは2pt未満否定的結果4件2026-09-05) | 取りこぼしを分解した。現行パイプラインの伸びしろは2pt未満（否定的結果4件・2026-09-05） |

**行を手で書かないこと。** `_eval/_make_index.py` が見出しからアンカーを生成する。
**日本語のアンカーは目視で確認できない**（`。、（）・！？「」` が落ちて空白がハイフンになる）ので、
**エントリを足したら再生成して貼り直し、`_eval/_link_check.py` で検査する。**
**題を書き換えたときも同じ** — アンカーが変わるので、この表と本文リンクの両方が切れる。

### 修正後の9本（2026-08-22）

**現行の基準値。** 24分もの9本、歌唱除外、参照 35,036字。
指標は [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) 修正後。レポートは `_eval/score-fixed9.txt`。

`ten-f1` / `ten-f2` は推論なしで作った検証用のコピーで、`_eval/_strip_filler.py` が
`ten-t075` から定型句（f1）と歌唱窓（f2、手作業のオラクル）を落としたもの。

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | 無字幕域 | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|---------|--------|------|
| clip（silero、現在の既定） | 19.3% | 82.6% | 91.1% | 20.4% | 0.91 | 1775字 | **0** | **0** |
| clip + 定型句 | 19.0% | 82.6% | **91.6%** | 20.0% | 0.90 | 1761字 | **0** | **0** |
| ten 0.75 | 16.1% | **86.6%** | 90.0% | 17.2% | 0.96 | 3816字 | **0** | 2 |
| **ten 0.75 + 定型句** | **15.5%** | **86.6%** | 90.8% | **16.6%** | 0.95 | 3686字 | **0** | 1 |
| ten + 定型句 + 歌唱（オラクル） | 16.1% | 85.8% | 91.0% | 17.3% | 0.94 | 2021字 | **0** | 1 |
| xxl（本家既定） | 20.5% | 82.3% | 89.3% | 24.3% | 0.91 | 2495字 | 9件 / 602s | 15 |

**結論が4つ。**

**(1) TEN を既定にすべき。** 既定に対し **−3.2pt**（定型句込みで −3.5pt）、XXL に対し −4.4pt。
**修正前の指標では −0.8pt しかなく3本で退行していた**ので見送る判断だったが、
その退行はすべて採点バグ由来だった。ファイル別は 8勝0敗1分（[現状](STATUS.md#現状)の表）。

**(2) precision の懸念は消えた。** 修正前は TEN の precision が 87.4% で既定より
3.4pt 低く見えていた。修正後は 90.8% 対 91.6% で、**差は 0.8pt。**
つまり「TEN は拾いすぎて精度を落とす」という読みは採点バグの像だった。

**(3) 歌唱フィルタは不要。** オラクルで歌唱窓を落とすと 15.5% → **16.1% と悪化する。**
修正前は 18.2% → 16.5% と改善していたので、**歌唱窓が効いていたのは
「参照が何も言っていない時間の課金」を消していたからで、修正で不要になった。**
歌の書き起こし自体は正味プラス（落とすと coverage が 86.6% → 85.8% 下がる）。

**(4) 払っているのは無字幕域の出力量。** 1775字 → 3686字（2.1倍）。
CERには入らないが字幕としては見える差なので、
[対象外の位置づけ](#歌唱oped-の扱い対象外だが拾う)で扱う。

### 一挙と追加素材の再採点（2026-08-22）

**いずれも silero（旧既定）での再採点。TEN では未測定。**
レポートは `_eval/score-long-fixed.txt` / `score-majilumiere-fixed.txt` /
`score-new4-fixed.txt`。

**保持キューの影響がここで最大になる。** 一挙は参照の画面時間と発話時間が
30〜40分ずれていた。

| 素材 | 発話時間 / 画面時間 | 保持キュー | 全文CER（旧 → 修正後） |
|------|-------------------|-----------|---------------------|
| ハクメイとミコチ 一挙（5h22m） | 3:59:54 / 4:34:11 | 185件・**34分14秒** | 20.5% → **19.2%** |
| 株式会社マジルミエ 一挙（4h52m） | 4:01:12 / 4:44:02 | 141件・**42分45秒** | 17.3% → **16.0%** |

**長尺劣化が無いという結論は維持された。** マジルミエ 16.0% は
24分もの9本の既定合計（19.3%）より良く、30秒超も0件。
ハクメイの collect 対照も 21.5% で clip の勝ちが変わらない
（ループ文字は clip 491字 対 collect 1628字で、collect が3.3倍という記録も維持）。

**追加4本（子供向け、silero）。合計 31.1% 対 XXL 33.5%。**

| 素材 | new-clip（silero） | xxl | coverage（こちら / XXL） |
|------|------------------|-----|----------------------|
| ニンジャラ（花火） | **21.1%** | 29.0% | 79.9% / 82.1% |
| ぷにる #20 | **29.3%** | 34.4% | 72.0% / 68.0% |
| 名探偵プリキュア #21 | 35.2% | **33.6%** | 67.4% / 70.9% |
| おねがいアイプリ（情熱） | 44.6% | **38.8%** | 56.4% / 61.9% |

**2勝2敗で、負ける2本はどちらもカバレッジで負けている**（−3.5pt / −5.5pt）。
9本側で TEN がカバレッジを +4.0pt 稼いだのと同じ穴なので、
**この4本に TEN を当てれば勝敗が変わる可能性が高い。未測定。**

構造面はこちらが一貫して良い。XXL は4本で 30秒超4件185秒・thin 10件・
ループ482字、こちらは 30秒超0件・thin 0件・ループ6字。

| ファイル | 内容 |
|---------|------|
| `_eval/prep-report.txt` | 素材10本の調査（音声トラック、字幕の中身、ギャップ） |
| `_eval/score3.txt` | サニタイズ前の3経路（**旧・全文CER欠陥あり**） |
| `_eval/score6.txt` | サニタイズ前後6設定の対照（**旧・同上**） |
| `_eval/score-final.txt` | **24分もの9本の最終版。** 3経路 + novad + anime-whisper |
| `_eval/score-nosong.txt` | **実運用条件。** アイプリ・死亡遊戯を除く7本、clip / novad / builtin |
| `_eval/score-sweep.txt` | **閾値スイープ。** clip 0.45 / 0.30 / 0.15 / novad |
| `_eval/score-vadparams.txt` | **VADパラメータ。** min_silence 3000/500/100、speech_pad 900/200、novad |
| `_eval/score-xxl.txt` | **本家との比較（9本）。** clip-fixed / builtin-fixed / XXL r245.4 |
| `_eval/score-xxl-clean6.txt` | **同・病的3本を除く6本。** ここで全指標こちらが勝つ |
| `_eval/hyp-xxl-raw/` | XXL の生JSON（変換前） |
| `_eval/score-noloop.txt` | **ループ抑制の上限（#11）。** clip / novad / xxl の各 noloop 対照 |
| `_eval/score-prodloop.txt` | **実装した検出器（#12）。** `--detector production` |
| `_eval/hyp/*-noloop/`, `*-prodloop/` | `strip_loops.py` の出力。推論はしていない |
| `_eval/score-long.txt` | **一挙1本目（ハクメイ）。** clip / collect |
| `_eval/score-prod.txt` | **実運用設定の検証（#13）。** clip-fixed / clip-prodloop / prod-beam10 |
| `_eval/hyp/prod-beam10/` | beam 10 + best_of 10 + language ja の出力 |
| `_eval/hyp/prod-lang/` | beam 5 + language ja の出力（`clip-prodloop` と内容一致） |
| `_eval/score-new.txt` | **追加素材の24分もの4本（#14）** |
| `_eval/score-new-long.txt` | **一挙2本目（マジルミエ 4h52m）。全測定中最良の17.3%** |
| `_eval/score-new-xxl.txt` | **子供向け4本での XXL 直接比較（#14）。取りこぼしが実装非依存だと確定した根拠** |
| `_eval/hyp/new-clip/` | 追加素材5本の出力（記録値と同条件） |
| `_eval/hyp-xxl-new/`, `_eval/hyp/xxl-new/` | XXL の追加素材出力（生 / 変換後） |
| `_eval/hyp/container-mkv-direct/` | MKV直渡しの出力（コンテナ等価性の確認） |
| `_eval/score-nosong-prodloop.txt` | **実運用条件7本の再採点。** 現行既定で 15.6%（旧記録は 18.4%） |
| `_eval/score-cond-prev.txt` | **`condition_on_previous_text=true` の検証（#15）。** cond-prev / clip-prodloop |
| `_eval/score-novad-clean6.txt` | **健全6本に novad を当てたコスト。** clip-prodloop / novad-prodloop |
| `_eval/hyp/novad-prodloop/` | novad に production 検出器を当てた出力（12本3355字を落とした） |
| `_eval/_novad_where.py` / `.txt` | novad の増分を歌唱帯とそれ以外に分けた集計 |
| `_eval/score-nosing.txt` | **歌唱除外での3者比較（#16）。** clip-prodloop / novad-prodloop / xxl |
| `_eval/score-negthr.txt` | **`--vad_neg_threshold` スイープ（#17）。** 既定 / 0.20 / 0.05 / 0.01 |
| `_eval/hyp/clip-neg020/`, `-neg005/`, `-neg001/` | 同・4本ぶんの出力 |
| `_eval/_neg_probe.py` / `.txt` | `neg_threshold` がどちらの経路で受け付けられるかの確認 |
| `_eval/score-ten1.txt` | **TEN VAD 初回（#18）。** 死亡遊戯1本、silero 閾値のまま |
| `_eval/score-ten-sweep.txt` | **同・閾値スイープ。** ten 0.45 / 0.60 / 0.75 / 0.90 |
| `_eval/score-ten-healthy.txt` | **同・健全2本での退行確認** |
| `_eval/score-ten4.txt` | **同・4本合計。** clip / ten 0.75 / novad / xxl |
| `_eval/hyp/ten-clip/`, `ten-t060/`, `ten-t075/`, `ten-t090/` | TEN VAD の出力 |
| `_eval/score-vadmodels.txt` | **VADモデル5者比較（#18）。** silero / ten / fsmn / inass / xxl |
| `_eval/vad-fsmn.json` / `.log` | FSMN-VAD の区間（`ext_vad_dump.py` 出力。log にラベル集計） |
| `_eval/vad-inass.json` / `.log` | inaSpeechSegmenter の区間（同。log に speech/music/noise の内訳） |
| `_eval/hyp/fsmn-clip/`, `inass-clip/` | 上の区間を `precomputed` で通した出力 |
| `_eval/_song_probe.py` / `.txt` | 参照の歌唱区間が何を持っているかの調査（マーカー発見の経緯） |
| `_eval/_drcs_survey.py` / `.txt` | 私用領域の codepoint とマーカー候補の全参照サーベイ |
| `_eval/_is_sung_check.py` / `.txt` | `normalize.is_sung()` の誤爆確認（15本、参照の1.6%） |
| `_eval/hyp/cond-prev/` | 同・死亡遊戯1本の出力とログ |
| `_eval/_cond_probe.py` / `.txt` | 同・セグメント数と末尾の内訳（縮退の中身を見た記録） |
| `_eval/_detector_diff.py` / `.txt` | 採点用検出器と実装の差分4件（全件目視した記録） |
| `_eval/score-aw-recheck.txt` | **anime-whisper の現行パイプラインでの再検証（#22）。** ten-t075 / aw-ten / anime-whisper をニンジャラ1本で比較 |
| `_eval/hyp/aw-ten/` | 同・anime-whisper を `ten-clip` で回した出力（1本） |
| `_eval/hyp/aw-old-prodloop/` | 同・旧 anime-whisper 出力に製品版ループ検出器を当てたもの（9本で2セグ427字しか落ちない＝ループが敗因ではない証拠） |
| `_eval/_ten_miss_where.py` / `.txt` | **取りこぼしの分解（#29）。** TEN 区間外の参照キューと、その位置の確率分布（A=`min_speech` / B=閾値 / C=床）。**窓の被覆率で判定すると `speech_budget` の緩さを拾うので、ゼロ被覆で判定する** |
| `_eval/_inside_miss_where.py` / `.txt` | 同（#29）・区間内の取りこぼしを5分類（VAD外 / 出力ゼロ / 重なり / 認識違い / 拾えた）。**重なりは9本で0件** |
| `_eval/_miss_kind.py` / `.txt` | 同（#29）・未産出（窓の hyp が参照で説明できるのにこのキューだけ出ない）と誤認識の分離。**判定は窓単位なのでキュー単位に使わないこと** |
| `_eval/_arib_floor.py` | 同（#29）・多系統で届かない床。**`--systems` / `--only` / `--out` で構成可変。**「どの系統も一致させられなかった文字」だけが CER と同じ数え方 |
| `_eval/_arib_floor.txt` | 同・5系統（TEN / silero / 内蔵 / novad / xxl）8本。床 4.66%、未一致文字 **9.15%** |
| `_eval/_arib_floor_xfamily.txt` | 同・**族外 qwen を含む7系統**3本。床 7.06%、未一致文字 8.64%。**anime-whisper が最下位（68.18%）** |
| `_eval/_floor_composition.py` / `.txt` | 同（#29）・床の記法別内訳と、緩和（`っ`削除 / 連続同字→1）の回収量。**両側に対称に当てて 0.28pt** |
| `_eval/_ten_segments_cache.json` | 上の4本が共有する TEN 区間のキャッシュ（キーに閾値と4パラメータを含む） |

`score3.txt` と `score6.txt` の全文CERは[指標の欠陥1・2](HANDOVER.md#全文cerの実装欠陥2つ修正済み性質を知らないと直した意味が分からない)を踏んだままの値。
per-block とループ・30秒超の数字は有効。`-final` と `-long` は修正後の再採点。

### 追加4本の TEN 測定（2026-08-22、決着）

**子供向け4本。TEN が4本すべてで silero に勝ち、XXL にも 4勝0敗。**
レポートは `_eval/score-new4-ten.txt`、推論は `_eval/hyp/ten-new/`。
参照 16,704字、歌唱除外、指標は [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) 修正後。

| 素材 | silero（`new-clip`） | **ten 0.75** | xxl | coverage（silero → ten / xxl） |
|------|--------------------|-------------|-----|------------------------------|
| ニンジャラ（花火） | 21.1% | **16.3%** | 29.0% | 79.9 → **87.1** / 82.1 |
| ぷにる #20 | 29.3% | **25.3%** | 34.4% | 72.0 → **76.4** / 68.0 |
| 名探偵プリキュア #21 | 35.2% | **23.0%** | 33.6% | 67.4 → **83.8** / 70.9 |
| おねがいアイプリ（情熱） | 44.6% | **23.6%** | 38.8% | 56.4 → **81.7** / 61.9 |
| **合計** | 31.1% | **21.9%** | 33.5% | 70.3 → **82.1** / 71.5 |

**silero では XXL に2勝2敗だったのが、TEN で 4勝0敗になった。** 負けていた2本
（プリキュア・アイプリ）はどちらもカバレッジ負けで、そこが TEN で +16.4pt / +25.3pt 動いた。
**予想どおりの向きで、幅は予想より大きい。**

- per-block plain 32.7% → **22.9%**、長さ比 0.79 → **0.95**
- precision は 89.6% → 86.6%（−3.0pt）。9本と同じ交換で、差引で大きく勝つ
- 構造面はこちらが一貫して良い。**30秒超 0件・thin 0件**（XXL は 4件185秒・thin 10件）。
  ループは 6件82字（XXL 4件482字）
- **払っているのは無字幕域の出力量。** 233字 → **1418字（6.1倍）**。9本での2.1倍より
  比が大きいのは、この4本が主題歌・番宣の比率が高いため。[対象外の位置づけ](#歌唱oped-の扱い対象外だが拾う)のまま
- TEN VAD 自体の所要は 24分ものあたり **8.0〜10.0秒**（音声長の 0.6%）

**これで [既知の問題 1-b](HANDOVER.md#1-b-子供向け番組の取りこぼし結論が覆ったvad起因ぶんは直せる) が閉じた。**
「子供向けの3割取りこぼしは Whisper 側の限界」という旧結論は、
**silero の限界を XXL 経由で2回測っていただけだった。**

### 一挙2本の TEN 測定（2026-08-22、決着）

**長尺でも TEN が勝ち、速度の退行も無かった。**
レポートは `_eval/score-long-ten.txt` / `score-majilumiere-ten.txt`、
推論は `_eval/hyp/ten-long/`。

**ハクメイとミコチ 一挙（5h22m、参照 37,987字）。**

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | 無字幕域 | ループ | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|---------|--------|--------|------|
| silero（`clip-long`、**ループ抑制前**） | 19.2% | 84.4% | 88.4% | 20.5% | 0.96 | 6105字 | 6件491字 | **0** | **0** |
| silero + ループ抑制（`clip-long-prodloop`） | 18.1% | 84.4% | **89.5%** | 19.5% | 0.94 | 6105字 | 2件10字 | **0** | **0** |
| **ten 0.75** | **16.1%** | **86.6%** | 89.3% | **17.1%** | 0.97 | 8919字 | 4件35字 | **0** | 1 |

**比較対象を作り直す必要があった。** `clip-long` は `loop_filter` 実装前の推論なので
ループが6件491字残っている（production 検出器を当てると4セグ481字落ちる）。
TEN 側は抑制済みなので、そのまま比べると TEN が 3.1pt 勝ちに見える。
**公平な比較は 18.1% 対 16.1% で 2.0pt 勝ち。**

**5h22m でも 16.1% は24分もの9本の合計と同値。** 長尺劣化が無いという結論が
TEN でも維持された。無字幕域は 6105字 → 8919字（1.46倍）で、
24分ものの2.1倍・子供向けの6.1倍より小さい。

**株式会社マジルミエ 一挙（4h52m、参照 50,255字）。全測定中の最良値。**

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | 無字幕域 | ループ | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|---------|--------|--------|------|
| silero（`new-clip`、抑制済み） | 16.0% | 85.7% | **92.0%** | 16.9% | 0.93 | 614字 | 4件29字 | **0** | **0** |
| **ten 0.75** | **14.1%** | **87.9%** | 91.4% | **15.0%** | 0.96 | 4485字 | 3件20字 | **0** | **0** |

**こちらは比較対象を作り直す必要がなかった**（`new-clip` は production 検出器を
当てても 0 drop で、既にループ抑制済み）。**−1.9pt で、15本の中で最も良い 14.1%。**

**無字幕域の増分がここで最大になる。614字 → 4485字（7.3倍）。**
参照の保持キューが 141件・42分45秒あり、無字幕域が36分24秒もある素材で、
そこに主題歌と提供クレジットが集中している。**CERには入らないが、
字幕としてはこの量のキューが付く。**[対象外の位置づけ](#歌唱oped-の扱い対象外だが拾う)のまま。

**両設定に共通する幻覚が1つある。「字幕作成者初音ミク」**が無字幕域に出ている
（silero 側にも 3件）。定型句フィルタの対象になりうる型だが、
[見送りを決めた](#g-定型句フィルタは見送り判断記録)ので残る。

### 15本の総括（2026-08-22）

**TEN は 15勝0敗0分。** 素材別の内訳は[現状](STATUS.md#現状)の層テーブル。

> **訂正の在り処（2026-09-03）。この「15勝0敗0分」は定型句フィルタなしの世代の話。**
> **両側にフィルタを当てて採り直すと 12勝1敗になる**
> （Summer Pockets #26 が 0.5pt 負ける）。
> **群の合計での勝敗と pt 差は変わらない。**
> [#28](#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)。
> **この節は当時の記録として残す。**

| 素材群 | 本数 | 参照 | silero | **ten 0.75** | 差 |
|--------|------|------|--------|-------------|-----|
| 24分もの | 9 | 35,036字 | 19.3% | **16.1%** | −3.2 |
| 子供向け | 4 | 16,704字 | 31.1% | **21.9%** | −9.2 |
| ハクメイ一挙 5h22m | 1 | 37,987字 | 18.1% | **16.1%** | −2.0 |
| マジルミエ一挙 4h52m | 1 | 50,255字 | 16.0% | **14.1%** | −1.9 |

**利得の幅は素材で桁違い**（子供向け −9.2pt 対 会話劇 −1.9pt）だが、
**退行した素材は1本も無い。** 24分もの9本では1本が同値（Summer Pockets 13.8%）で、
残り14本はすべて改善。

**時間内訳（[次の着手 I](#i-一挙の時間内訳を取る閾値を振る前に) の答え）。**

| 素材 | VAD | 全体 | VAD の割合 | silero の全体 | 区間数 silero → ten |
|------|-----|------|-----------|--------------|--------------------|
| ハクメイ（5h22m） | 103.9s | **1213.9s** | **8.6%** | 1195.7s（+1.5%） | 517 → 661 |
| マジルミエ（4h52m） | 95.8s | **1338.4s** | **7.2%** | 1413.2s（**−5.3%**） | 444 → 510 |

**VAD は支配的ではなかった。** 音声長の 0.54%（24分ものの 0.6% と同じ比）で
線形に伸びるだけで、**フレーム単位の Python ループが長尺で不利になるという懸念は
外れた。** 総時間も silero と ±5% の範囲で、マジルミエでは TEN のほうが速い。
区間数が増えているのに総時間が動かないのは
[I の観測](#i-一挙の時間内訳を取る閾値を振る前に)どおりで、
**コストはクリップ数ではなく出力トークン量で決まる**ため。

なお **TEN 側の2本はどちらも同じマシンで採点ジョブが走っている最中の値**なので、
単独実行ならもう少し速い。silero 側に有利な条件で測って負けていない。

**したがって「一挙を速くする」余地は VAD 側には無い。** 残るのは閾値と `collect` で、
どちらも精度を払う既に却下したトレード。**16倍速（実時間の6.3%）を受け入れるのが筋。**

### 24分もの9本（参照 36,053 字）

`-fixed` は `sanitize_segments()` 導入後。CER は2種類あり、意味が違うので必ず両方見る。

| 設定 | セグメント | ループ文字 | 30秒超 | 合計 | 最長 | CER全文 | CERブロック | 長さ比 |
|------|-----------|-----------|--------|------|------|--------|-----------|--------|
| builtin（修正前） | 2407 | 255 | **14** | **1581s** | **370s** | — | 29.2% | 0.81 |
| builtin-fixed | 2477 | 255 | 0 | 0s | 18s | **27.1%** | 28.6% | 0.81 |
| ext-collect（修正前） | 2764 | 1359 | **14** | **1208s** | 224s | — | 25.3% | 0.90 |
| collect-fixed | 2804 | 1353 | 0 | 0s | 29s | **23.3%** | 25.0% | 0.90 |
| ext-clip（修正前） | 2805 | 1363 | 0 | 0s | 24s | — | 26.0% | 0.92 |
| clip-fixed（ループ抑制前の既定） | 2805 | 1363 | 0 | 0s | 24s | **24.3%** | 26.0% | 0.92 |
| **clip-prodloop（現在の既定）** | 2798 | **32** | 0 | 0s | 24s | **22.0%** | **23.4%** | 0.89 |
| novad（対照） | 3284 | **3417** | 0 | 0s | 30s | 28.7% | 30.1% | **1.07** |
| anime-whisper | 651 | 1491 | 0 | 0s | 30s | **41.8%** | 62.6% | 0.80 |

CERの内訳（カバレッジ／精度、全文レベル）。**CERだけ見ると同点でも中身が違う。**

| 設定 | coverage | precision | 判定 |
|------|----------|-----------|------|
| **clip-prodloop（現在の既定）** | 80.3% | **90.7%** | 取りこぼし優勢（参照の20%を落とす） |
| clip-fixed | 80.4% | 87.7% | 取りこぼし優勢（20%） |
| collect-fixed | 80.2% | 88.8% | 取りこぼし優勢（20%） |
| builtin-fixed | 74.1% | **91.9%** | 取りこぼし優勢（26%） |
| novad（対照） | **85.0%** | 79.0% | 両方（15%取りこぼし / 21%が参照外） |
| anime-whisper | 64.2% | 80.6% | 取りこぼし優勢（36%） |

### 実運用条件（アイプリ・死亡遊戯を除く7本、参照 29,492 字）

歌詞が参照に丸ごと入っている作品と、（）に効果音注記が入る作品を外した数字。
到達不能な誤差を抜いた「期待できる値」として読める。

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | ループ文字 |
|------|--------|----------|-----------|-----------|--------|-----------|
| **clip-prodloop（現在の既定）** | **15.6%** | 86.8% | **91.2%** | **17.2%** | 0.95 | **32** |
| clip-fixed（ループ抑制前） | 18.4% | 86.8% | 87.8% | 20.4% | 0.99 | 1116 |
| novad（対照） | 24.9% | 87.6% | 81.3% | — | 1.08 | 2517 |
| builtin-fixed | 20.7% | 80.6% | **92.1%** | — | 0.88 | 255 |

**現在の既定で再採点済み**（`_eval/score-nosong-prodloop.txt`、推論なし）。
除外した2本はアイプリと死亡遊戯で、**ニンジャラは7本の中に残っている**
（ループ最悪の1本）。ループ文字1116字の大半がそれなので、抑制で
**18.4% → 15.6%（−2.8pt）** 改善した。per-block も 20.4% → 17.2%。

**内訳の動き方が「ループを潰すのは無条件に得」の再確認になっている。**
カバレッジは 86.8% で完全に不変（ループセグメントは参照に一致する文字を
持っていない）、精度だけが 87.8% → 91.2% に上がる。判定も
`both`（13%取りこぼし / 12%が参照外）から
`MISSED SPEECH dominates`（13%取りこぼし）に変わり、
**この条件では余剰出力側の問題が消えて取りこぼしだけが残る。**

**配布時に「期待できる値」として出すならこの 15.6% が現行の数字。**
`novad` と `builtin-fixed` の行はループ抑制前の記録のまま（再採点していない）。

**この7本にニンジャラが残っている点は忘れないこと。** 子供向け1本を含んだ
数字なので、会話劇だけに絞ればさらに良い（[#5](#5-cer-24-の読み方合計値に意味は薄い) の上位5本は 8.8〜16.7%）。

### 一挙放送1本（5時間22分、参照 38,225 字）

clip / collect の2設定のみ。両方とも全文CERは厳密解。

| 設定 | セグメント | 検出発話 | ループ文字 | 30秒超 | 最長 | CER全文 | CERブロック | 長さ比 | 推論 |
|------|-----------|---------|-----------|--------|------|--------|-----------|--------|------|
| clip-long（既定） | 4139 | 2:23:14 | **491** | 0 | 20s | **20.5%** | 21.3% | 0.96 | 1196s |
| collect-long | 4098 | 2:25:20 | **1628** | 0 | 28s | 22.5% | 23.5% | 0.97 | 1036s |

内訳は clip がカバレッジ83.9%／精度87.5%、collect が83.2%／85.9%。
**clip は両方で上位**なので、ここはトレードオフではなく単純に優れている。
判定はどちらも「両方」（clip は16%取りこぼし／13%が参照外）で、
24分もの（20%／12%で取りこぼし優勢）より幻覚寄りに傾く。
5時間走るぶんループが入り込む余地が増えるという読みと整合する。

collect 側はサニタイズ前に190秒のセグメントを含み、54箇所が分割された。
clip 側の分割は1箇所で、これは30秒超ではなく `--max_gap 3.0` による
単語間ポーズ分割。

---

### #1. 既定経路に30秒超セグメントの欠陥があった（既存バグ・修正済み）

> **採点条件** 出典 `_eval/score3.txt` `_eval/score6.txt` ／ 24分もの9本・歌唱含む ／
> 世代 **pre-#19（参照ありの数値は再現しない）** ／
> ただしこの節の主張（30秒超・thin・最長）は**参照を使わないので今も有効**

Whisper のデコード窓は30秒なので、30秒を超えるセグメントはデコーダからは出ない。
すべて `restore_speech_timestamps` の産物である。無音を波形から削除して推論し
あとで時刻を復元するため、**削除された無音をまたぐセグメントは開始と終了が
別々に引き伸ばされ、無音ぶんだけ長さが膨らむ**。テキストは短いまま時間だけ伸びる。

```
builtin  終末ツーリング  0:15:56.6-0:17:57.6  「あの時のこと覚えてる」 = 121秒
builtin  アイプリ        最長 352秒
builtin  クレバテス      0:22:06.6-0:24:06.8  「テレビアニメ追放者食堂へようこそ」 = 120秒
```

**これは `collect` を入れたから起きたのではなく、内蔵VAD経路に元からある。**
むしろ内蔵のほうが酷い（1581s / 最長370s）。`clip` だけが影響を受けない
（波形を加工せず `seek_clips` で読むので復元処理を通らない）。
一挙でも同じで、collect 側に最長190秒が出て54箇所分割された一方、
clip 側に30秒超は1本も無い。

字幕として370秒表示されるキューは壊れている。**既定設定の全ユーザーに影響していた。**
`--standard` 等で字幕整形を有効にすると `--max_gap` で分割されるが、
整形オプションを使わない既定では何も分割していなかった。

**修正済み。** `subtitle_format.sanitize_segments()` を追加し、
推論直後・字幕整形の前に常時通すようにした。実測で 30秒超が両経路とも 0 になり、
最長は builtin 370s→18s、collect 224s→29s、thin は 20/17→0。
設計上のポイントは[設計判断](HANDOVER.md#30秒超セグメントのサニタイズは常時通す)にある。

副作用が小さいことも実測で確認した。セグメント増加は builtin +70本(2.9%)、
collect +40本(1.4%)。**24分ものの `clip` は 2805セグメント・ループ11件1363字・
CER 26.0% が完全に無変化**で、これが「通常出力を壊していない」いちばん強い
証拠になる（clip 経路には30秒超の修復対象が存在しない）。
CER はむしろ builtin 29.2%→28.6%、collect 25.3%→25.0% と改善した。
長すぎるセグメントが分割されて時間ブロックへの割り当てが正しくなったぶん。

なお「clip は完全に素通りする」は5時間素材では正確ではない。一挙の clip でも
1箇所分割されている。ただしこれは30秒超の修復ではなく、`--max_gap 3.0` を
超える単語間ポーズによる分割。

ループ件数が builtin 4→6、collect 6→8 に増えるが、ループ**文字数**は
255→255、1359→1353 でほぼ不変。長いループセグメントが分割されて
件数が増えただけで、幻覚の総量は変わっていない。検出器の粒度の話であって
実害ではない。

### #2. 24分ものでは collect と clip に差が無かった（一挙で決着）

> **採点条件** 出典 `_eval/score6.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 結論は #6 で上書き済み

**この節は24分もの9本だけの話で、結論は #6 で上書きされている。**
差が出なかった記録として残す。

ループ文字数 1359 vs 1363 でほぼ同一。**`collect` はループを少ない本数に
まとめているだけで、出力される幻覚の総量は変わらない。**

CER も 23.3% vs 24.3% で、ファイル別の方向が一貫しない。合計差は
ニンジャラ1本（collect 43.4% / clip 53.7%）が作っていて、逆向きに
桃源暗鬼1本（collect 17.0% / clip 12.6%）が効いている。
ファイル間分散が9%〜54%あるので1ptは誤差。

つまり24分ものの範囲では **`--vad_segment_mode collect` を既定にした判断に
根拠が無い**というところまでしか言えなかった。決めるだけの材料が無い状態で、
これが一挙を測る動機になった。

### #3. 外部VADは内蔵VADよりCERで3pt良い（想定外）

> **採点条件** 出典 `_eval/score-final.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19**

全文CERで builtin 27.1% に対し clip 24.3% / collect 23.3%。長さ比が
builtin 0.81 vs 0.90〜0.92 なので、**内蔵VADは発話を2割落としている。**
外部VADはハルシネーションを起こしやすいという通説と逆向きの結果。
pyannote 等は未測定（silero 系のみ）。

### #4. anime-whisper は large-v3 に負けた（モデルカードと逆）

> **採点条件** 出典 `_eval/score-final.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 現行パイプライン・修正後指標での再検証は [#22](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持)（結論は維持）
>
> **原因が切り分けられた（[#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03)）。**
> **ベースモデルの kotoba-v2 が同じ壊れ方をする**（4本で 37.9%・長さ比 0.84）ので、
> **負けているのはファインチューンではなくベースの蒸留構造**（デコーダ2層）。
> 下の「要因3つ」はどれも症状として正しいが、根はここ1つ。

全文CERで **anime-whisper 41.8% vs large-v3(clip) 24.3%**。
17pt の差で負けている。**モデルカードの CER 13.0 vs 16.5（anime-whisper 有利）は
手元の録画TVアニメでは再現しない。** 全9本すべてで large-v3 が上。

負けている要因は3つ見える。

**発話の取りこぼし。** 長さ比 0.80。参照の8割しか出していない。

**ループが最多。** ループ文字1491字で4設定中最悪。しかもこれは
`no_repeat_ngram_size=5`（モデルカードのCER測定条件、`whisp_models.py` が
自動で入れる）を効かせた状態の数字。

**セグメントが粗すぎる。** 651セグメント、large-v3 の 2804 に対して4分の1。
1キューに30秒ぶん・複数話者の台詞が入るので、字幕用途としては
そのままでは使えない。`--standard_asia` 等の整形が事実上必須になる。

期待どおりだったのは非言語音で、`ぷちゅぷっ、ぷっ、ちゅぅぅ`、`ぐすっ`、
`えへへ` などを拾っている。字幕は載せないのでCERでは減点されるが、
モデルの売りは機能している。出力そのものは日本語として妥当で、
壊れているわけではない。

```
噂は聞いてるが、こうやって見るのは初めてだな。一ノ瀬四季、写真で見るより全然ガキだな。
```

**注意：この結論は全文CERで見て初めて出た。** ブロックCERだと 62.6% で、
20pt ぶん不当に悪化する（セグメントが粗いので中点割り当てが破綻する）。
最初はこの62.6%を見て「壊滅的」と誤読した。

### #5. CER 24% の読み方（合計値に意味は薄い）

> **採点条件** 出典 `_eval/score-final.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 現行既定での層分けは[現状](STATUS.md#現状)の表

合計 24.3% は文字数加重なので、悪い3本に引きずられている。
ファイル別（clip-fixed・全文CER）を見ると二極化している。
括弧内は collect-fixed の値で、方向が一貫していないことが分かる。

| 作品 | CER | coverage | precision | 内訳の読み |
|------|-----|----------|-----------|-----------|
| 公女殿下の家庭教師 #10 | **8.8%**（8.4%） | 92.5% | 95.2% | 取りこぼし8%。会話劇 |
| LIAR GAME #10 | **11.1%**（10.8%） | 89.5% | 94.2% | 取りこぼし11%。会話劇 |
| 桃源暗鬼 | **12.6%**（17.0%） | 89.9% | 90.6% | 両方（10%／9%）。アクション |
| Summer Pockets #26 | **14.0%**（13.0%） | 87.4% | 92.3% | 取りこぼし13%。会話劇 |
| クレバテス #06 | **16.7%**（15.8%） | 84.8% | 90.4% | 取りこぼし15%。ファンタジー |
| 終末ツーリング #12 | 18.3%（18.0%） | 85.8% | 90.0% | 両方（14%／10%）。日常＋エンジン音 |
| 死亡遊戯で飯を食う #09 | 42.4%（41.1%） | **59.7%** | 87.3% | **取りこぼし40%。出した文字は87%正しい** |
| ニンジャラ | 53.7%（43.4%） | 75.4% | **63.4%** | **出力の37%が参照外。唯一の幻覚ケース** |
| おねがいアイプリ | 54.4%（54.9%） | **47.3%** | 85.6% | **取りこぼし53%。出した文字は86%正しい** |

**中央値は16.7%、上位5本は8.8〜16.7%。** 深夜の会話劇なら10%前後まで出ている。
一挙も20.5%なので、**長尺で悪化していない**（#6）。

**悪い3本は「間違えている」のではなく「出していない」（ニンジャラは逆に出しすぎ）。**
これは長さ比からの推測ではなく、カバレッジ／精度で確定した。
**出した文字の86〜87%は参照に一致している**ので、認識誤りの指標として
読んではいけない。ニンジャラだけが精度63.4%で、出力の37%が参照外＝幻覚。

**CERが近いのに原因が真逆**という点が重要で、死亡遊戯 42.4% と
ニンジャラ 53.7% を「どちらも精度が悪い」とまとめると対処法を間違える。
前者はVADと認識の recall、後者はループ抑制の話。

アイプリは字幕被覆97.6%で **OP・ED・挿入歌まで字幕が付いている**。
しかも音楽記号（U+266A / U+266C）が0個なので `build_blocks()` の
歌唱区間除外が発動しない。歌詞が丸ごと参照に入っていて Whisper は歌を
書き起こせないので、**構造的に到達不能な誤差**がかなり混ざっている。
歌の多い作品を測るなら、音楽記号以外の手段で歌唱区間を除く必要がある。

**その手段を実装して量が確定した。到達不能ぶんは13.5pt。**
`--exclude-songs` で歌唱区間を両側から落とすと **54.4% → 40.9%**
（参照の22.9%が歌詞だった）。[測定結果 #16](#16-歌唱区間を除外したら構図が変わったexclude-songs)。

死亡遊戯は30分の尺で検出発話が6分56秒しかない。VADの取りこぼしが疑わしい。
参照の（）に「スマホゲームの音」「すすり泣き」「食べる音」が入る作品でもあり、
参照側も純粋な発話書き起こしではない。

ニンジャラは逆で長さ比1.19。出しすぎ（幻覚）の側で、**この1本だけは
collect のほうが10pt良い**（43.4%、長さ比1.06）。24分ものの合計で
collect が勝っていた差はほぼこれ。**ループ抑制で 31.8% まで下がった**（#12）。

**参照そのものが床を作っている点も忘れないこと。** ARIB字幕は読みやすさのために
編集されていて、フィラーや言い直しが落ち、話者名が入り、歌は代表記号だけ。
**CER 0% は原理的に出ない。** 到達可能な下限は測っていないが、上位5本が
8.8〜16.7%に固まっているのは、そのあたりが実質的な床に近いという傍証と読める。

参考として、anime-whisper のモデルカードが主張する large-v3 の CER は 16.5。
手元の上位6本は 8.8〜18.3% でこの値を挟んでいる。ベンチマークも正規化条件も
違うので厳密な比較ではないが、測定が大きく外れてはいないことの傍証にはなる。

### #6. 一挙放送で clip が勝ち、既定を clip に戻した

> **採点条件** 出典 `_eval/score-long.txt` ／ 一挙1本 5h22m・歌唱含む ／ 世代 **pre-#19** ／
> 同素材の修正後の再採点は[一挙と追加素材の再採点](#一挙と追加素材の再採点2026-08-22)、
> TEN での値は[一挙2本の TEN 測定](#一挙2本の-ten-測定2026-08-22決着)

5時間22分1本。24分もの9本の合計（3時間36分）より長いので、
**単一の測定としてはこのプロジェクトで最大。**

厳密に計算されている指標では clip が全勝している。

| 指標 | clip | collect | 差 |
|------|------|---------|-----|
| ループ文字（参照不要） | **491** | 1628 | collect が3.3倍 |
| per-block CER plain（厳密Levenshtein） | **21.3%** | 23.5% | clip が2.2pt良い |
| 全文CER plain（帯状DP・両方厳密解） | **20.5%** | 22.5% | clip が2.0pt良い |
| 30秒超セグメント | 0（最長20s） | 0（最長28s、修復前190s） | — |
| 長さ比 | 0.96 | 0.97 | 同等 |
| 推論時間 | 1196s | **1036s** | collect が15%速い |

対応差分を見ると、**collect にだけ出ている区間の上位が全部ループ**である。

```
collect のみ 1:31:35.7-1:31:43.1  よーし、よーし、よーし、…（14回）
collect のみ 1:04:57.9-1:05:03.8  ううううううう…
collect のみ 0:38:21.1-0:38:39.8  あああああ…（18.7秒）
```

つまり collect の余分な占有はほぼ幻覚。24分ものではループ文字が
1353 vs 1363 で分離しなかった（#2）のに対し、長尺では3.3倍の差が付いた。
**「clip_timestamps のゼロ埋めが幻覚を誘発する」という collect 既定化の
根拠は、向きが逆だったことになる。** 推測される理由は
[設計判断](HANDOVER.md#外部vadの区間は2通りの渡し方がある既定は-clip)の該当項に書いた。

これを踏まえて **`--vad_segment_mode` の既定を `clip` に変更した。**
24分ものでは collect が1pt良かったが、あれはニンジャラ1本が作っていた差
（#5）。長尺で2pt負け・ループ3.3倍・30秒超の唯一の発生源、という3点で
collect を既定に置く理由が無い。速度15%は字幕用途では割に合わない。

副産物として **長尺劣化が無いことも分かった。** 全文CER 20.5% は24分もの
合計24.3%より良く、中央値16.7%と同水準。単語タイムスタンプも
5:21:51 まで正しく付いていて（参照の最終キューは 5:20:25）、
`sanitize_segments()` も5時間スケールで機能している。

**この結論を出すのに指標の欠陥2つを踏んだ。** 最初に出た全文CERは
clip 34.1% / collect 36.0% で、これは(1)話間47分43秒の素材を挿入として
課金していたぶんと(2)collect 側だけ帯が打ち切られて上限値になっていた
ぶんが乗った値だった。修正後は 20.5% / 22.5%。**14pt動いている。**

### #7. VADは「カバレッジを売って精度を買う」交換だった（novad対照）

> **採点条件** 出典 `_eval/score-final.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19**

`eval/run.py` に `novad` 設定が最初から入っていたのに一度も走らせておらず、
**VADを使うと精度が上がるのかを測っていなかった。** 24分もの9本で実測した。

| | clip（既定） | novad | 差 |
|---|---|---|---|
| coverage | 80.4% | **85.0%** | VADで4.6pt損 |
| precision | **87.7%** | 79.0% | VADで8.7pt得 |
| CER全文 | **24.3%** | 28.7% | 差引でVADが4.4pt得 |
| 長さ比 | 0.92 | **1.07** | novadは唯一の出しすぎ |
| ループ文字 | 1363 | **3417** | novadは2.5倍 |

**VADを入れる判断は正しい**が、初めて数字で言えるようになった。
内蔵VADはこの交換をさらに極端にしていて、精度91.9%と引き換えに
カバレッジ74.1%まで落とす。字幕用途では取りこぼしのほうが痛いので
外部VADが有利という #3 の読みも、これで裏付けられた。

**取りこぼし20ptのうち、VAD起因は約5pt。** novad でもまだ15%落ちるので、
残り15ptはVAD以外（認識器の限界、または参照側の到達不能ぶん＝歌詞・
話者名・効果音注記）。

**さらに重要なのは、その5ptが全体に薄く広がっているのではなく2本に集中している点。**

| ファイル | clip coverage | novad coverage | 差 |
|---------|--------------|---------------|-----|
| おねがいアイプリ | 47.3% | **73.2%** | **+25.9pt** |
| 死亡遊戯で飯を食う #09 | 59.7% | **73.2%** | **+13.5pt** |
| 他7本（合計） | 86.8% | 87.6% | +0.8pt |

**VADはアイプリで参照の26%、死亡遊戯で13%を捨てている。他7本では1pt未満。**
つまりVADの取りこぼしは全体的な性質ではなく、特定の素材で局所的に破綻している。

ただし単純にVADを切る答えではない。novad はアイプリで精度が
85.6%→71.2% に落ちる（CERは54.4%→43.5%と改善はする）。
アイプリの検出発話は 7分58秒/24分40秒 = 発話率0.32、死亡遊戯は0.23で、
どちらも「アニメ素材なら0.25〜0.6が妥当」というレンジの下端にある。
**この時点では「発話率を見て threshold を動かす自動調整」の根拠に見えたが、
#8 でそのルールは成立しないと分かった。**

### #8. `--vad_threshold` は取りこぼしの knob ではなかった（否定的結果）

> **採点条件** 出典 `_eval/score-sweep.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 結論（閾値は knob ではない）は世代に依存しない相対比較

#7 で「VADが取りこぼしている、閾値を下げれば取り返せるはず」となったので、
既定0.45に対して 0.30 と 0.15 を9本で振った。**取り返せなかった。**

カバレッジ（実際に回収できた参照文字）がほとんど動かず、精度だけが落ちる。

| 作品 | 指標 | 0.45 | 0.30 | 0.15 | novad |
|------|------|------|------|------|-------|
| 死亡遊戯 | coverage | 59.7% | 61.3% | 62.4% | **73.2%** |
| 死亡遊戯 | precision | 87.3% | 86.1% | **59.7%** | 64.5% |
| アイプリ | coverage | 47.3% | 48.5% | 51.7% | **73.2%** |
| アイプリ | precision | 85.6% | 84.1% | **76.2%** | 71.2% |

**閾値を下げて増えた文字はほぼ参照に無い。** 死亡遊戯は 0.15 で出力が
2200字→3104字に増えるが、カバレッジは2.7ptしか上がらず精度が27pt落ちる。
**字数だけ見て「効いた」と読むと間違える**（実際に一度読み間違えた）。
novad が73.2%まで取れるのに閾値では届かないので、
**落としている原因は threshold ではない。**

合計値も安定しない。**大きな逆向きの swing が打ち消し合っているだけ。**

| 作品 | 0.45 | 0.30 | 0.15 |
|------|------|------|------|
| ニンジャラ | 53.7% | **32.2%** | 28.5% |
| LIAR GAME | 11.1% | **19.3%** | 10.9% |
| 桃源暗鬼 | 12.6% | 11.9% | **17.1%** |
| 死亡遊戯 | 42.4% | 40.9% | **59.0%** |
| 終末ツーリング | 18.3% | 19.5% | **22.4%** |
| クレバテス | 16.7% | 15.8% | 16.4% |
| 公女殿下 | 8.8% | 8.3% | 8.5% |
| Summer Pockets | 14.0% | 13.4% | 13.4% |
| アイプリ | 54.4% | 53.9% | 54.2% |
| **合計** | **24.3%** | **22.7%** | **23.3%** |

合計では 0.30 が全指標で既定を上回る（CER 22.7%、カバレッジ81.3%、
精度88.6%、ループ文字861で既定1363の37%減）。**しかし中身を見ると
既定に戻す理由がある。** 1.6ptの改善はニンジャラの **-21.5pt**（幻覚が収まった）が
LIAR GAME の **+8.2pt**（逆に幻覚が出た）を上回ったぶんで、
LIAR GAME は 11.1% → 19.3% → 10.9% と**非単調**。
閾値と品質の関係が安定していないので、**既定は 0.45 のまま。**

**設計方針への影響が大きい。** 「speech ratio が低いときだけ threshold を
下げる」というルールは成立しない。理由が2つ。閾値では取りこぼしが取り返せない。
そして**最大の利得が出たニンジャラは発話率49.0%で健全側**なので発話率トリガーでは
発動せず、逆に発動対象のアイプリ（0.32）・死亡遊戯（0.23）では効かなかった。
**ルールが効かせたい相手と実際に効く相手が食い違っている。**

なお、ニンジャラの改善は結果的にループ抑制（#11・#12）で得られた。
VADを触らずに 53.7% → 31.8% まで下がっている。

### #9. VADパラメータ空間は打ち止め。既定が最良だった（否定的結果）

> **採点条件** 出典 `_eval/score-vadparams.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> `neg_threshold` は当時未配線で、後から振った結果は [#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果)

#8 で閾値が knob でないと分かったので、残る候補を潰した。
`--vad_min_silence_duration_ms` を 3000 → 500 → 100（silero既定は100）、
`--vad_speech_pad_ms` を 900 → 200（silero既定は30）。1変数ずつ振ってある。

| 設定 | CER全文 | coverage | precision | ループ文字 | セグメント |
|------|--------|----------|-----------|-----------|-----------|
| **clip-fixed（既定 0.45/3000/900）** | **24.3%** | **80.4%** | **87.7%** | **1363** | 2805 |
| clip-ms500 | 28.6% | 79.9% | 83.3% | 2086 | 3162 |
| clip-ms100 | 30.6% | 79.3% | 81.1% | 1763 | 3861 |
| clip-pad200 | 27.2% | 79.7% | 85.3% | 2031 | 2717 |
| novad | 28.7% | **85.0%** | 79.0% | 3417 | 3284 |

**カバレッジが1つも上がらない。むしろ下がる**（80.4% → 79.3〜79.9%）。
`min_silence` を下げるとクリップが細切れになるだけで（2805→3861セグメント）、
拾える発話は増えない。`speech_pad` 短縮も同じ。

**結論。触れる範囲のVADパラメータ（threshold / min_silence / speech_pad）は
すべて試して、現在の既定が最良だった。** 既定値の妥当性が裏付けられた一方、
**「VADを調整して取りこぼしを取り返す」という道は閉じた。**

**訂正。この時点では `neg_threshold` を振っていなかった**（そもそも配線されて
いなかったので気付いていない）。後から露出して振り、**同じく否定的**だった
（[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果)）。
**4つ振り終えた現在が本当の「打ち止め」。**
silero の発話確率が、この2本の欠けている台詞と雑音を分離できていない、
というのが素直な読み（アイプリは歌唱、死亡遊戯は静かな台詞と推測）。

なお `--vad_window_size_samples` は候補から外れた。**そもそも配線されていない。**
`vad.py` は silero-vad パッケージが import できると `utils = None` になり、
その経路（通常こちら）は `window_size_samples` を渡していない。
torch.hub フォールバック側でだけ効く。silero-vad 6.2.1 の既定は512なので、
既定1536は**モデルに到達しておらず**512で動いていた。宣言だけで
配線されていないオプションとして扱うこと。

**代わりに残った線は「VADを緩めて後段で幻覚を落とす」。** これが #11 に繋がった。

### #10. Faster-Whisper-XXL との比較（既定同士。推奨設定では未実施）

> **採点条件** 出典 `_eval/score-xxl.txt` `_eval/score-xxl-clean6.txt` ／
> 24分もの9本（および病的3本を除く6本）・歌唱含む ／ 世代 **pre-#19** ／
> 現行既定での XXL 比較は[修正後の9本](#修正後の9本2026-08-22)と[追加4本](#追加4本の-ten-測定2026-08-22決着) ／
> 構造面（30秒超・thin・ループ字数）は参照を使わないので**今も有効**

> **見出しの「推奨設定では未実施」は 2026-08-22 に解消した。**
> **XXL の推奨＝既定**で、開発者は追加設定の推奨リストを公表していない。
> 使用例に足されているのは整形プリセットだけで、**整形は全文CERを動かさない**。
> したがって既定同士の比較がそのまま推奨設定同士の比較になっている。
> 根拠と XXL 側の既定一覧は[今後やるなら 2](HANDOVER.md#2-推奨設定同士での本家比較決着既に測っていた2026-08-22)。
> **見出しは既存のアンカーを壊さないために残してある。**

本家 Faster-Whisper-XXL r245.4（無料版、Purfview作）と24分もの9本で比較した。
**測定した条件は「既定同士」だけで、両者をチューニングした条件は未実施。**

まず実行条件。**無料版XXLは RTX 5090（sm_120）で既定のまま動かない。**

```
-ct float16 / int8 / int8_float32
  → RuntimeError: cuBLAS failed with status CUBLAS_STATUS_NOT_SUPPORTED
-ct float32
  → 動く。8.75 audio秒/秒（24分ものが2分55秒）
```

`-ct float32` を明示して初めて通った。**これがこのプロジェクトの存在理由の実測。**
精度面では float32 は float16 と同等以上なのでXXLに不利ではないが、
**速度は比較にならない**（whisp-carrier は float16 で同じファイル85秒）。

条件の対応関係。**VADパラメータは完全に同一**で、これは whisp-carrier が
XXL互換で作られているため（silero既定の100/30から離れているのは意図的）。

| | whisp-carrier (clip-fixed) | XXL r245.4 |
|---|---|---|
| モデル | large-v3 | large-v3（同一） |
| compute_type | float16 | float32（制約） |
| VAD経路 | 外部silero + clip_timestamps | 内蔵 `silero_v4_fw` |
| threshold / min_silence / speech_pad | 0.45 / 3000 / 900 | **同一** |
| condition_on_previous_text | **false** | **true** |
| 幻覚対策 | 重複検出・ループ検出のみ | `hallucinations_list` / `ignore_dupe_prompt` / `v3_offsets` / `prompt_reset_on_no_end=2` / `reprompt` が既定ON |
| language | 自動判定（ja 92〜99%） | `ja` 明示 |

結果。

| 設定 | CER全文 | coverage | precision | CERブロック | 長さ比 | ループ文字 | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|-----------|--------|------|
| clip-fixed | 24.3% | **80.4%** | 87.7% | **26.0%** | 0.92 | 1363 | **0** | **0** |
| **XXL** | **23.7%** | 80.1% | **88.1%** | 27.8% | 0.91 | **53** | **9件 / 602s** | 15 |
| builtin-fixed（VAD経路をXXL側に寄せた条件） | 27.1% | 74.1% | 91.9% | 28.6% | 0.81 | 255 | 0 | 0 |

**合計値だけを見ると「差が無い」に見える**（全文CER 0.6pt、カバレッジ0.3pt、
精度0.4pt）。**これは誤読。ファイル別に並べると構図が逆転する。**

| 作品 | whisp-carrier | XXL | 差 |
|------|--------------|-----|-----|
| 公女殿下 #10 | **8.8%** | 12.6% | whisp -3.8 |
| LIAR GAME #10 | **11.1%** | 15.5% | whisp -4.4 |
| 桃源暗鬼 | **12.6%** | 12.8% | whisp -0.2 |
| Summer Pockets #26 | **14.0%** | 15.0% | whisp -1.0 |
| クレバテス #06 | **16.7%** | 18.2% | whisp -1.5 |
| 終末ツーリング #12 | **18.3%** | 24.2% | whisp -5.9 |
| おねがいアイプリ | 54.4% | **53.8%** | XXL -0.6（同点） |
| 死亡遊戯 #09 | 42.4% | **35.5%** | XXL +6.9 |
| ニンジャラ | 53.7% | **34.7%** | **XXL +19.0** |

**健全な6本は6勝0敗で whisp-carrier。負けているのは病的な2本だけ**
（アイプリは実質同点）。しかも **XXLの優位はほぼニンジャラ1本**で、
その中身はループ抑制（whisp 1091字 → XXL 5字）。
合計での同点は、この逆向きの2傾向が打ち消し合った結果。

問題の3本を外した数字（参照25,698字）。**全指標で whisp-carrier が勝つ。**

| 設定 | CER全文 | coverage | precision | CERブロック | ループ | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|--------|------|
| **whisp-carrier** | **13.2%** | **88.5%** | **92.3%** | **14.6%** | 25字 | **0** | **0** |
| XXL | 16.0% | 87.0% | 90.6% | 20.2% | 39字 | 3件/276s | 7 |
| builtin-fixed | 17.8% | 83.3% | 92.9% | 19.2% | 32字 | 0 | 0 |

CER 2.8pt、カバレッジ1.5pt、精度1.7pt、**per-block では5.6pt**（14.6% vs 20.2%）。
per-block の差が大きいのは、XXLがこの6本でも30秒超を3件276秒（最長195秒）
出していてセグメントも粗い（1794対2115）ため。
**字幕としての実用性は per-block に近いので、体感差はCERの2.8ptより大きい。**

ループも同じ構図で、**合計値で読むと誤る。1本に集中している。**

| 作品 | clip-fixed | XXL |
|------|-----------|-----|
| ニンジャラ | **1091字** | 5字 |
| 死亡遊戯 | 223字 | 0字 |
| アイプリ | 24字 | 9字 |
| 残り6本（合計） | **25字** | 39字 |

合計1363字のうち**1091字（80%）がニンジャラ1本**。健全な6本では25字で、
XXL（39字）より少ない。**XXLが強いのは最悪ケースだけ**で、
通常ケースでは負けている。

**逆にXXLは30秒超セグメントを5本に分散して出している。**

| 作品 | XXL 30秒超 | 最長 | 例 |
|------|-----------|------|-----|
| アイプリ | **5件 / 270s** | 80s | — |
| 終末ツーリング | 1件 / 195s | **195s** | `よかったindieってことで` |
| ニンジャラ | 1件 / 57s | 57s | `やるなーガムッチ` |
| クレバテス | 1件 / 47s | 47s | — |
| 公女殿下 | 1件 / 34s | 34s | — |

**これは #1 と同じ既存バグ**で、XXLは内蔵VAD経路を使うので同じ症状が
出ていて修復していない。whisp-carrier は `sanitize_segments()` で全ファイル0件。
`thin` も15件対0件。

**トレードオフの性質が逆。** XXLのループ優位は1本の異常ケースで出て、
XXLの30秒超は5本に分散して出る。字幕用途では後者のほうが日常的に当たる。
そして**ループ側は #11・#12 で解消した。**

**測定していないこと。** 推奨設定同士の比較。両者とも既定のままなので、
`--standard_asia` などの字幕整形を入れた条件、XXL側の `--sentence` や
`--ff_*` を入れた条件は未測定。
**「チューニングして勝てるか」はこの測定では答えていない**（[今後やるなら](HANDOVER.md#今後やるなら)）。

**なお、CERで勝っても「上位互換」にはならない。** 音声フィルター
（[既知の問題 2](HANDOVER.md#2-音声フィルターが実用レベルに達していない)）、`--realign` の安定性、話者分離の不在、
`--ff_rnndn_sh` や `mdx_kim2` 相当の不在が残る。
**文字起こしの主経路に限った優位**だと理解しておくこと。

#### 実運用での裏付け：Amatsukaze から素の XXL を呼ぶと落ちる（2026-08-23）

**「RTX 5090 で既定のまま動かない」を、コマンドラインではなく実運用経路で再現した。**
Amatsukaze の Whisper パスに XXL を指定し、**whisper-model を `未指定`、
whisper-option 欄を空**にして回した結果。

```
"...\Faster-Whisper-XXL_r245.4_windows\Faster-Whisper-XXL\faster-whisper-xxl.exe"
    "<...>\a0-0-0-0-main.aac" --output_dir "<...>\whisper" -f srt vtt

Model not found at: ...\Faster-Whisper-XXL\_models\faster-whisper-medium
model.bin: 100%|##########| 1.53G/1.53G [00:16<00:00, 91.7MB/s]
Standalone Faster-Whisper-XXL r245.4 running on: CUDA
Detecting language using up to the first 30 seconds.
Traceback (most recent call last):
  File "faster_whisper\transcribe.py", line 1118, in transcribe
  File "faster_whisper\transcribe.py", line 2147, in detect_language
  File "faster_whisper\transcribe.py", line 1719, in encode
RuntimeError: cuBLAS failed with status CUBLAS_STATUS_NOT_SUPPORTED
[PYI-33992:ERROR] Failed to execute script '__main__' due to unhandled exception!

AMT [warn] Whisper字幕生成に失敗: Exception thrown at Subtitle.cpp:94
AMT [info] フィルタパス1 予定フレーム数: 8632     ← エンコードは続行する
```

**この観測が足す情報が4つある。**

**(1) 素材にもVRAMにも依存しない。** 死んだのは `detect_language` → `encode`、
つまり**最初のエンコーダ forward** で、音声のデコードにも入っていない。

**(2) モデルサイズにも依存しない。** [#10](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施) の
失敗は large-v3 で観測したが、これは **XXL 自前の既定である medium**（1.53GB を
その場でダウンロードしている）。**大きいモデルだから落ちるのではない。**

**(3) オプション皆無で落ちる。** 渡っているのは `--output_dir` と `-f srt vtt` だけ。
**`-ct` を書かなければ既定の compute type で走り、それが Blackwell では通らない**
（どの値が既定かはログに出ないので断定しない。`-ct float32` を明示すれば動くことは
[#10](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施) で確認済み）。

**(4) 利用者から見た壊れ方は「録画は完成して字幕だけ付かない」。**
Amatsukaze は `Subtitle.cpp:94` で例外を出して警告に落とし、**エンコードを続行する**。
ジョブは失敗にならないので、**ログを読まないと字幕が無い理由が分からない。**

**配布時の位置づけに使える。** 「50番台では追加オプションなしで動くのが whisp-carrier」
という主張が、開発者の手元実験ではなく**利用者と同じ経路の実測**で言えるようになった。

**主張の範囲は2つに絞ること。** **版**は `r245.4`（無料版）に限る
（CTranslate2 側が sm_120 対応を入れれば話が変わる）。**カード**は 5090 で実測しただけ。
Blackwell は全部 sm_120 なので 5080/5070 でも同じはずだが、**測ってはいない。**

### #11. ループを潰した場合の上限。既定に載せるだけで本家を抜く

> **採点条件** 出典 `_eval/score-noloop.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 落としたループの本数と文字数は参照を使わないので**今も有効**（推論なしの上限測定）

推論なしの上限測定。`eval/strip_loops.py` で `score.py` の `is_loop()` が
判定したセグメントを出力から落として再採点した。
**候補（novad）だけでなく基準（clip-fixed）と本家（xxl）にも同じ処理を当てて**
条件を揃えてある。レポートは `_eval/score-noloop.txt`。

落とした量は既存の集計と一致する（clip 11本1363字、novad 21本3417字、
XXL 8本53字）。検出器が同じなので当然だが、突き合わせの確認になる。

| 設定 | CER全文 | coverage | precision | CERブロック | 長さ比 | ループ |
|------|--------|----------|-----------|-----------|--------|--------|
| clip-fixed | 24.3% | 80.4% | 87.7% | 26.0% | 0.92 | 1363字 |
| **clip-noloop** | **22.0%** | 80.3% | 90.7% | **23.4%** | 0.89 | 0 |
| novad | 28.7% | 85.0% | 79.0% | 30.1% | 1.07 | 3417字 |
| **novad-noloop** | **21.4%** | **84.8%** | 86.5% | **22.4%** | **0.98** | 0 |
| xxl | 23.7% | 80.1% | 88.1% | 27.8% | 0.91 | 53字 |
| xxl-noloop | 23.7% | 80.1% | 88.2% | 27.7% | 0.91 | 0 |

**結論が3つ。**

**(1) 既定のループを潰すだけで本家を抜く。** clip 24.3% → **22.0%** で、
XXL の 23.7%（ループを抜いても23.7%）を **1.7pt 上回る。**
per-block では 23.4% 対 27.8% で **4.4pt。** #10 の「合計では同点」は
これで解消する。**XXL側は同じ処理をしても動かない**（53字しか無い）ので、
上限同士の比較としてもこちらの勝ち。

**(2) カバレッジは減らない。** 80.4% → 80.3%（novad は 85.0% → 84.8%）。
**ループセグメントは参照に一致する文字をほぼ持っていない**ので、
「丸ごと落とすと正しいテキストも消える」という上限測定の楽観バイアスは、
実測では 0.1〜0.2pt しかなかった。得られる精度は +3.0pt（87.7 → 90.7）。

**(3) 誤爆のコストが測れた。これがいちばん実装に効いた。**
健全6本で `score.py` の検出器が落としたのは合計25字（`ジョジョジョジョジョ` /
`そそそそんなわけ` / `YouYouYouYou`）。その代償は

```
公女殿下     8.8% → 8.9%   （0.1pt 悪化）
LIAR GAME   11.1% → 11.0%  （0.1pt 改善）
クレバテス    16.7% → 16.7%  （不変）
```

**±0.1pt。** [既知の問題 1](HANDOVER.md#1-ハルシネーションループは解決残るのはループ形をしない余剰出力)の「誤爆が多く無効化した」は、
少なくともこの閾値・この素材では **CERに現れない。** ただし落ちているのは
悲鳴や言い直しという**実在の発話**なので、字幕としては消したくない側面がある。
CERで見えないコストとして意識しておくこと。

ファイル別（全文CER）。**ループが効くのはニンジャラだけ**で、他8本は
0.1pt 以内しか動かない。

| 作品 | clip-fixed | clip-noloop | novad-noloop | xxl |
|------|-----------|-------------|--------------|-----|
| 公女殿下 #10 | **8.8%** | 8.9% | 10.3% | 12.6% |
| LIAR GAME #10 | 11.1% | 11.0% | **9.5%** | 15.5% |
| 桃源暗鬼 | 12.6% | 12.6% | **12.4%** | 12.8% |
| Summer Pockets #26 | 14.0% | 14.0% | **12.2%** | 15.0% |
| クレバテス #06 | **16.7%** | **16.7%** | 23.3% | 18.2% |
| 終末ツーリング #12 | **18.3%** | **18.3%** | 30.5% | 24.2% |
| 死亡遊戯 #09 | 42.4% | 42.4% | **30.9%** | 35.5% |
| ニンジャラ | 53.7% | **31.8%** | 35.0% | 34.7% |
| おねがいアイプリ | 54.4% | 54.4% | **39.4%** | 53.8% |
| **合計** | 24.3% | 22.0% | **21.4%** | 23.7% |

**ニンジャラ 53.7% → 31.8%（−21.9pt）で XXL の34.7%を抜く。**
#10 で「XXLの優位はほぼニンジャラ1本」と書いた19ptがここで消える。

**死亡遊戯は clip では動かない**（42.4% 不変）。落ちた223字が字幕域の外に
あったため。**CERには出ないが、30秒級の壊れたキューが1本消えるので
字幕としては改善している。** CERだけ見ていると取りこぼす種類の利得。

**novad-noloop が合計で最良（21.4%）だが、既定にはできない。**
勝ち方が偏っていて、病的な3本で大勝ちする代わりに健全な2本で負ける
（クレバテス +6.6pt、終末ツーリング +12.2pt）。この2本は novad の
余剰出力が**ループ形をしていない**ので検出器が拾えず、精度が
77.3〜86.5% に留まる。合計21.4%対22.0%の 0.6pt は、この相殺の残り。

**つまり「VADを外す」は素材依存の判断で、「ループを潰す」は無条件に得。**
novad を活かす道は残っているが、素材依存の判断を1つのルールに落とす話になる。

### #12. 実装した検出器は上限にそのまま到達した（誤爆ゼロで）

> **採点条件** 出典 `_eval/score-prodloop.txt` ／ 24分もの9本・歌唱含む ／ 世代 **pre-#19** ／
> 「健全6本の出力が1文字も変わらない」「検出器の差は4件」は**出力の直接比較なので
> 採点指標に依存しない＝今も有効**

`loop_filter.py` を実装したので、**採点用の上限と同じ土俵で測り直した。**
`eval/strip_loops.py --detector production` で実物の検出器を既存の出力に当て、
`_eval/hyp/clip-prodloop/` として採点した（`_eval/score-prodloop.txt`）。

| | clip-fixed | **clip-prodloop（実装）** | clip-noloop（上限） | xxl |
|---|---|---|---|---|
| CER全文 | 24.3% | **22.0%** | 22.0% | 23.7% |
| coverage | 80.4% | 80.3% | 80.3% | 80.1% |
| precision | 87.7% | **90.7%** | 90.7% | 88.1% |
| CERブロック plain | 26.0% | **23.4%** | 23.4% | 27.8% |
| 落としたセグメント | — | **7本 / 1331字** | 11本 / 1363字 | — |
| 残るループ判定 | 11本 / 1363字 | 4本 / 32字 | 0 | 8本 / 53字 |

**上限との差が無い。** 全文CER・per-block・カバレッジ・精度のすべてで
`clip-noloop` と同じ値（per-block markup だけ 32.4% 対 32.3%）。
**落とした量は上限より少ない**（7本対11本）のに数字が同じなのは、
差分の4本が短い発話で、CERにほとんど寄与していないため。

**健全6本での誤爆はゼロだった。** 2つの検出器が食い違うのは9本で4セグメントだけで、
**すべて「採点側が落とし、実装側が残す」向き。** 中身を出すとこうなる。

```
クレバテス     'ジョジョジョジョジョ!'   ジョ x5 = span 10 < 12   → 残す
公女殿下       'そそそそんなわけ'         そ x4 = span 4           → 残す
LIAR GAME     'はああああああ'           あ 6連続 < 8            → 残す
ニンジャラ      'うわあああああ!'         あ 5連続 < 8            → 残す
```

**4つとも実在の発話**（叫び・言い直し）で、狙って残した側。
したがって**健全6本の出力は clip-fixed と完全に同一**で、上限測定で見えていた
±0.1pt の揺れも消えた。**それでいて合計CERは上限と同じ 22.0%。**

落ちたのは3ファイルの7セグメントだけ。

| 作品 | 落ちた数 | 文字数 | CER |
|------|---------|--------|-----|
| ニンジャラ | 5本 | 1084字 | 53.7% → **31.8%** |
| 死亡遊戯 #09 | 1本 | 223字 | 42.4%（不変。字幕域の外） |
| おねがいアイプリ | 1本 | 24字 | 54.4%（不変） |

**残存する「ループ判定」4本32字は上の意図的に残した4つ。**
`score.py` の検出器はこれをループと数えるので、レポートの `loops` 欄は
0にならない。**XXL の残存53字より少ない**ので、そこも負けていない。

### #13. 実運用設定（beam 10）は測定設定（beam 5）よりわずかに悪い

> **採点条件** 出典 `_eval/score-prod.txt` ／ 24分もの9本（一挙を除外）・歌唱含む ／
> 世代 **pre-#19** ／ 「`--language ja` が出力を1セグメントも変えない」は
> **出力の直接比較なので今も有効**

**実運用側に入っていた `--beam_size 10 --best_of 10` と yaml の `language: ja` を
測定側に入れて、9本を回して比べた**（beam 10 の出どころは Amatsukaze ではなく
whisper-option 欄。下の訂正を参照）。 レポートは `_eval/score-prod.txt`、
出力は `_eval/hyp/prod-beam10/`。比較相手は `clip-prodloop`
（beam 5・ループ抑制あり＝現行既定と同じ処理）にしてある。`clip-fixed` を
相手にすると beam の効果とループ抑制の効果が混ざる。

| 設定 | CER全文 | coverage | precision | per-block | ループ | セグメント | 長さ比 |
|------|--------|----------|-----------|-----------|--------|-----------|--------|
| clip-fixed（beam5・ループ抑制なし） | 24.3% | 80.4% | 87.7% | 26.0% | 11 (1363字) | 2805 | 0.92 |
| **clip-prodloop（beam5・現行既定）** | **22.0%** | **80.3%** | **90.7%** | **23.4%** | 4 (32字) | 2798 | 0.89 |
| prod-beam10（実運用） | 22.3% | 80.1% | 90.6% | 23.7% | 4 (31字) | 2762 | 0.88 |

ファイル別（全文CER）。**9本中8本で同等または悪化し、改善は1本だけ。**

| 作品 | beam 5 | beam 10 | 差 |
|------|--------|---------|-----|
| クレバテス #06 | 16.7% | 16.7% | 0.0 |
| 公女殿下 #10 | 8.8% | 8.9% | +0.1 |
| Summer Pockets #26 | 14.0% | **13.8%** | **−0.2** |
| 桃源暗鬼 | 12.6% | 12.6% | 0.0 |
| 終末ツーリング #12 | 18.3% | 19.4% | **+1.1** |
| 死亡遊戯 #09 | 42.4% | 42.5% | +0.1 |
| ニンジャラ | 31.8% | 32.8% | **+1.0** |
| おねがいアイプリ | 54.4% | 55.5% | **+1.1** |
| LIAR GAME #10 | 11.1% | 11.3% | +0.2 |

**推論は決定的なので、この差はノイズではない**（同入力・同設定なら同出力。
[コンテナ等価性](#コンテナ等価性mkv直渡しでバイト一致)で確認済み）。
0.3pt は小さいが、**方向が一貫している**のが根拠になる。

**機構は「beam 10 のほうが出力が減る」。** カバレッジ 80.3% → 80.1%、
長さ比 0.89 → 0.88、セグメント 2798 → 2762。個別では
ニンジャラ 75.1% → 74.1%、アイプリ 47.3% → 46.1% とカバレッジが落ちる。
ビーム幅を広げると尤度の高い短い系列が選ばれやすく、
`condition_on_previous_text=False` では継続を報酬づける文脈が無い、と読める。

**時間はほぼ変わらない**（クレバテス #06 で 131.3s → 135.0s、+2.8%）。
つまり **beam 10 は +3% の時間を払って 0.3pt 悪化しており、両方失っている。**

**したがって実運用側を変える。** 方法は2つあり、**欄を直すほうが素直。**

- **Amatsukaze の whisper-option 欄から `--beam_size 10 --best_of 10` を消す。**
  既定が 5 なので何も書かなければ 5 になる
- **yaml で `override: true` のまま `beam_size: 5` / `best_of: 5` と書く。**
  欄を触らずに戻せる。当時はこちらを採った

> **訂正（2026-08-22）。この節は当初「Amatsukaze はコマンドラインで
> `--beam_size 10 --best_of 10` を渡してくる」と書いていたが、それは誤り。**
> 渡していたのは**プロファイル画面の whisper-option 欄に入っていた文字列**で、
> 以前の実験の名残だった。**ソフトが強制しているわけではない。**
> したがって「Amatsukaze を触らずに戻す」必要も本来は無く、
> **yaml の `beam_size` / `best_of` は欄を空にすれば不要になる。**

#### `--language ja` は出力を1文字も変えない（交絡を解消済み）

上の条件には `--language ja` も入っていたので、beam 単独の効果か切り分けた。
`prod-lang`（beam 5 + `--language ja`）を走らせて `clip-prodloop`
（beam 5 + 言語自動判定）と比べたところ、**9本すべてで
(start, end, text) の列が完全一致した。** 出力は1セグメントも変わらない。

理由は単純で、**自動判定は元々全ファイルで ja を 92〜100% の確度で当てている**
ので、`ja` を強制しても同じデコード経路に入る。

**したがって #13 の 0.3pt は全部 `beam_size` / `best_of` の効果。**
`language` を yaml に書くかどうかは精度に影響しない（分割で出る短いファイルの
誤判定対策としては意味があるので、書いておいて損はない）。

**比較の落とし穴を2つ踏んだので記録しておく。**

- **`clip-fixed` を相手にすると 6/9 一致になる。** `clip-fixed` は
  ループ抑制の導入前の記録なので、差が出る3本は問題の3本
  （死亡遊戯 181→180 / ニンジャラ 315→310 / アイプリ 194→193）で、
  これは `language` ではなく**ループ抑制の差**。比較相手は `clip-prodloop`
- **`clip-prodloop` とはバイト比較できない。** あれは
  `eval/strip_loops.py` が書き直した JSON なので、キー順や id の付け方が
  `whisp_carrier.py` の writer と違う。**セグメント数は9本すべて一致するのに
  バイトは9本すべて不一致になる。** 中身（start / end / text）で比べること

### #14. 追加素材5本。**取りこぼしは「問題の3本」ではなく子供向け番組の性質だった**

> **採点条件** 出典 `_eval/score-new.txt`（24分もの4本）・`_eval/score-new-long.txt`（一挙 4h52m）・
> `_eval/score-new-xxl.txt`（XXL比較）／ いずれも歌唱含む ／ 世代 **pre-#19** ／
> **この節の中心的な結論（子供向けの取りこぼしは実装で直せない）は [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) で否定された**

[追加5本](HANDOVER.md#素材)を記録値と同一条件（beam 5・言語自動・整形なし・ループ抑制あり）で
回した。出力は `_eval/hyp/new-clip/`、レポートは `_eval/score-new.txt`
（24分もの4本）と `_eval/score-new-long.txt`（一挙）。

#### 24分もの4本（参照 17,411 字）

| 作品 | CER全文 | coverage | precision | 長さ比 | 検出発話 |
|------|--------|----------|-----------|--------|---------|
| ニンジャラ「花火が開く夜空に」 | 21.1% | 79.9% | 90.8% | 0.88 | 12:33 / 23:55 |
| ぷにるはかわいいスライム #20 | 30.6% | 72.1% | 87.5% | 0.82 | 12:15 / 24:10 |
| 名探偵プリキュア！ #21 | 34.8% | 67.5% | 88.9% | 0.76 | 8:32 / 24:40 |
| おねがいアイプリ（情熱） | 54.2% | 47.0% | 88.6% | 0.53 | 6:35 / 24:40 |
| **合計** | **34.1%** | **67.7%** | **89.0%** | 0.76 | — |

**既存9本（22.0% / 80.3% / 90.7%）より12pt悪い。しかし故障の質は同じ。**
精度は 87.5〜90.8% で既存と変わらず、**カバレッジだけが 47〜80% に落ちている。**
4本すべて `MISSED SPEECH dominates`。間違えているのではなく出していない。

**結論が2つ出た。**

**(1) アイプリの悪さは作品の性質。** 別話で 54.2% / カバレッジ 47.0% となり、
既存話の 54.4% / 47.3% とほぼ同一。**1話の事故ではない。**

**(2) 「問題の3本」という括りは間違いだった。** 新規のぷにる（30.6%）と
プリキュア（34.8%）が同じ故障モードを示した。**取りこぼしは特定の3本ではなく、
子供向け番組で一貫して起きる。** [素材の項](HANDOVER.md#解決済み問題の3本は局ではなく内容だった)で
「局か内容か」を交絡候補としていたが、**内容側が本命**になった
（追加4本はニンジャラ・ぷにる・プリキュア・アイプリで全部子供向け）。

**検出発話の少なさが直接の症状。** プリキュアは24:40中8:32（35%）、
アイプリは6:35（27%）しか発話として拾えていない。会話劇は24分中14〜15分（60%）
出るので**半分以下**。[測定結果 #9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)で
VADパラメータは打ち止めと結論しているので、**ここはパラメータでは動かない。**

**ループ抑制は新素材でも正常。** 抑制後の残存は合計1件6字（`ぬあああああ`）。
落としたのはニンジャラ 444字・ぷにる 267字で、
**プリキュアとアイプリはループ0**。つまり子供向けでもループの出方は一様でなく、
**取りこぼしとループは別の現象。**

**ニンジャラのループは別話でも再発した**（444字、`ええええ…` 20秒）。
[未検証だった項目](#未検証のこと)に答えが出た形。ただし
**アイプリの別話ではループが出ていない**ので、再発は作品によって違う。

#### 取りこぼしは実装ではなく Whisper 側の限界（既存データで確認）

**「XXL でも同じなら Whisper 特有の問題」という切り分けができる。**
`_eval/score-xxl.txt` に per-file のカバレッジが既に入っていた。XXL は同じ
large-v3 を使うが、内蔵VAD経路・`condition_on_previous_text=true`・
独自の幻覚対策群・`-ct float32` という別実装である。

| 作品 | うち（clip） | うち（内蔵VAD） | **XXL** | 判定 |
|------|-------------|----------------|---------|------|
| おねがいアイプリ | 47.3% | 35.7% | **49.3%** | **実装非依存** |
| ニンジャラ | 75.4% | 62.4% | **75.3%** | **実装非依存** |
| 死亡遊戯 #09 | 59.7% | 63.5% | **69.1%** | XXL が9.4pt良い |
| （合計9本） | 80.4% | 74.1% | 80.1% | 同等 |

**アイプリとニンジャラは XXL でもカバレッジがほぼ同じ。** つまり
[#7〜#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)で
「VADパラメータでは取り返せない」と結論した取りこぼしは、
**VADの調整不足ではなく、モデルか音声そのものに由来する。**
別実装が同じ壁に当たっている。

**死亡遊戯だけは XXL が9.4pt良いので、ここには実装差の余地がある。**
当時の仮説は XXL の `condition_on_previous_text=true` が静かな台詞を文脈で
拾っていることだった。**これは測って否定された**（カバレッジ 59.7% → 21.3%、
[#15](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定)）。
**余地がある**という読みは正しく、**埋めたのは `novad` + ループ抑制**
（CER 30.9% / カバレッジ 71.6% で XXL の 35.5% / 69.1% を上回る）。

内蔵VAD（`builtin-fixed`）がアイプリで35.7%まで落ちるのに対し XXL は49.3%
なのは、XXL が内蔵VAD経路でも独自のパラメータを使っているため。
**外部VAD + clip（47.3%）は XXL の内蔵経路と同等**まで来ている。

#### 追加4本で XXL と直接比較した（決定的）

上は既存素材での確認なので、**子供向け4本に XXL を直接当てた。**
出力は `_eval/hyp-xxl-new/`（生）と `_eval/hyp/xxl-new/`（変換後）、
レポートは `_eval/score-new-xxl.txt`。XXL は r245.4、`-ct float32 --language ja`。

| 設定 | CER全文 | coverage | precision | per-block | ループ | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|--------|------|
| **うち（new-clip）** | **34.1%** | 67.7% | **89.0%** | **36.2%** | 1件 6字 | **0** | **0** |
| XXL | 36.3% | **69.1%** | 83.1% | 40.0% | 4件 482字 | **4件 185s** | 10 |

**カバレッジの差は 1.4pt しかない**（69.1% 対 67.7%）。別実装でも
**子供向け番組では参照の3割を落とす。** つまり
[#7〜#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)の
「VADパラメータでは取り返せない」は調整不足ではなく、
**モデルがそもそも拾えていない**ということ。**ここは実装で埋められない。**

ファイル別。**[#10](#10-faster-whisper-xxl-との比較既定同士推奨設定では未実施)と同じ構図が再現した**
（健全側はこちらが勝ち、病的な側は XXL が勝つ）。

| 作品 | うち | XXL | 差 |
|------|------|-----|-----|
| ニンジャラ（花火） | **21.1%** | 29.0% | うち −7.9 |
| ぷにる #20 | **30.6%** | 36.4% | うち −5.8 |
| プリキュア #21 | 34.8% | **33.0%** | XXL −1.8 |
| アイプリ（情熱） | 54.2% | **48.5%** | XXL −5.7 |
| **合計** | **34.1%** | 36.3% | **うち −2.2** |

**XXL は多く拾う代わりに精度を落としている。** 検出発話はこちらより
50〜59%多い（プリキュア 12.8m 対 8:32、アイプリ 10.5m 対 6:35）のに、
カバレッジは 1.4pt しか増えず精度は 5.9pt 落ちる（89.0% → 83.1%）。
差引で CER は悪化。

**構造的な欠陥では差が大きい。** XXL は 30秒超4件185秒（最長72秒）、
thin 10件、ループ482字。こちらは 30秒超0・thin 0・ループ6字。
プリキュアでは72秒のキューに
「フランスはこれまではその観点で創り出せない」という無関係なテキストが入った。
**字幕としての実用性は per-block に近いので、36.2% 対 40.0% の3.8ptが体感差。**

#### 一挙2本目（マジルミエ #01-#12、4時間52分、参照 50,563 字）

レポートは `_eval/score-new-long.txt`。**これまでの全測定で最良の数字。**

| 指標 | マジルミエ | ハクメイ（1本目・5h22m） | 24分もの9本 |
|------|-----------|------------------------|------------|
| CER全文 | **17.3%** | 20.5% | 22.0% |
| coverage | **85.4%** | 83.9% | 80.3% |
| precision | **91.0%** | 87.5% | 90.7% |
| per-block plain | **18.1%** | 21.3% | 23.4% |
| 30秒超 | **0** | 0 | 0 |
| ループ（抑制後） | 4件 29字 | — | 4件 32字 |
| 検出発話 | 2:23:17 / 4:51:59 | — | — |

**長尺劣化が無いことが2本目で裏付けられた**（[#6](#6-一挙放送で-clip-が勝ち既定を-clip-に戻した)は1本のみの観測だった）。
24分もの平均より4.7pt良く、4時間52分でも30秒超はゼロ、ループも29字だけ。
`sanitize_segments()` と `loop_filter` が5時間スケールで機能することの2例目。

マジルミエは職場ドラマなので「会話劇は強い」というパターンとも整合する。
**つまり尺ではなく内容が効く**という読みが、長尺側からも支持された。

### #15. `condition_on_previous_text=true` は死亡遊戯を破壊した（仮説の否定）

> **採点条件** 出典 `_eval/score-cond-prev.txt` ／ 死亡遊戯 #09 の1本・**歌唱含む** ／
> 世代 **pre-#19** ／ カバレッジ・推論時間・セグメント数・出力の打ち切り位置は
> 参照側の欠陥に影響されない量なので**結論は今も有効**
>
> この節はかつて `--exclude-songs` 世代に分類されていたが、
> **出典の条件行に歌唱除外が無い**（`1 file(s) scored | ... | only 2026030522` のみ）。
> 除外を導入したのは次の [#16](#16-歌唱区間を除外したら構図が変わったexclude-songs)。

[次の着手 C](#不要になったc-死亡遊戯の取りこぼし) の仮説を実測した。
**「XXL の `condition_on_previous_text=true` が静かな台詞を文脈で拾っている」は成立しない。**
出力は `_eval/hyp/cond-prev/`、レポートは `_eval/score-cond-prev.txt`、
内訳の走り書きは `_eval/_cond_probe.py` / `.txt`。

条件は記録値と同一（`ext-clip` / large-v3 / beam 5 / 言語自動 / 整形なし /
ループ抑制あり）で、**`--condition_on_previous_text true` の1点だけを変えた。**
VADは両方とも51区間なので、差は純粋にデコーダ側の条件付けだけ。

| | clip-prodloop（既定） | cond-prev | 差 |
|---|---|---|---|
| CER全文 | 42.4% | **79.4%** | **+37.0pt 悪化** |
| coverage | **59.7%** | **21.3%** | **−38.4pt** |
| precision | 87.3% | 87.6% | 同等 |
| per-block plain | 42.8% | 83.3% | +40.5pt 悪化 |
| セグメント | 180 | **67** | −113 |
| 出力文字 | 2023 | **986** | 49% |
| 占有時間 | 6:59.6 | **1:58.2** | 28% |
| 最終セグメントの終端 | 27:05 | **18:16** | **以降が空**（尺は30:00） |
| 推論時間 | 65.4s（`clip-fixed`） | **384.2s** | **5.9倍** |

**予測と逆向きに、しかも桁で外れた。** 仮説はカバレッジ 59.7% → 69.1% への接近を
期待していたが、実際は 21.3% まで落ちた。**取りこぼしを増やしただけで、
静かな台詞は1つも拾っていない。**

**故障の形は縮退。** 出力の中身を見ると、20.6秒にわたって `. . . . .` を並べた
セグメントと、`いい加減にしろよ` を終端状態として反復する列になる。

```
     219.3-   239.9  . . . . . . . . . . . . . . . . . . . . . . . . （20.6秒）
     462.8-   465.8  いい加減にしろよ
     465.8-   466.0  いい加減にしろよ
    1083.3-  1083.7  おいしいね
    1091.4-  1092.7  いい加減にしろよ
    1093.8-  1095.6  いい加減にしろよ
```

これは[設計判断](HANDOVER.md#condition_on_previous_text--falseデフォルト)に記録されていた
「`True` → 76セグメント（ハルシネーションで後半が壊れる）」の再現で、
**ループ抑制が入った現在でも同じ壊れ方をする。**

**遅延の正体は温度フォールバックではない。** `--temperature` の既定は
スカラー 0 なので再試行は起きない。**各窓が縮退してトークン上限まで
デコードしている**のが5.9倍の中身で、これ自体がハルシネーションの兆候。

**`loop_filter` はここでは無力だった。落としたのは0件。** 理由が2つあって、
**どちらもコードで確認した設計どおりの挙動**である。

- **`. . . . .` は `normalize()` が空文字列にする。** `_PUNCT_CHARS` に
  `.` と空白が入っているので、あの20.6秒のセグメントは折り畳んだ結果が
  空になり、`reason()` は最初の `if not folded: return None` で抜ける。
  **判定対象がゼロ文字なのでルール1〜3のどれにも到達しない。**
  文字クラスを `eval/normalize.py` の `plain` と揃えた結果で、
  **句読点だけで構成された縮退は原理的に見えない**
- **`いい加減にしろよ` の反復はセグメントをまたぐ。** `loop_filter` の
  docstring が明示しているとおり検出はセグメント単位で、
  **またぐ繰り返しは `whisp_carrier.py` の `is_hallucination()` の担当**

そして `is_hallucination()` は動いていた。ただし `MAX_DUPES = 2` なので
**連続3回目以降しか捨てない。** 出力に `いい加減にしろよ` が対で残っているのが
その痕跡で、**生の出力にはさらに反復が入っていたことになる。67セグメントは
その打ち切り後の数字。**

**ここが今回いちばん実装に効く発見。** 縮退の2つの形（句読点だけのセグメント、
セグメントをまたぐ反復）は、**現在の検出器のどちらにも十分には引っかからない。**
[既知の問題 1](HANDOVER.md#1-ハルシネーションループは解決残るのはループ形をしない余剰出力)の
「ループ形をしない余剰出力」に、この2つを追記しておくこと。
**ただし既定（`false`）ではこの縮退そのものが起きないので、優先度は低い。**

**XXL が `true` で運用できているのは、対策群と組み合わせているから。**
`hallucinations_list` / `ignore_dupe_prompt` / `prompt_reset_on_no_end=2` /
`reprompt` が既定ONで、**`true` を成立させるための装備がある。**
こちらにあるのはセグメント単位の後処理だけなので、
**`true` を単体で借りてくることはできない。**

**そして仮説を追う必要はもう無い。** 同じ素材で `novad` + ループ抑制が
**CER 30.9% / カバレッジ 71.6%** に達していて、**XXL（35.5% / 69.1%）を
全指標で上回っている**（`_eval/score-noloop.txt`、推論不要で読める）。
実装差の余地は既に埋まっていて、残っているのは
**「いつ VAD を外すか」の発動条件だけ**（[次の着手 C](#不要になったc-死亡遊戯の取りこぼし)）。

### #16. 歌唱区間を除外したら構図が変わった（exclude-songs）

> **採点条件** 出典 `_eval/score-nosing.txt` ／ 24分もの9本・**`--exclude-songs`（この節で導入）** ／
> 世代 **pre-#19** ／ **この節の結論（歌唱除外で構図が変わる）は [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) で
> 前提ごと変わった**。修正後は歌唱窓を落とすと悪化するので、フィルタは入れない

**字幕用途では歌に字幕は要らない**という判断で、参照と仮説の両側から歌唱区間を
落として9本を採点した。レポートは `_eval/score-nosing.txt`、
参照は 36,053 → **35,036字**（1.6%減）。比較は
`clip-prodloop`（現在の既定）/ `novad-prodloop` / `xxl`（本家既定）。

#### 音楽記号は存在した。ただし `score.py` が探していない場所に

**`build_blocks()` の歌唱除外はこの素材で一度も発動していなかった。**
`normalize.MUSIC_RE` が U+2669〜U+266F を探すのに対し、
**この局の字幕は音楽記号を DRCS ビットマップとして送っていて、
私用領域にマップされる。** つまり照合すべき codepoint が存在しない。

**U+EC00 を音符と見なすのは誤り。** 一度そう仮説を立てたが、
**クレバテス62件・公女殿下56件・LIAR GAME74件の通常台詞に乗っている**ので
行継続の矢印であって音符ではない。DRCS は per-file のビットマップ定義なので
codepoint からは区別できない。**この線は使えない。**

代わりに参照そのものが持っている2つのマーカーを使った（`normalize.is_sung()`）。

| マーカー | 意味 |
|---------|------|
| **波ダッシュ1文字だけのキュー** | 器楽区間。「ここに音楽が鳴る」だけで歌詞は印字されない |
| **キュー全体が `「」` で囲まれている** | 印字された歌詞 |

**15本すべてに当てて誤爆を確認した**（`_eval/_is_sung_check.py` / `.txt`）。

| 素材 | 落ちる参照文字 | 落ちるブロック時間 |
|------|--------------|------------------|
| **終末ツーリング #12** | **0字（0.0%）** | **330.7s** |
| おねがいアイプリ（はじめて） | 1002字（**22.9%**） | 309.0s |
| おねがいアイプリ（情熱） | 677字（**17.6%**） | 242.9s |
| 死亡遊戯 #09 | 0字 | 136.3s |
| プリキュア #21 | 30字（0.8%） | 110.0s |
| マジルミエ（4h52m） | 308字（0.6%） | 921.5s |
| ハクメイ（5h22m） | 238字（0.6%） | 902.7s |
| Summer Pockets #26 | 15字（0.4%） | 43.9s |
| 公女殿下 / LIAR GAME / クレバテス / ニンジャラ2本 / ぷにる / 桃源暗鬼 | **0字** | 0〜192s |
| **合計** | **2270字 / 142192（1.6%）** | — |

**健全な会話劇では1字も落ちない。** 誤爆は長尺2本の引用台詞で数件あり
（マジルミエの `（越谷）「でっけえことはいいことだ」` はセリフだが全体が
括弧で囲まれるので歌詞と判定される）、**合計0.02%なので影響しない。**

**終末ツーリングが0字で330.7秒というのが要点。** 歌詞が印字されていないので
参照からは何も減らないが、**そのブロックが消えることで
「novad がそこに書いた誤った歌詞」も課金対象から外れる。**
`build_blocks()` がブロックを落とし、`score_cer_whole()` は
生き残ったブロックからしか仮説テキストを拾わないので、**除外は自動的に対称。**

**既定はオフにしてある。** 記録されている全数値は除外なしで測ったものなので、
既定を変えると再現できなくなる。`--exclude-songs` で明示する。

#### 結果（9本、参照 35,036字）

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | ループ | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|--------|--------|------|
| clip-prodloop（現在の既定） | 19.7% | 82.6% | **90.8%** | 20.9% | 0.91 | **4 (32字)** | **0** | **0** |
| **novad-prodloop** | **19.3%** | **85.8%** | 87.8% | **20.8%** | 0.98 | 9 (62字) | **0** | 3 |
| xxl（本家既定） | 21.4% | 82.3% | 88.3% | 25.4% | 0.93 | 8 (53字) | **9件 / 602s** | 15 |

**ファイル別。**

| 作品 | clip | novad | XXL | novad の向き |
|------|------|-------|-----|-------------|
| 公女殿下 #10 | **8.8%** | 10.3% | 12.6% | +1.5 |
| LIAR GAME #10 | 11.1% | **9.5%** | 15.5% | **−1.6** |
| Summer Pockets #26 | 14.1% | **12.3%** | 15.4% | **−1.8** |
| 桃源暗鬼 | 12.6% | **12.4%** | 12.8% | −0.2 |
| クレバテス #06 | **16.7%** | 23.3% | 18.2% | **+6.6** |
| **終末ツーリング #12** | 17.3% | **15.7%** | 21.1% | **−1.6（符号が反転した）** |
| ニンジャラ（ガムッチ） | **31.8%** | 35.2% | 34.7% | +3.4 |
| **死亡遊戯 #09** | 42.4% | **30.6%** | 35.1% | **−11.8** |
| **アイプリ（はじめて）** | 40.9% | **35.8%** | 41.5% | **−5.1** |
| **合計** | 19.7% | **19.3%** | 21.4% | −0.4 |

**結論が4つ。**

**(1) 終末ツーリングの損失は全部が器楽区間だった。** 除外前は
clip 18.3% 対 novad 30.5% で **novad が12.2pt負け**ていたのが、
除外後は 17.3% 対 **15.7%** で **novad の勝ちに反転した。**
[前節](#健全素材に-novad-を当てたときのコスト実測)で「production 検出器が
この1本から1件も落とせない」と書いた余剰出力は、**参照が歌詞を印字していない
器楽区間に書かれた幻覚**であって、対話の認識精度とは無関係だった。

**(2) 発動条件の余裕の問題が解消した。** 前節では
「損失最大の終末ツーリング（発話率0.40）がトリガー境界の隣にいて余裕が0.08しかない」
と書いたが、**その1本が損失側から消えた。** 残る損失は
クレバテス（発話率0.50）とニンジャラ（0.45）で、狙う2本（0.23 / 0.32）から
十分離れている。**クレバテスの損失は歌唱由来ではない**
（参照の器楽区間は6.7秒1件のみ、歌詞キューは0件で、除外前後の数字が同一）。
そこはカバレッジが 84.8% → 82.6% と**下がる**ので、
[既知の問題 1](HANDOVER.md#1-ハルシネーションループは解決残るのはループ形をしない余剰出力)の別問題。

**(3) アイプリの「到達不能な誤差」は13.5ptあった。**
clip 54.4% → **40.9%**、XXL 53.8% → 41.5%。
参照の22.9%が歌詞なので、**歌に字幕を出さない前提ならこの13.5ptは
そもそも誤差ではない。** 配布時に出す数字はこちらのほうが実態に近い。

**(4) 本家との差が広がった。** 推奨設定（novad 混成なし）でも
**19.3% 対 21.4%**、per-block は **20.8% 対 25.4%（4.6pt）。**
ファイル別では clip が **8勝1敗**（負けは死亡遊戯のみ）、
novad が **7勝2敗**（クレバテスとニンジャラ）。
構造的欠陥は依然として一方的で、**XXL は30秒超9件602秒・thin 15件、
こちらは0件と3件。**

#### 発話率トリガーを入れた場合の上限（混成）

死亡遊戯とアイプリだけ `novad` にし、残り7本を `clip` にした合計を
参照文字数で加重して出した（CER は 距離/参照長 なのでこの加重は厳密）。

| 構成 | CER全文 |
|------|--------|
| clip 一本（現在の既定） | 19.7% |
| novad 一本 | 19.3% |
| **混成（発話率0.35未満で novad）** | **18.4%** |
| xxl（本家既定） | 21.4% |

**現在の既定より1.3pt、本家より3.0pt良い。** ただしこれは
**2点フィットの上限値**で、[素材の項](HANDOVER.md#素材を足すべきか判断済み)のとおり
低発話率の素材を足さずに閾値を確定させるのは過学習。
**混成をレポートとして直接採点したものではない**（加重計算）。

### #17. `--vad_neg_threshold` も knob ではなかった。VAD空間は本当に打ち止め（否定的結果）

> **採点条件** 出典 `_eval/score-negthr.txt` ／ 4本（死亡遊戯・アイプリ・終末ツーリング・
> クレバテス）・`--exclude-songs` ／ 世代 **pre-#19** ／
> **決定的な診断（区間数が死亡遊戯だけ動かない＝検出側の失敗）は VAD のログから出た量で、
> 採点指標に依存しない＝今も有効。** それが [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) に繋がった

**[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果) で「打ち止め」と書いたが、
実際には振っていないパラメータが1つ残っていた。** `neg_threshold`（silero の
終話判定ヒステリシス）で、**`vad_parameters` にも `vad.py` にも渡していなかったので
常に自動値 `max(threshold - 0.15, 0.01)` = 0.30 で動いていた。**
`--vad_neg_threshold` として露出し、外部VAD（既定経路）と内蔵VADの両方に配線した。

レポートは `_eval/score-negthr.txt`（歌唱除外、参照 13,282字）、
出力は `_eval/hyp/clip-neg020` / `-neg005` / `-neg001`。
素材は狙いの2本（死亡遊戯 0.23 / アイプリ 0.32）と
健全側の2本（終末ツーリング 0.40 / クレバテス 0.50）。

#### 合計（4本）

| neg_threshold | CER全文 | coverage | precision | per-block |
|--------------|--------|----------|-----------|-----------|
| **既定（自動 0.30）** | 27.2% | 74.9% | **89.4%** | 28.1% |
| 0.20 | 27.3% | 75.0% | 89.0% | 28.2% |
| 0.05 | **27.0%** | 75.3% | 89.3% | **27.9%** |
| 0.01（silero の下限） | 27.4% | **75.3%** | 88.6% | 28.2% |

**全振り幅が 0.4pt で、しかも単調でない**（最良は中間の 0.05、下限の 0.01 は悪化）。
カバレッジも 74.9% → 75.3% の 0.4pt しか動かない。**`--vad_threshold` のとき
（[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果)）と同じ形の否定的結果。**

#### 狙いの2本でも動かない

| 作品 | 指標 | 既定 | 0.20 | 0.05 | 0.01 | **novad（参考）** |
|------|------|------|------|------|------|-----------------|
| 死亡遊戯 | CER | 42.4% | 43.0% | 42.6% | **41.8%** | **30.6%** |
| 死亡遊戯 | coverage | 59.7% | 59.7% | 60.1% | **60.8%** | **71.6%** |
| 死亡遊戯 | precision | **87.3%** | 86.3% | 86.0% | 84.9% | 87.1% |
| アイプリ | CER | 40.9% | 40.6% | **40.1%** | 40.5% | **35.8%** |
| アイプリ | coverage | 61.3% | 61.6% | **62.1%** | 61.7% | **80.3%** |

**下限まで振って死亡遊戯のカバレッジは +1.1pt。** `novad` が同じ素材で
+11.9pt 取るので、**埋まったのは差の 9%。** アイプリも +0.8pt で、
`novad` の +19.0pt に対して 4%。**この knob では届かない。**

健全側は 0.05 まではほぼ無害だが、**終末ツーリングは下げるほど単調に悪化する**
（17.3% → 17.3% → 18.0% → **19.4%**、精度 91.3% → 88.8%）。
つまり**下限側では novad と同じ壊れ方が始まる**だけで、
利得と損失の交換比は改善しない。

#### 決定的な診断：死亡遊戯は「切り方」ではなく「検出」の失敗

**VAD区間数の動き方が答えを持っていた。**

| 作品 | 既定 | 0.20 | 0.05 | 0.01 |
|------|------|------|------|------|
| クレバテス | 19 | 18 | 17 | **12** |
| 終末ツーリング | 39 | 38 | 35 | **22** |
| アイプリ | 21 | 22 | 18 | **15** |
| **死亡遊戯** | **51** | **50** | **53** | **50** |

**健全な素材では下限で区間が3〜4割まとまるのに、死亡遊戯だけ動かない。**
ヒステリシスを緩めると「短いポーズで切れていた隣接区間」が繋がるので、
区間数が減るのが正しい応答である。**死亡遊戯で減らないということは、
落ちている発話が「早く切られた区間の続き」ではない**ということ。
実際の無音で隔てられているので、終話判定を緩めても繋がる相手がいない。

**つまり死亡遊戯の取りこぼしは検出（開始）側の失敗。**
[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果) で開始閾値を
0.15 まで下げてもカバレッジが 62.4% で止まったのと整合する。
**落ちている音声は silero の発話確率が 0.15 未満**で、
「静かな台詞をVADが切っている」のではなく **silero がそれを発話と見なしていない。**
`novad` が効くのは判定そのものを迂回するからで、**VADの内側には解が無い。**

#### 結論

**触れるVADパラメータは4つすべて振り終えた。** threshold / min_silence /
speech_pad（[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果)・[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)）と
neg_threshold（本節）。**どれも既定を上回らず、既定 0.45 / 3000 / 900 / 自動が最良。**
`--vad_window_size_samples` は配線されておらず silero 既定 512 で動いている
（[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)）ので、**残っているパラメータは無い。**

**`--vad_neg_threshold` は残す。** 既定は `None`（silero の自動値）なので
既存の挙動は不変で、記録値もすべて再現できる。
**別のVADモデルや別の素材では効く可能性があるので、宣言だけで
配線されていないオプションを1つ減らした意味はある。**

**未測定。** `neg_threshold` と `min_silence_duration_ms` の2次元。
`min_silence` が 3000ms（silero既定100の30倍）なので終話判定を支配していて、
ヒステリシスの余地をそこが潰している可能性はある。ただし
**`min_silence` を下げる方向は [#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果) で単独では悪化と出ている**
（500 で 28.6%、100 で 30.6%）ので、組み合わせで良くなる見込みは薄い。
**そして上の診断（検出側の失敗）が正しければ、2次元でも届かない。**

### #18. TEN VAD が silero を置き換えた。**取りこぼしは実装で直せる**

> **採点条件** 出典 `_eval/score-ten1.txt`（死亡遊戯1本）・`_eval/score-ten-sweep.txt`（閾値）・
> `_eval/score-ten-healthy.txt`（健全2本）・`_eval/score-ten4.txt`（4本合計）・
> `_eval/score-vadmodels.txt`（5モデル比較）／ いずれも `--exclude-songs` ／ 世代 **pre-#19** ／
> **同じ判断を修正後の指標で9本・4本・一挙2本に広げたのが
> [修正後の9本](#修正後の9本2026-08-22)・[追加4本](#追加4本の-ten-測定2026-08-22決着)・[一挙2本](#一挙2本の-ten-測定2026-08-22決着)で、結論は強まった。数値はそちらから引くこと**

**[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) の診断が正しく、そして解決策になった。**
「落ちている発話は silero の確率floorの下にある＝VADの内側に解は無い」なら、
**別のモデルなら見えるかもしれない。** [TEN VAD](https://github.com/ten-framework/ten-vad)
（TEN Framework、Apache-2.0）を `vad.py` のバックエンドとして足して測った。

レポートは `_eval/score-ten1.txt`（1本目）、`_eval/score-ten-sweep.txt`（閾値）、
`_eval/score-ten-healthy.txt`（健全2本）、`_eval/score-ten4.txt`（4本合計）。
出力は `_eval/hyp/ten-clip`、`ten-t060`、`ten-t075`、`ten-t090`。

#### 比較を成立させるためにやったこと

**TEN VAD はフレーム単位の確率しか返さないので、区間の組み立てはこちら側で書く。**
そこで silero と同じ後処理をそのまま移植した（`vad.segments_from_probabilities()`）。
閾値トリガー、`neg_threshold` による終話候補、`min_silence` での確定、
`min_speech` による除去、そして**ギャップが パディング×2 より狭いときは半分に割る**
という silero のパディング規則まで同じ。**変わるのはモデルだけ**という条件にしてある。
`get_speech_segments_silero()` は silero パッケージを呼ぶ元のままなので、
**記録値は動かない。**

#### 閾値はモデル固有。silero の 0.45 をそのまま使うと損をする

死亡遊戯 #09（歌唱除外、参照2183字）で振った。

| 設定 | CER全文 | coverage | precision | per-block |
|------|--------|----------|-----------|-----------|
| clip（silero 0.45、現在の既定） | 42.4% | 59.7% | 87.3% | 42.7% |
| ten 0.45 | 39.3% | 73.1% | 74.6% | 37.6% |
| ten 0.60 | 35.3% | 71.7% | 79.0% | 37.4% |
| **ten 0.75** | **30.5%** | **73.8%** | 83.2% | 32.2% |
| ten 0.90 | 31.8% | 70.4% | **87.3%** | 32.1% |
| novad | 30.6% | 71.6% | 87.1% | **31.7%** |
| xxl | 35.1% | 69.1% | 78.7% | 41.2% |

**0.45 でも取りこぼしは取り返せている**（カバレッジ 59.7% → 73.1%）が、
精度が 74.6% まで落ちて差引の利得が小さい。**0.75 が最良で、
0.90 なら silero と同じ精度 87.3% を保ったままカバレッジ +10.7pt。**
確率スケールはモデル固有なので、**同じ数値の閾値は同じ厳しさを意味しない。**

#### 4本合計（参照 13,282字、歌唱除外）

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | 30秒超 | thin |
|------|--------|----------|-----------|-----------|--------|--------|------|
| clip（現在の既定） | 27.2% | 74.9% | **89.4%** | 28.1% | 0.84 | **0** | **0** |
| **ten 0.75** | **24.1%** | **83.6%** | 83.9% | **24.6%** | 1.00 | **0** | 1 |
| novad | 25.9% | 81.7% | 83.9% | 26.9% | 0.97 | **0** | 2 |
| xxl | 27.6% | 75.8% | 85.9% | 31.2% | 0.88 | 7件 / 511s | 11 |

**ten 0.75 が CER・カバレッジ・per-block のすべてで最良。**
現在の既定に対し **−3.1pt**、novad に対し −1.8pt、XXL に対し **−3.5pt**
（per-block では **−6.6pt**）。

**ファイル別。3勝1敗で、負けはクレバテス1本。**

| 作品 | clip | **ten 0.75** | novad | XXL |
|------|------|-------------|-------|-----|
| クレバテス #06 | **16.7%** | 21.2% | 23.3% | 18.2% |
| 終末ツーリング #12 | 17.3% | **14.0%** | 15.7% | 21.1% |
| 死亡遊戯 #09 | 42.4% | **30.5%** | 30.6% | 35.1% |
| アイプリ | 40.9% | **33.2%** | 35.8% | 41.5% |

**結論が4つ。**

**(1) 取りこぼしは実装で直せる。** [既知の問題 1-b](HANDOVER.md#1-b-子供向け番組の取りこぼし結論が覆ったvad起因ぶんは直せる) と
[#14](#14-追加素材5本取りこぼしは問題の3本ではなく子供向け番組の性質だった) で「Whisper側の限界」と書いた取りこぼしのうち、
**少なくともVAD起因のぶんは silero の限界だった。** アイプリは
カバレッジ 61.3% → **81.7%**、死亡遊戯は 59.7% → **73.8%**。
「別実装（XXL）でも同じ壁に当たる」という論拠は、
**XXL も silero を使っているから**成立していただけ。

**(2) TEN は novad を全ファイルで上回る。** 同じ recall の利得を、
**門を残したまま**得るので余剰出力が少ない。
[次の着手 C](#不要になったc-死亡遊戯の取りこぼし) の
「素材依存で novad に切り替える」というルールは**不要になる可能性が高い。**

**(3) XXL に負けている素材が死亡遊戯からクレバテスに移った。**
クレバテスは元々 novad でも唯一カバレッジが落ちる異常な1本で、
[#16](#16-歌唱区間を除外したら構図が変わったexclude-songs)のとおり歌唱由来でもない。
**ここは `ten 0.90` を試す価値がある**（死亡遊戯で精度を silero 並みに戻せた設定）。

**(4) 30秒超と thin はほぼ増えない。** ten 0.75 で 30秒超0件・thin 1件。
XXL は 7件511秒・11件。**構造的欠陥での優位は維持している。**

#### 参考：inaSpeechSegmenter と FSMN-VAD も測った（TEN が最良）

**同じ4本・同じ後処理で3モデルを並べた。** レポートは `_eval/score-vadmodels.txt`。

| 設定 | CER全文 | coverage | precision | per-block | 長さ比 | 30秒超 |
|------|--------|----------|-----------|-----------|--------|--------|
| clip（silero、現在の既定） | 27.2% | 74.9% | **89.4%** | 28.1% | 0.84 | **0** |
| **ten 0.75** | **24.1%** | **83.6%** | 83.9% | **24.6%** | 1.00 | **0** |
| fsmn-clip | 25.4% | 79.7% | 85.8% | 26.4% | 0.93 | **0** |
| inass-clip | **50.5%** | **50.1%** | **91.2%** | 51.3% | 0.55 | **0** |
| xxl（本家既定） | 27.6% | 75.8% | 85.9% | 31.2% | 7件 / 511s |

**FSMN-VAD は silero より良く、TEN より悪い。** 中間に入る。
`ten` と同じくカバレッジを買って精度を売る形だが交換比が悪く、
CER で TEN に1.3pt負ける。**ただし silero に対しては1.8pt勝っている**ので、
「silero が floor 下の発話を落としている」という [#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) の
診断が2つのモデルで独立に裏付けられた形になる。

**inaSpeechSegmenter は使えない。素材と前提が合っていない。**
CER 50.5%、カバレッジ 50.1% で既定の半分以下。理由はラベルの内訳が示している。

| 素材 | speech | music | noise | noEnergy |
|------|--------|-------|-------|----------|
| クレバテス #06 | 853s | 557s | 142s | 144s |
| 終末ツーリング #12 | 306s | **910s** | 276s | 173s |
| 死亡遊戯 #09 | 342s | **812s** | 411s | 235s |
| おねがいアイプリ | **145s** | **1152s** | 83s | 100s |

**アイプリは尺1480秒のうち1152秒（78%）が music と判定される。**
INASS は「音声と音楽が交替する放送素材」向けに作られていて、
**アニメのように台詞が常時BGMの上に乗る素材では speech/music 分類が music 側に倒れる。**
台詞ごと捨てるので、狙いだった「歌唱区間の除外」も同時に成立しない。
精度だけは 91.2% で最良（拾ったものは合っている）だが、
**半分しか拾わないので字幕には使えない。**

**この結果自体が有用な否定。** [候補を探した時点](#済みe-ten-vad-の検証を広げる)では
「speech / music / noise を分類できるから問題の形に合う」と考えたが、
**その分類器が想定する音の混ざり方がこの素材と違う**という理由で外れた。

#### 隔離環境と `precomputed` バックアップ経路

**どちらも主環境に入れられない。**

| パッケージ | 障害 |
|-----------|------|
| inaSpeechSegmenter 0.8.0（MIT） | `tensorflow[and-cuda]` と `onnxruntime-gpu` を要求。**Windows では `nvidia-nccl-cu12` が無く解決できない**（`--no-deps` + CPU版TFで回避）。TF は Windows GPU 非対応なので CPU 実行（4本で 16〜27秒/本） |
| funasr-onnx 0.4.2（MIT） | **`numpy<=1.26.4` を要求。** 主環境の numpy 2.x をダウングレードしてしまう |

そこで `_venv_inass` / `_venv_fsmn` に隔離し、`eval/ext_vad_dump.py` が
区間を JSON に書き、`--vad_method precomputed --vad_segments_json` が読む。
**JSONの区間は16msフレーム格子にラスタライズして
`segments_from_probabilities()` を通す**ので、min_silence / speech_pad / min_speech は
どのモデルでも同一。**比較しているのは検出だけで、各プロジェクトの平滑化ではない。**

**ただし TEN vs silero ほど統制は取れていない。** TEN と silero はフレーム確率から
同じ規則で区間を作るが、INASS と FSMN は**それぞれの内部平滑化を通した区間**を
返すので、そこは各自の設計が残る。**FSMN の 25.4% と TEN の 24.1% の差を
モデル性能の差と断定しないこと。**

**FSMN は量子化版で測っている。** ModelScope の ONNX リポジトリは
`model_quant.onnx`（495kB）しか配っておらず、`quantize=False` を指定すると
funasr_onnx が「funasr を入れて自分でエクスポートせよ」と言って止まる。

#### 残っている検証（既定を変える前に必要）

- **残り5本での確認。** 測ったのは4本（狙い2本＋健全2本）だけ。
  公女殿下・LIAR GAME・Summer Pockets・桃源暗鬼・ニンジャラは未測定
- **`ten 0.90` を4本に当てる。** クレバテスの退行が精度側なら戻せるかもしれない
- **一挙（4〜5時間）での挙動。** 長尺は未測定。フレーム単位のPython ループなので
  **VADの実行時間が silero より長い可能性がある**（4本では気にならない範囲）
- **exe 同梱。** `ten-vad` は 1.3MB の wheel に **prebuilt な `ten_vad.dll`** を
  含む形なので、PyInstaller が拾えるか（`binaries` に明示する必要があるか）は未確認。
  ONNX モデルも公開されているので、そちら経由に切り替える選択肢もある
- **ライセンス表記。** Apache-2.0 なので `THIRD-PARTY-NOTICES.md` に追記が必要

**既定は変えていない。** `--vad_method ten` で明示したときだけ使う。
`requirements.txt` にも入れていないので、**既存環境は影響を受けない。**

### #19. 採点指標に欠陥があった。参照キューの表示時間を発話時間として使っていた

> **採点条件** これは採点コード（`eval/arib_vtt.py`）自体の修正。
> しきい値の根拠は `_eval/_cue_length.txt`（15本 23,848キューの保持時間分布）、
> 規模は `_eval/_sparse_blocks.txt` ／ **この節以降が「修正後」世代**

**#1〜#18 を全部参考情報に落とした原因。** `eval/arib_vtt.py` の docstring は最初から
こう警告していた。

> Cue end times are not speech end times. Because of the contiguous chain an end
> time only means "this is when the next caption replaced it", so it must not be
> used to score timestamp accuracy.

**そしてその警告を一段上の `text_regions()` が踏んでいた。** `(c.start, c.end)` で
「字幕が出ていた区間」を作り、`score.py` がそこから無字幕域とCERブロックの両方を
導いていたので、**次の字幕が来るまで画面に残るキューが主題歌の無音を丸ごと
「字幕あり」に塗りつぶしていた。**

犯人は2件で特定できた（`_eval/_cue_length.txt`）。

```
106.6s  25字 ( 4.27 秒/字)  1:17.3 クレバテス  「やっぱりこのハイデンは今とんでもないことになってる。」
105.0s   8字 (13.12 秒/字)  0:41.0 公女殿下   「フェリシア！？大丈夫！？」
108.6s   2字 (54.32 秒/字) 56:03.3 ハクメイ   「あ～あ…。」
```

これが **118.3秒に参照70字**、**109.0秒に参照11字**というブロックを作り、
`min_gap = 20s` の無字幕域除外をすり抜けて採点対象になっていた。
その中で主題歌が流れるので、**歌を書き起こす設定だけが、参照が何も言っていない
時間の出力を挿入誤りとして課金されていた。**

#### 規模

20秒以上かつ参照1.0字/秒未満のブロック（`_eval/_sparse_blocks.txt`）。

| | 修正前 | 修正後 |
|---|--------|--------|
| 該当時間 | 1232秒（採点ブロック時間の10.5%） | **379秒（3.5%）** |
| そこにある参照 | 728字（2.1%） | 252字（0.7%） |
| 課金される文字（clip） | 728 | **100** |
| 課金される文字（ten 0.75） | 1869 | **268** |
| 課金される文字（xxl） | 1142 | **201** |

**TEN の超過は 1141字 → 168字で、85%が消えた。** これが9本の退行のほぼ全量だった。
無字幕域の総時間は 22:37.5 → **37:44.8** に増えた（保持されていた15分が正しく回った）。

#### 直し方

キューに `speech_end` を足し、表示時間を「その字幕の裏にある発話が続き得る長さ」で
上限した。**しきい値は推測ではなく実測から**（15本 23,848 キュー）。

| 統計 | 値 |
|------|-----|
| 保持時間の中央値 | 0.24 秒/字 |
| p90 | 0.71 秒/字 |
| p95 | **1.17 秒/字** |
| 20秒以上保持されるキュー | 88件（0.37%） |

`SPEECH_SECONDS_PER_CHAR = 1.2`（p95。**95%のキューは無変更**）、
`MIN = 2.0`、`MAX = 20.0`（`min_gap` と同値。この長さの保持は本来無字幕域として
報告されるべきもの）。**`speech_end` は必ず短くする方向にしか働かない。**

比率だけでは足りない。25字のキューは 1.2 秒/字でも 30 秒買えてしまい、
クレバテスの106.6秒を止められない。逆に絶対値だけだと長い台詞を切る。両方必要。

#### 副作用の確認

- **参照文字数はほぼ不変**（35,036 → 35,025、11字）。キャプション本文は失っていない
- 残った低密度ブロックは**本物**。死亡遊戯 19:47 の詩の朗読（0.98字/秒）、
  20:25 の泣き声、公女殿下の24秒。これらは実在する低密度発話で、除外すべきではない
- ブロック数は微増（無音が開いて分割される。Summer Pockets 49 → 54）

**旧値の再現性より正しさを採ってフラグにせず既定挙動として入れた。**
そのぶん #1〜#18 が参考情報に落ちている。

### #20. 定型句フィルタは 0.6pt 効くが、見送った

> **採点条件** 出典 `_eval/score-fixed9.txt` ／ 24分もの9本・`--exclude-songs` ／
> 世代 **修正後（現行）** ／ 全件のテキストは `_eval/_filler_dropped.txt`、
> リスク再測定は `_eval/_filler_risk.txt`、音量測定は `_eval/_filler_energy.txt`

**Whisper が非音声区間に吐く定型句を落とすだけ。** レポートは
`_eval/_filler_dropped.txt`（全件のテキスト）と `_eval/score-fixed9.txt`。

| 設定 | フィルタ前 | フィルタ後 | 差 |
|------|-----------|-----------|-----|
| clip（現在の既定） | 19.3% | **19.0%** | −0.3pt |
| ten 0.75 | 16.1% | **15.5%** | **−0.6pt** |

**誤爆ゼロを全件目視で確認した。** 検出は 9本で clip 15件 / ten 43件 / xxl 11件、
**すべてが「ご視聴ありがとうございました」「チャンネル登録をお願いいたします」で、
台詞は1件も引っかかっていない。** 固定リストなので原理的に誤爆しない。

`loop_filter.py` が見逃す理由も明確で、**繰り返しでも30秒超でもない。**
[既知の問題 1](HANDOVER.md#1-ハルシネーションループは解決残るのはループ形をしない余剰出力) の
「(1) ループ形をしない余剰出力」の一部がこれで塞がる。

分かりやすい例。**クレバテス** 0:11:00.0-0:11:11.6 のブロックは**参照が「ウオー！」**
（叫び声のみ）で、clip は空文字、ten は「ご視聴ありがとうございました」を出している。
**TEN が叫び声を発話として拾い、Whisper が非音声に定型句を吐いた形。**

#### 「無音区間のカットオフ」では捕まらない（否定的結果）

テキストではなく音のレベルで切る案を測った（`_eval/_filler_energy.txt`、
16kHz モノラルWAVから区間ごとの RMS）。**成立しない。**

| | p5 | 中央値 | p75 | 端 |
|---|-----|-------|-----|-----|
| 定型句が出た区間（43件） | −41.2 | **−28.7 dBFS** | −27.1 | 最大 **−19.6** |
| それ以外（3,615件） | −34.0 | **−28.2 dBFS** | −26.7 | 最小 −66.3 |

**通常の発話と同じレベルにいる。** 平均 −30.5 対 −28.7 dBFS。動作点が無い。

| ゲート | 定型句の捕獲 | 巻き込む他の区間 |
|-------|------------|----------------|
| −40 dBFS | 3 / 43 | 39 |
| −35 dBFS | 9 / 43 | 136 |
| −30 dBFS | 18 / 43 | **915**（全体の25%） |

**理由は上のクレバテス 11:00 が示している。最大値 −19.6 dBFS はその叫び声の区間。**
これは無音への幻覚ではなく**「音のある非音声」への幻覚**で、
叫び声・効果音・音楽の立ち上がりに TEN が正しくエネルギーを見つけ、
Whisper が最も事前確率の高い句を出している。だからエネルギーでは触れない。

#### ジャンル依存でリスクが上がる（定型句側を弱くする論点）

**定型句リストのリスクは均一ではなく、その句が実際に喋られるジャンルで最大になる。**
バラエティは番組の締めに本当に「ご視聴ありがとうございました」と言い、
ドラマの配信者役は「チャンネル登録」と言う。**最も発火する場所で最も誤爆する。**
アニメ15本で0件だったのは、そのジャンルに無かっただけで汎化の根拠にならない。

**Amatsukaze の利用者は映画やドラマも録る**ので、ここは実運用で効く。
しかも実写は silero が落としていた側の性質（静かな台詞が密なミックスの下）なので
TEN の出力量が増える方向で、**リスクと露出が同時に上がる。**

#### 唯一手つかずの信号：no_speech_prob

テキストでもエネルギーでもない信号が1つ残っている。**faster-whisper が
セグメントごとに出す `no_speech_prob`**（これは音声でない確率）で、
今回失敗した word probability（トークンの確信度）とは別の量。

**現在は JSON に保存していないので未検証。** 頭打ちでない理由がある。
既定の `no_speech_threshold=0.6` は **`avg_logprob < −1.0` との論理積**で
初めて捨てるので、「非音声確率は高いが尤度は普通」という今回のケースは生き残る。
**論理積を外して `no_speech_prob` 単独で見る余地が残っている。**

やるならまず出力に1フィールド足して分布を測るところから。
それだけなら誤爆リスクはゼロで、動作点が見つからなければそこで打ち止めが確定する。

#### 「誤爆ゼロ」は言い過ぎだった。リスクを測り直した

上の「誤爆ゼロ」は**このコーパスで観測されなかった**という意味で、
**原理的に誤爆しないという意味ではない。** 40設定・93,646区間と参照18本で
測り直した（`_eval/_filler_risk.txt` / `_eval/_filler_in_ref.txt`）。

**(1) 危険な項目は1件もヒットしていなかった。**

| ヒット | 項目 |
|-------|------|
| **635** | ご視聴ありがとうございました |
| 7 | チャンネル登録をお願いいたします |
| 2 | ご視聴ありがとうございます |
| 1 | 最後までご視聴 |
| **0** | **チャンネル登録**（単体） |
| 0 | 高評価とチャンネル登録 / ご覧いただきありがとうございました |

**単体の `チャンネル登録` は効果ゼロでリスクだけ。** 配信者キャラが
「チャンネル登録してね」と言えば誤爆する項目が1件も仕事をしていない。

**(2) 部分一致は645件中3件（0.5%）で、3件とも幻覚だった。**
区間全体の完全一致に限っても捕獲率 99.5% を維持できるので、
「残りの台詞まで消える」危険は構造的に排除できる。

**(3) 参照側には7語すべて0件。** 近い言い方まで広げると
「ありがとうございました」は15件あるが（「今日はありがとうございましたじゃあまたあしたな」等）、
**「ご視聴」「視聴」「チャンネル」「高評価」は1件もない。**

#### 確信度ゲートは失敗した（否定的結果）

「非音声への幻覚なら Whisper の確信度が低いはず」と考えて `words[].probability` の
平均を測ったら、**逆だった。**

| | n | p5 | 中央値 | p75 |
|---|---|---|---|---|
| 定型句にマッチした区間 | 645 | **0.720** | 0.789 | 0.824 |
| コーパス全体 | 93,644 | **0.582** | 0.928 | 0.973 |

**定型句の p5 がコーパスの p5 より高い。** 閾値 0.7 で切ると定型句は 645件中20件しか
落ちず、実台詞 8,590件が同じ線の下に入る。**確信度は使えない。**

理由は納得できる。「ご視聴ありがとうございました」は学習データにある高頻度句なので、
**Whisper は無音に対して自信を持って吐く。** それがこの幻覚クラスの正体。
`no_speech_prob` / `avg_logprob` は JSON に保持していないので未検証だが、
同じ向きの信号なので期待は薄い。

#### 安全側に寄せた設計（実装するなら）

| 変更 | 効果 | コスト |
|------|------|--------|
| 部分一致 → 区間全体の完全一致 | 残りの台詞が消える危険を排除 | 捕獲 645 → 642 |
| 0ヒットの3項目を削除 | 一番危険な項目が消える | ゼロ |
| `--filler_filter false` で切替 + 全件ログ | 誤爆しても発見・回避できる | ゼロ |

さらに保守的にするなら `ご視聴ありがとうございました` 1語だけでも
635/645（98.4%）を捕獲する。

**それでも「作中の放送や配信を締める台詞が完全一致する」リスクは残る**ので、
**0.6pt との釣り合いが悪いと判断して[実装は見送った](#g-定型句フィルタは見送り判断記録)。**
リストは `_eval/_strip_filler.py` の `FILLER`。

### #21. INASS を歌検出に転用するのは危険（否定的結果）

> **採点条件** 採点レポートではなく既存の `_eval/vad-inass.json`（4本の speech 区間）と
> `clip-prodloop` の出力を突き合わせた集計 ／ 世代 **修正後（現行）** ／
> 参照側のマーカー調査は `_eval/_drcs_survey.txt`

[#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) で inaSpeechSegmenter は VAD として却下したが、
**却下理由（アイプリの78%を music と判定する）は歌検出器が欲しい性質そのもの**なので、
music ラベルを歌唱帯マーカーに転用する案が出た。**測って却下した。**

既存の `_eval/vad-inass.json`（speech区間のみ、4本）から逆算した。確定台詞は
`clip-prodloop` がブロック内に出した出力（precision 91.1%）。

| 素材 | 確定台詞 | INASS speech 内 | music マスクで危険な文字 |
|------|---------|----------------|------------------------|
| クレバテス #06 | 773s | 630s（81.5%） | 586字（13.6%） |
| 死亡遊戯 #09 | 323s | 200s（61.9%） | 459字（30.7%） |
| 終末ツーリング #12 | 602s | 261s（43.4%） | 1476字（50.1%） |
| おねがいアイプリ | 463s | 113s（24.4%） | **1791字（74.8%）** |
| **合計** | **2161s** | **1204s（55.7%）** | **4312字（38.7%）** |

**INASS は確定台詞の44%を speech と認識していない。** 危険域に入るのは明白な台詞。

```
終末ツーリング 05:13.0  頂上からは富士山北アルプス諏訪湖までパノラマ絶景が楽しめる
アイプリ      04:16.8  2人ともクラスメイトで一緒に相振りもしてるのおじいちゃんは何をお願いしてたの
クレバテス     03:08.1  もちろんハイデンも応戦しているが、王と首都機能を失った現状ではどうしたって止められない
```

**「アニメは台詞が常時BGMに乗る」という却下理由が、マスク用途でも同じように効く。**
数字は上限（speech以外は music / noise / noEnergy の合計）だが、
アイプリは非speech時間1335秒のうち1152秒（86%）が music なので実質タイト。

**そして [#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) の修正で歌検出そのものが不要になった**ので、
この線は追わなくてよい。`eval/ext_vad_dump.py` に music 区間を出す口を足す
（`run_inass()` は既に `by_label` を集計しているので数行）案も保留。

#### 参照側の音符マーカーについて（調査済み）

ARIB は音楽を DRCS ビットマップの音符で示していて、`_eval/_drcs_survey.txt` が
既に時間ごと抽出している。**ただし運用が番組でばらばらで、必要な場所を覆っていない。**

| 素材 | music-only キュー | 合計時間 |
|------|------------------|---------|
| 終末ツーリング #12 | 9 | 330.7s |
| 死亡遊戯 #09 | 11 | 136.3s |
| ニンジャラ（ガムッチ） | 20 | 53.6s |
| 桃源暗鬼 | 21 | 43.0s |
| **クレバテス #06** | **1** | **6.7s**（11:11。OPではない） |
| **公女殿下 #10** | **0** | **0s** |
| **LIAR GAME #10** | **0** | **0s** |

**退行していた3本のうち2本にはマーカーが1つもない。** だから
`--exclude-songs`（マーカー起点）はこれらの番組では何も落とせなかった。
検出器の検証用の正解としては使えるが、運用判断には使えない。

### #22. anime-whisper を現行パイプラインで再検証した（#4 の結論は維持）

> **採点条件** 出典 `_eval/score-aw-recheck.txt` ／ ニンジャラ「ガムッチ」1本・
> `--exclude-songs`（この素材は落ちる参照0字）／ 世代 **修正後（現行）** ／ VAD は ten 0.75
>
> **原因はベースの蒸留構造だと確定した（[#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03)）。**
> 同じニンジャラで **kotoba-v2 が 43.3%**（anime-whisper 50.7% / large-v3 26.7%）。
> **ベースだけでも large-v3 に 16.6pt 負ける**ので、結論の維持は正しく、
> 理由は「ファインチューンが悪い」ではなく「2層デコーダでは足りない」。

**[#4](#4-anime-whisper-は-large-v3-に負けたモデルカードと逆) は
`collect` 経路・silero・ループ抑制なし・[欠陥のある指標](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた)で測っていた**ので、
条件を今に揃えて1本だけ測り直した。レポートは `_eval/score-aw-recheck.txt`、
推論は `_eval/hyp/aw-ten/`。

**素材はニンジャラ「ガムッチ、カッペイッチ!？」1本**（`202603280730010102`）。
**anime-whisper に最も有利な条件を選んだ。** 子供向けアニメの演技セリフ＝
モデルカードの学習分布ど真ん中で、かつ**コーパス中で最もループが出るファイル**
（large-v3 で1091字＝9本合計の80%）。#4 の敗因がループなら、抑制が入った今
ここで最も救われるはず、という読み。参照は歌詞を印字しないので
`--exclude-songs` が比較を歪めない（落ちる参照0字）。

```powershell
python eval/run.py --config ten-clip -m anime-whisper --tag aw-ten --only 202603280730010102
python eval/score.py --hyp _eval\hyp\ten-t075 --hyp _eval\hyp\aw-ten `
                     --hyp _eval\hyp\anime-whisper --only 202603280730010102 `
                     --exclude-songs --report _eval\score-aw-recheck.txt
```

| 設定 | 全文CER | coverage | precision | per-block | セグメント | 長さ比 | ループ |
|------|--------|----------|-----------|-----------|-----------|--------|--------|
| **large-v3 / TEN（現行既定）** | **27.4%** | **78.9%** | **82.8%** | **29.8%** | 357 | 0.95 | 1件6字 |
| anime-whisper / 現行経路 | 50.7% | 59.8% | 66.4% | 62.6% | 143 | 0.90 | 2件53字 |
| anime-whisper / #4 の条件 | 52.4% | 52.0% | 74.5% | 63.9% | 72 | 0.70 | 2件150字 |

**結論が4つ。**

**(1) 結論は維持。23.3pt 差で負け。** #4 の「9本全敗」を覆す材料は出なかった。
**最も有利な素材を選んでこの差**なので、非推奨の根拠はむしろ強まった。

**(2) ループ抑制は救いにならない。仮説は否定された。**
`strip_loops.py --detector production` を旧出力に当てると、
**9本で2セグメント427字（1.4%）しか落ちず、ニンジャラでは0件**。
**#4 の「ループ文字1491字」は `score.py` の緩い検出器で数えた値**で、
製品版の3条件（12字以上で文字種2以下／同一文字8連続／1〜6字の単位が4回かつ12字以上）に
かからない形の繰り返しだった。現行経路でも aw-ten のループは2件53字で
large-v3 と同水準なので、**そもそもループが敗因ではない。**

**(3) 実際の故障は「もっともらしい誤認識」。** `paired diff` に並ぶ。

```
我が一族 → 罠一族     令和 → 霊話     過去の栄光 → 過去の英語
```

**(4) パイプラインの改善は効いたが差引でほぼ消える。** 52.4% → 50.7%（−1.7pt）。
TEN VAD でカバレッジ 52.0% → **59.8%**、長さ比 0.70 → **0.90** と大きく改善したのに、
**精度が 74.5% → 66.4% に落ちて相殺される。** 拾う量が増えたぶん外れも増える形で、
[#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) の
「カバレッジを買って精度を売る」交換が、このモデルでは割に合っていない。

**副産物2つ。**

- **速度は本物の利点。** 推論 43.5秒（large-v3 は同素材で80〜100秒）。2層デコーダのぶん
- **[今後やるなら 4](HANDOVER.md#4-anime-whisper-の扱い決定済み記録)の「整形ありで再測定する価値」も潰れた。**
  整形が効くのは per-block だけで、**セグメント粒度に依存しない全文CERで既に 23.3pt 負けている。**
  整形では埋まらない

**セグメントの粗さは改善したが残っている。** 72 → 143 で、large-v3 の 357 に対し2.5倍粗い。
per-block 62.6% 対 29.8%（32.8pt）で、**字幕としての体感差はこちらに近い。**

### 歌唱・OP/ED の扱い：対象外だが拾う

**方針として「OP/ED と歌唱シーンは対象外。ただし拾うことがある」と明示する。**
根拠が3つある。

**(1) 本家も拾う。** 同じ窓で数えた（`_eval/_song_pickup.txt`、総文字数 / 採点内）。

| 設定 | 歌唱窓での出力 |
|------|--------------|
| clip（silero） | 54字 / 23字 |
| ten 0.75 | 2274字 / 1160字 |
| xxl | 630字 / 281字 |

**silero がほぼ拾わないのは性能ではなく検出漏れ。** XXL は630字拾っていて、
公女殿下では89字が採点に入る。Whisper 系の共通挙動で、whisp-carrier 固有の欠陥ではない。

**(2) 落としたほうが悪くなる。** [修正後の測定](#修正後の9本2026-08-22)で
歌唱窓を落とすと 15.5% → 16.1% に悪化し、coverage も 86.6% → 85.8% 下がる。

**(3) 参照の運用が番組でばらばら。** アイプリは歌詞を印刷する（参照の22.9%）が、
公女殿下・LIAR GAME・クレバテスは印刷もマーカーも無い。
**同じ挙動が番組によって加点にも減点にもなる**ので、製品の目標にはできない。

**残る実務上の注意は無字幕域の出力量。** TEN にすると 1775字 → 3686字（2.1倍）で、
提供クレジット・番宣・主題歌にキューが付きやすくなる。CERには出ないが利用者には見える。
[定型句フィルタ](#20-定型句フィルタは-06pt-効くが見送った)なら一部を削れるが（3816 → 3686字）、
**見送りを決めたので現状はこの量が出る。**

### 指標の設計欠陥（記録）

当初の一次指標「無字幕域に出たセグメント数」は**この素材では使えない。**
無字幕域は無音ではなく、提供クレジット・番宣・次番組・ニュースが入っている。

```
クレバテス  ATXをご覧の皆さんテレビアニメ追放者食堂へようこそを…
桃源暗鬼    この番組はご覧のスポンサーの提供でお送りしました
桃源暗鬼    クマに4人が襲われ38歳の男性が死亡しました
死亡遊戯    落語は伝統芸能である前に大衆演芸受けてなんぼじゃん
```

**正しく書き起こしたものを幻覚として数えていた。**

**一挙で救えると思っていたが、救えなかった。** 採点可能な無字幕域47分43秒の
68%が一挙だったのでそう考えたが、実際に測ると clip 5820字 / collect 5735字で
ほぼ同一、中身は次のとおりで、話間ギャップはOP・ED・次回予告で埋まっていた。

```
心地よい風吹いたら外でご飯食べよう   ← ED主題歌。3:06:53 / 4:27:53 / 4:53:53 に毎話出る
この物語は漫画家を目指す一人の少女とその旅を綴った記録である   ← ナレーション
私が王宮に戻らなければ王朝と帝国の全面戦争が勃発します   ← 次番組
```

**無字幕域指標はこの素材群では最後まで使えない。**

代わりに参照不要の指標（ループ検出・30秒超・長いのに文字が少ない）を
`score.py` に足した。全域に適用でき、この交絡を受けない。
上の測定結果はすべてこちらから出ている。無字幕域の数字はレポートに
残してあるが、**解釈しないこと**（`score.py` の docstring にも書いてある）。

### #23. 音声フィルターを全部測った（`--ff_lowhighpass` だけが効く・2026-08-25）

> **採点条件** 出典 `_eval/score-ff-pilot.txt`（7条件×1本）/
> `_eval/score-ff-lowhighpass.txt`（10本）/ `_eval/score-ff-thrgrid.txt` ・
> `_eval/score-ff-fullgrid.txt`（3閾値×4条件）/ `_eval/score-ff-amp-matched.txt`（発話量マッチ）/
> `_eval/score-ff-amp3.txt`（増幅系3本）/ `_eval/score-ff-fftdn.txt` / `_eval/score-ff-rnndn.txt`。
> いずれも `--exclude-songs` ／ TEN VAD ／ 明記した閾値以外は既定 0.75 ／ `large-v3`。

**それまで `--ff_*` の精度への影響は1件も測っていなかった。**
「実験的」と書いていたのは**効果が分かっていないという意味**で、
「掛けると悪くなると測った」わけではなかった（[設計判断の節](HANDOVER.md#音声フィルター--実験的)を参照）。
**そこを埋めた。結論は「1つだけ効き、残りは全部害。運用点は動かない」。**

#### 方法（3つの工夫）

1. **対照を同一セッションで取り直した。** `eval/run.py --tag ff-none` を
   フィルター無しで回し、**記録値 `ten-t075` と全指標が完全一致**することを確認してから
   比較に入っている（30.5% / 260セグメント / coverage 73.8% / precision 83.3%）。
   ハーネスが決定的であることの確認でもある。
2. **VAD の交絡を先に潰した。** フィルターは振幅と帯域を変えるので、
   閾値を固定したままでは「認識が良くなった」と「VAD が拾う量が変わった」が混ざる。
   **VAD は推論の前段でモデルに依存しないので、`-m tiny` で回せば `[VAD]` 行は本番と同一。**
   7条件×3閾値を tiny で先に回して、どのフィルターが VAD をどれだけ動かすかを地図にした
   （`none@0.75` の 1066.0s が large-v3 と一致することで検証済み）。
3. **交絡が大きい条件は「発話量を揃えた閾値」で測り直した。**
   `loudnorm` は 0.78、`fftdn` は 0.82 で、無フィルター 0.75 の 1066.0 秒に
   ±3 秒まで一致する。ここで比べれば**Whisper に入る音の量が同じ**になる。

#### 発話秒の無フィルター比（死亡遊戯 #09、tiny）

| フィルター | thr 0.60 | thr 0.75 | thr 0.90 |
|---|---|---|---|
| なし（基準） | 1226.2s | 1066.0s | 849.4s |
| **`lowhighpass`** | **+0.4%** | **−0.2%** | **+0.4%** |
| `loudnorm` | +4.3% | +3.6% | +2.4% |
| `loudnorm` + `lowhighpass` | +4.8% | +3.4% | +3.3% |
| `fftdn 12` | +2.0% | +3.9% | **+11.0%** |
| `speechnorm` | +3.8% | +2.6% | +4.4% |
| `gate` | −2.9% | **−6.8%** | −5.6% |
| `rnndn_sh`（0.75 のみ） | — | **−21.5%** | — |

**`lowhighpass` だけが VAD 中立。** だからこのフィルターの効果は
「閾値を実質的に動かした副作用」ではないと切り分けられる。

#### 全文CER（死亡遊戯 #09、3閾値 × 5条件）

| 閾値 | なし | `lowhighpass` | `loudnorm` | `speechnorm` | `fftdn 12` |
|---|---|---|---|---|---|
| 0.60 | 35.3% | **33.8%** | 36.2% | 33.3% | 37.2% |
| **0.75（既定）** | 30.5% | **28.8%** ← 全条件の最小 | 33.3% | 34.4% | 34.2% |
| 0.90 | 31.8% | 31.6% | 32.0% | 32.0% | 33.9% |

**17条件を測って、既定の閾値を変える理由になる組み合わせは1つも出なかった。**
最小値は「既定 0.75 + `lowhighpass`」。

#### `--ff_lowhighpass` を10本に広げた（唯一の改善）

| 素材 | 基準 | 帯域通過 | 差 |
|---|---|---|---|
| 公女殿下 #10 | 8.7% | 9.3% | **+0.6** |
| LIAR GAME #10 | 9.7% | 9.7% | ±0 |
| 桃源暗鬼 | 11.9% | 11.6% | −0.3 |
| Summer Pockets #26 | 13.8% | 13.9% | +0.1 |
| 終末ツーリング #12 | 14.0% | 13.4% | −0.6 |
| ニンジャラ（花火） | 16.3% | 16.5% | +0.2 |
| クレバテス #06 | 16.4% | 16.3% | −0.1 |
| アイプリ（はじめて） | 23.7% | 23.3% | −0.4 |
| ニンジャラ（ガムッチ） | 27.4% | 26.3% | **−1.1** |
| 死亡遊戯 #09 | 30.5% | 28.8% | **−1.7** |
| **合計** | **16.1%** | **15.9%** | **−0.2** |

**6勝1分3敗。** coverage 86.7 → 86.9%、precision 89.9 → 90.0%、30秒超は両方0件、
ループ 8件72字 → 9件71字。

**規則性がある。難しい素材ほど効き、いちばん綺麗な素材では悪化する。**
CER 23%超の3本が −0.4 / −1.1 / −1.7、10%未満の会話劇は ±0〜+0.6。
**閾値の側から見ると同じことで、緩い閾値（0.60/0.75）で効き 0.90 で消える**
（−1.5 / −1.7 / −0.2）。難素材4本の 0.90 では合計 23.5% → 23.6% で**むしろ悪化**。
**「限界的な区間を拾いに行っているときに、その区間の音を整えると効く」**で両方説明できる。

#### 増幅系（`loudnorm` / `speechnorm`）

| 3本合計（死亡遊戯・公女殿下・ガムッチ） | 全文CER | coverage | precision | ループ |
|---|---|---|---|---|
| なし | **19.6%** | 84.2% | **88.4%** | 2件/14字 |
| `loudnorm` | 20.1%（+0.5） | 84.2% | 87.4% | **13件/88字** |
| `speechnorm` | 20.4%（+0.8） | 84.1% | 87.9% | 5件/42字 |
| `lowhighpass` | **19.1%（−0.5）** | **85.1%** | 88.1% | 3件/27字 |

**発話量を揃えても悪化は残る**（死亡遊戯、`loudnorm`）。

| 条件 | 発話秒 | 全文CER | precision | ループ |
|---|---|---|---|---|
| なし @0.75 | 1066.0 | **30.5%** | 83.3% | **0** |
| `loudnorm` @0.75 | 1104.7（+3.6%） | 33.3% | 78.9% | 5件/32字 |
| **`loudnorm` @0.78（マッチ）** | **1064.4（−0.2%）** | **32.7%** | 81.6% | 2件/18字 |

**2.8pt のうち戻るのは 0.6pt だけで、+2.2pt は増幅そのものの害。**
落ちるのは precision（出した文字が参照と合わない）で、実際に出るのは
「ああああああ」「はっはっは」「チッチッチッ」— **息・笑い・環境音が発話レベルまで
持ち上がって喋りとして書かれている。** CER の符号は閾値で揺れる
（`speechnorm` は 0.60 で −2.0pt）が、**ループが増える向きは全条件で一貫**していた。
**この実装は「30秒超0件・ループ0件」を売りにしているので、CER の +0.5pt より重い副作用。**

#### `--ff_fftdn 12`（無条件に悪い）

| 条件 | 全文CER | coverage | ループ |
|---|---|---|---|
| なし @0.75 | **30.5%** | **73.8%** | 0 |
| `fftdn` @0.75 | 34.2%（+3.7） | 71.1% | 3 |
| **`fftdn` @0.82（発話量マッチ）** | **34.3%（+3.8）** | 70.9% | 0 |
| `fftdn` @0.60 / @0.90 | 37.2% / 33.9%（対照 35.3% / 31.8%） | −1〜3pt | 0 |

**発話量を揃えても変わらない＝交絡ゼロで +3.8pt 悪化。**
ループは増えないのに coverage が落ちるので、**幻覚ではなく認識の破壊**
（広帯域のスペクトル減算が子音を削る）。

#### `--ff_rnndn_sh`（モデルを取ってきて測った。同梱しない）

**それまで「モデル未同梱で exit 1」としか書けていなかった。**
[GregorR/rnnoise-models](https://github.com/GregorR/rnnoise-models) の `sh.rnnn` を
取得して測った（297,646 バイト、SHA-256 `70BB6685EB0C2A1D18E2918DCA3FBFBD39317010B1802EB1B6EA73A92F3FDEC0`、
上流が「tools/ と README 以外は創作性が無く著作権の対象にならない」と明示。
出所は `_tools/rnnoise/PROVENANCE.txt`）。**同梱は不要で、そのことを測定で言える。**

**`arnndn` は 48kHz しか受け付けないので順序が効く。** 現行チェーンは
先に 16kHz モノラルへ落とすので（順序バグ 2-a の修正でそう固定した）、
**ffmpeg が 16k→48k にアップサンプルしてからモデルに食わせる**。
そこで2通り測った。2本合計（死亡遊戯・公女殿下）。

| 条件 | 全文CER | coverage | precision | 文字数比 |
|---|---|---|---|---|
| なし | **15.4%** | 87.1% | 91.4% | 0.95 |
| `lowhighpass` | 15.3% | 87.4% | 91.0% | 0.96 |
| **`rnndn_sh`（現行チェーン）** | **28.8%（+13.4）** | **72.9%（−14.2）** | 90.6% | **0.80** |
| **`rnndn_sh`（48kHz の元音源に先に掛ける）** | **24.2%（+8.8）** | 78.4%（−8.7） | 90.1% | 0.87 |

**順序を直すと 4.6pt 戻るが、それでも +8.8pt 悪い。**
precision は 90% 台のまま coverage だけ崩れる＝**出したものは正しいが喋りごと大量に消している。**
RNNoise の設計目的（通話・室内騒音）とアニメ音声（BGM・効果音）が合っていない。
**チェーンの順序を組み替える価値も無い**（組み替えても負ける）。

#### 副産物3つ

- **[既知の問題 2](HANDOVER.md#2-音声フィルターが実用レベルに達していない) の未解決項目
  「`--ff_lowhighpass` で後半が消える」が再現しなかった。**
  **実素材10本（24〜30分）を通して段の検査も全部通り、後半消失は1本も起きていない。**
  順序バグ（2-a）の修正で消えていたと考えるのが自然。**再現しない現象として閉じてよい。**
- **`gate` は mean を動かさないのに VAD の発話を −6.8% 削り、区間を細切れにする**
  （74 → 83、0.90 では 90）。波形の一部を実際に削っている。
- **`loudnorm` の後に `lowhighpass` を掛けると peak が 0.0 dB に達する**
  （`[FF] lowhighpass: peak=0.0 dB`）。**併用はクリップ寸前**で、段の計測を見ないと気付けない。

#### 再現手順

```powershell
# 1条件を足すだけ。--extra はそのまま whisp-carrier に渡る
python eval/run.py --config ten-clip --tag ff-lowhighpass --extra=--ff_lowhighpass
python eval/score.py --hyp _eval/hyp/ten-t075 --hyp _eval/hyp/ff-lowhighpass `
                     --exclude-songs --report _eval/score-ff-lowhighpass.txt

# VAD だけ先に見るなら tiny で足りる（[VAD] 行は本番と同一）
python eval/run.py --config ten-clip --tag probe -m tiny --hyp-root _tmp_ffvad `
                   --extra=--ff_loudnorm --extra=--vad_threshold --extra=0.78

# rnndn を測るなら sh.rnnn をリポジトリ直下に置く（audio_filter は裸の名前を渡す）
# 48kHz 先の版はこの形。PowerShell から日本語パスを渡すと化けるので Python か cmd で
ffmpeg -i <元mkv> -map 0:a:0 -af arnndn=m=sh.rnnn -ar 16000 -ac 1 -c:a pcm_s16le out.wav
```

### #24. ボーカル抽出は3つの不具合で「動いていなかった」（順序を直して測った・2026-08-29）

> **採点条件** 出典 `_eval/score-ff-vocal.txt`（**10本×3条件**）。
> `--exclude-songs` ／ TEN VAD ／ 閾値 0.75（既定）／ `large-v3` ／ `ten-clip`。
> **素材は [#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) の
> `--ff_lowhighpass` を広げたときと同じ10本**なので、あの表と直接並べられる。
> **推論は `_eval/hyp/ff48-none`（対照）と `_eval/hyp/ff48-roformer` に残してある。**

**[#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) で6系統を測ったとき、
`--ff_vocal_extract` だけは「exe で使えない」を理由に測らずに残した。そこを埋めた。**
**測る前に不具合が3つ出てきて、このオプションは一度も正しく動いたことが無かった。**
機構と修正内容は[既知の問題 2-d](HANDOVER.md#2-d-ボーカル抽出は3つの不具合で動いていなかった修正済み2026-08-29)。
ここには測定を置く。

| | 不具合 | 実測した症状 |
|---|-------|------------|
| 1 | 分離が 16kHz モノラル化の**後**に走っていた（[2-a](HANDOVER.md#2-a-チャンネル選択が無音を作っていた原因の記録) と同じ型） | 無音にならないので段の検査を通る。44.1kHz ステレオ前提のモデルに 8kHz 以下・モノラルを渡していた |
| 2 | `mdx_kim2` の segment_size を秒から derive していた | **毎回失敗。** `RuntimeError: The size of tensor a (160) must match the size of tensor b (161)` が onnx2torch の中で出て、`IndexError` に化けていた |
| 3 | ステム選択が**伴奏側**を掴んでいた | 死亡遊戯 #09 で発話 **27.1s / 1800s**、`lang=en (49.37%)`、**7セグメント**（正常時 260） |

#### 方法（測る前に足場を2つ作った）

**1. 原音レートの素材セットが必要になった。** `eval/prep.py` は 16kHz モノラルを書くので、
**そのまま `eval/run.py` に渡すと、順序を直しても分離モデルは 16kHz モノラルを見る。**
`_eval/wav48/`（10本、48kHz ステレオ s16、約2.9GB）を `extract_track_native()` で作り、
`--wav-dir` で差し替えた。

**2. 同一セッションの対照を取り直した。** 順序を直すと **48kHz s16 の量子化が1回余分に入る**
（原音 → native WAV → 分離 → 16kHz モノラル）。`_eval/wav48/` を 16kHz モノラルに
落とした `_eval/wav48to16/` を `ff48-none` として回し、記録値と突き合わせた。

| 10本合計 | `ten-t075`（記録値） | `ff48-none`（対照） |
|---|---|---|
| 全文CER | **16.1%** | **16.1%** |
| coverage | 86.7% | 86.7% |
| precision | 89.9% | 89.9% |
| per-block plain | 17.2% | 17.2% |
| ループ | 8件 72字 | 8件 73字 |

**合計はすべて一致した。** ファイル単位では ±0.3pt 以内に収まる
（最大が公女殿下の 8.7% → 8.4%）。**死亡遊戯では VAD まで完全一致**
（74区間 / 1066.0秒 / 260セグメント。
[#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) の `none@0.75` と同じ値）。
**余分な量子化はこの配管のノイズ床であって、結果を動かさない。**
以下の比較はすべて `ff48-none` に対して取っている。

> **`_eval/wav` とはビット一致しない。** 10本すべてで音声データの MD5 が違う
> （フォーマットは同じ 16000/1/2）。ffmpeg が float → s16 でディザを掛けるので、
> 48kHz で1回挟むと LSB が動く。**「同じ設定で作った WAV だから同じ」ではない。**

#### 結果（`mb-roformer`、10本）

| 素材 | 対照 | roformer | 差 |
|---|---|---|---|
| 公女殿下 #10 | 8.4% | 9.7% | **+1.3** |
| LIAR GAME #10 | 9.8% | 10.3% | +0.5 |
| 桃源暗鬼 | 12.0% | 12.0% | ±0 |
| Summer Pockets #26 | 13.9% | 13.9% | ±0 |
| 終末ツーリング #12 | 13.9% | 15.4% | **+1.5** |
| ニンジャラ（花火） | 16.3% | **16.1%** | −0.2 |
| クレバテス #06 | 16.5% | **15.6%** | **−0.9** |
| アイプリ（はじめて） | 23.8% | 23.9% | +0.1 |
| ニンジャラ（ガムッチ） | 27.4% | **26.6%** | **−0.8** |
| 死亡遊戯 #09 | 30.6% | 34.4% | **+3.8** |
| **10本合計** | **16.1%** | **16.5%** | **+0.4** |

**3勝2分5敗、合計 +0.4pt。** coverage 86.7% → 86.6%、precision 89.9% → 89.8%、
per-block plain 17.2% → 17.3%。

**[`--ff_lowhighpass`](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) と同じ表で並べると、
規則性が違う。** あちらは「難素材ほど効き、いちばん綺麗な素材で悪化」で単調だったが、
**こちらは両端で悪化して中央で勝つ。** 悪化の1位が最難の死亡遊戯（+3.8）、
2位が最良の公女殿下（+1.3）。

**勝った3本は BGM と効果音が密な素材**（クレバテス＝アクション、ニンジャラ2本＝子供向けアクション）で、
**負けた素材は静かな会話劇**（公女殿下・LIAR GAME・終末ツーリング）**と低発話率の死亡遊戯。**
**分離すべき音楽がある素材では効き、無い素材ではアーティファクトだけが残る**という読みは
機構と整合するが、**同じく BGM が密なアイプリと桃源暗鬼が ±0 なので規則としては弱い。**
**10本で合計が負けている以上、素材で振り分ける根拠には足りない。**

#### 機構：区間が繋がって伸びる

**10本すべてで「VAD の区間数が減り、発話秒が増える」。例外は1本も無い。**
BGM が抜けると台詞の切れ目で確率が閾値を割らなくなり、区間が融合する。

| 素材 | 対照（区間 / 発話秒） | roformer | 発話秒の差 |
|---|---|---|---|
| ニンジャラ（花火） | 30 / 1246.7s | **18** / 1354.3s | **+8.6%** |
| ニンジャラ（ガムッチ） | 51 / 1170.3s | **45** / 1264.9s | **+8.1%** |
| 終末ツーリング #12 | 51 / 1251.8s | **33** / 1351.0s | **+7.9%** |
| 死亡遊戯 #09 | 74 / 1066.0s | **72** / 1135.7s | +6.5% |
| アイプリ（はじめて） | 50 / 1290.1s | **30** / 1361.6s | +5.5% |
| クレバテス #06 | 39 / 1465.4s | **28** / 1512.8s | +3.2% |
| 桃源暗鬼 | 28 / 1334.7s | **27** / 1372.4s | +2.8% |
| Summer Pockets #26 | 54 / 1262.1s | **53** / 1295.5s | +2.6% |
| LIAR GAME #10 | 23 / 1421.1s | **19** / 1438.1s | +1.2% |
| 公女殿下 #10 | 36 / 1303.7s | **32** / 1318.5s | +1.1% |

**区間数の減り方がいちばん大きいのはアイプリ（50 → 30）と終末ツーリング（51 → 33）。**
**ただし発話秒の増加量と CER の差には対応が無い**（増加が最大の花火は勝ち、
3位の終末ツーリングは +1.5pt で最大の負け）。**VAD の変化だけでは結果を説明できない。**

**発話が増えているのに coverage が落ちる素材がある**（死亡遊戯 73.7% → 69.9%）ので、
そこは**取りこぼしではなく認識側の損失。** [#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) の
`fftdn` と同じ形（precision も一緒に落ちる）。分離の残留アーティファクトが
子音を削っていると読むのが自然だが、**そこは検証していない。**
**逆にアイプリでは coverage が 81.6% → 83.4% に伸びて precision が下がり**、
CER は ±0.1 に落ち着いた。**素材によって効く向きが違う。**

**代わりに歌は拾えるようになる。** 無字幕域の出力が10本合計で
314セグメント / 4239字 → **403セグメント / 5056字（+19%）。**
中身は OP/ED と挿入歌の歌詞（`月が描き出したシルエット` `お江戸お江戸さすらいのひと`、
クレバテスでは英語詞まで）。**ボーカルを分離するのだから当然の方向**で、
他の `--ff_*` には無かった挙動。
**ただし[歌唱は対象外](#歌唱oped-の扱い対象外だが拾う)なので、これは字幕としての価値にならない。**

**構造指標では得と損が1つずつ。**

| 指標（10本合計） | 対照 | roformer |
|---|---|---|
| ループ | 8件 73字 | **5件 35字** |
| thin | 2件 | 2件 |
| 30秒超 | 0件 | 0件 |
| 最長セグメント | 26秒 | **30秒** |

**ループは減る**（花火で3件→0件、公女殿下で1件→0件）。BGM が抜けて
反復に落ちにくくなったと読める。**代わりに最長が 30秒に触れた**
（死亡遊戯 `0:22:05.3-0:22:19.6` に14.3秒の `……………`、桃源暗鬼にも1件）。
**30秒超0件はこの実装の売り**なので、上限に接触するのは好ましくない。

#### 結論

**既定は変えない。`--ff_vocal_extract` は「使わない」で確定。**
[#23](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) の結論
（`--ff_lowhighpass` だけが効く）も動かない。
**ただし「未測定」ではなくなった**ので、README と HANDOVER の書き方を
「効果不明」から「測って悪い」に直した。

**素材で振り分ける道も閉じた。** 3本で測った時点では「ニンジャラだけ −0.8pt 勝った」が
残っていたが、**10本にすると勝ちが3本に増えた代わりに合計は負けたまま**で、
勝敗を分ける規則は BGM の密度で完全には説明できない（アイプリと桃源暗鬼が反例）。
**[`--ff_lowhighpass`](#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25) に与えた
「難素材向けの明示オプションとして案内する」扱いも、こちらには与えられない** —
あちらは難易度で単調だったが、こちらは最難の素材でいちばん悪化する。
**案内する条件を書けない。**

**`mdx_kim2` は不具合を直して動くようにしたが、精度は測っていない。**
onnxruntime が CPU に落ちるので 60秒の素材で約30秒かかり、
`mb-roformer`（24分もの1本 約65秒）と桁が違う。
**`mb-roformer` が負けている以上、遅くて古い側を測る価値は低い。**

#### 再現手順

**素材セットを作る `_mkwav48.py`。** リポジトリ直下に置いて実行する。
**PowerShell から日本語パスを渡すと化けるので、ファイル操作は Python 側で完結させる**
（[この環境の注意](HANDOVER.md#実行時の出力は英語ストリームは-utf-8)と同じ理由）。

```python
# _mkwav48.py — 原音レートの素材セットと、その16kモノ版（対照入力）を作る。
# 分離を原音で走らせるために必要。prep.py の 16k モノでは分離モデルが原音を見られない。
import pathlib
import sys

sys.path.insert(0, '.')
import audio_filter as af

stems = {p.stem for p in pathlib.Path('_eval/hyp/ten-t075').glob('*.json')}

native = pathlib.Path('_eval/wav48')
native.mkdir(parents=True, exist_ok=True)
for mkv in pathlib.Path('sample').rglob('*.mkv'):
    if mkv.stem in stems:
        af.extract_track_native(str(mkv), str(native / f'{mkv.stem}.wav'))

control = pathlib.Path('_eval/wav48to16')
control.mkdir(parents=True, exist_ok=True)
for wav in sorted(native.glob('*.wav')):
    af.extract_audio(str(wav), str(control / wav.name))
```

```powershell
python _mkwav48.py

python eval/run.py --config ten-clip --wav-dir _eval/wav48to16 --tag ff48-none
python eval/run.py --config ten-clip --wav-dir _eval/wav48     --tag ff48-roformer `
                   --extra=--ff_vocal_extract --extra=mb-roformer

python eval/score.py --hyp _eval/hyp/ten-t075 --hyp _eval/hyp/ff48-none `
                     --hyp _eval/hyp/ff48-roformer `
                     --exclude-songs --report _eval/score-ff-vocal.txt
```

> **この測定中に踏んだ環境の罠。** 長時間ジョブを子プロセスで走らせるとき、
> **コンソールを持たせないと audio-separator が引き込む Intel Fortran ランタイムが
> `forrtl: error (200): program aborting due to window-CLOSE event` で落ちる。**
> ターミナルを閉じても同じことが起きるので、`CREATE_NEW_CONSOLE` で
> 独立したコンソールを与えること。**症状は「10本すべて exit 2」の形で出る。**

### #25. ニワトリ・ファイターは言語自動判定の失敗だった（`-l ja` で解決・2026-09-02）

> **採点条件** **CER は出せない**（この素材に参照が無い。MKV に入っているのは
> whisper の SRT トラック1本だけ）。判定は参照不要指標と、下で定義する
> **キュー密度**、`lang=`、セグメント数。仮説側は MKV の字幕トラックから抽出した
> `0.9.1` の実物と、`0.9.2` exe での再実行2本 ／ 世代 **修正後（現行）**

[P](STATUS.md) の解析。**結論は3つで、うち2つは HANDOVER の想定を否定している。**

#### (1) 0.9.2 で完全に再現する

同じ音声（MKV から抽出した16kHzモノラルWAV）を現行 exe に通した。

| | 報告時（0.9.1・Amatsukaze 経由） | 0.9.2（`--no_config`） |
|---|---|---|
| `lang=` | `ko (36.45%)` | **`ko (36.45%)`** |
| セグメント数 | 115 | **115** |
| `[LOOP]` | 4件・474字 | **4件・474字** |
| `[VAD] ten` | 44 regions / 1179.1s | **44 regions / 1179.1s** |
| `[STT] Done in` | **410.3s** | **89.7s** |

**桁まで一致する。** 決定論的に同じ出力なので、[N](#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正) の修正は無関係。

#### (2) 410.3秒は素材のせいではない（HANDOVER の手順4を否定）

**同じ音声が単体では 89.7s で終わる。** 実時間比 6.2% で、
[コーパスの 4.3〜6.6%](#5-cer-24-の読み方合計値に意味は薄い) の範囲内。
**温度フォールバックの繰り返しではない。**

**3本すべてが遅かったことが決定的。** `sample/errdata/` にはログが3本あり、
HANDOVER は第三羽だけを引用していたが、残り2本も同様に遅い。

| ログ | regs | speech | segs | 秒/seg | `[STT]` | 実時間比 | `lang=` |
|------|------|--------|------|--------|---------|---------|---------|
| 141300.178（第一羽） | 70 | 983.2s | 245 | 4.0 | 314.8s | 21.9% | ja 99.22% |
| 141324.735（第二羽） | 56 | 1124.2s | 293 | 3.8 | 489.3s | 34.0% | ja 99.22% |
| 141340.252（第三羽） | 44 | 1179.1s | 115 | 10.3 | 410.3s | 28.5% | **ko 36.45%** |
| コーパス10本 | 23〜74 | — | 262〜412 | 2.1〜2.6 | 69.9〜97.8s | **4.3〜6.6%** | ja 84〜99.9% |

**言語判定が正常だった2本も 3.5〜5倍遅い。** つまり遅さと言語判定は独立で、
**共通要因は Amatsukaze が NVEnc のエンコードを同時に走らせていること**（GPU の競合）。
**単体実行では消える。** 精度の問題ではないので追わない。

#### (3) 本当の欠陥は「不可能なキュー密度」で、既存の指標は全部すり抜ける

**出力の 38% が物理的にありえない。** 日本語の発話は毎秒5〜8字なので、
`plain` 折り畳み後の**字数÷キュー秒数**を見る。

| | 第三羽 0.9.1 | コーパス10本 |
|---|---|---|
| 中央値 | 8.2 字/秒 | **4.5〜5.6** |
| p90 | **65.7** | **6.6〜7.4** |
| >15字/秒 かつ 20字以上 | **42件（37.8%）** | 合計2件（0〜0.3%/本） |
| キュー合計長 | **208.8s（尺の14.5%）** | — |

**うち35件がちょうど 1.40 秒。** 分布ではなく固定値で、
**中身は正しい日本語の台詞**（`164.9s 1.40s 107字`
「何だ今には植えてるわよずいぶん物騒なもん持ってるじゃねえか…」）。
**認識は合っていて、タイムスタンプだけが潰れている。**

**`score.py` はこれを検出できない。** `thin` は
**逆の形**（20秒以上で1字/秒未満＝長くて空）を見るので、
**短くて詰まりすぎているキューには検出器が1つも無い。**
実際 第三羽 は `>30s` 0件・`thin` 0件・ループ抑制後の残存0件で、
**既存の参照不要指標では「健全」に見える。**

#### `-l ja` で直る（HANDOVER の手順2が当たり）

| | A: 自動判定 | B: `-l ja` |
|---|---|---|
| `lang=` | ko (36.45%) | **ja (100.00%)** |
| セグメント数 | 115 | **315** |
| キュー合計長 | 208.8s（14.5%） | **771.8s（53.6%）** |
| 不可能キュー | 42件（37.8%） | **2件（0.6%）** |
| 中央値 / p90 密度 | 8.2 / 65.7 | **4.7 / 7.1** |
| ハングル | 342字（10.3%） | **0字（0.0%）** |
| 所要 | 89.7s | 95.9s |

**すべての指標がコーパスの正常域に入る。** セグメント数 315 は
[24分ものの 262〜412](#5-cer-24-の読み方合計値に意味は薄い) の範囲内。

**[#13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い) に追記が必要。**
あそこは「`--language ja` は10本すべてで出力を1セグメントも変えなかった」と記録している。
**それは今も正しいが、変える素材が存在することが分かった。**
**判定が成功している素材では無影響、失敗している素材では決定的。**

#### 機構と、入れた緩和

**Whisper は冒頭から言語を判定するので、OP の主題歌や提供クレジットが
ファイル全体の言語を決めてしまう。** この素材の OP は英語混じりの歌で、
出力にも英語の歌詞らしきもの（`latin` 7.7%）が並んでいる。

**入れたのはログ1行だけ**（`whisp_carrier.LANGUAGE_CONFIDENCE_WARN = 0.70`）。
**自動判定でかつ確信度がこれ未満のとき `[STT] warning:` を出す。**
再試行も上書きもしない — 多言語のファイルは正当なので。
**閾値の根拠は分離の広さ**で、正しく判定できた13本が 84.1〜99.9%、
失敗した1本が 36.5%。**間はどこで切っても同じ。**

**根本原因（タイムスタンプの潰れ）は直していない。** 言語判定が外れたときに
何が起きているかまでは追えていない。**着手するなら、
上の「不可能なキュー密度」を `score.py` の参照不要指標に足すのが先**で、
それが無いと直ったかどうかを機械で確認できない。

### #26. 定型句フィルタを実装した（#20 の見送りを撤回・2026-09-02）

> **採点条件** **推論なし。** [#20](#20-定型句フィルタは-06pt-効くが見送った) の上限測定に使った
> `_eval/hyp/ten-f1` / `clip-f1` と、実装を `ten-t075` / `clip-prodloop` に当てた結果を
> **セグメント単位で突き合わせた** ／ 世代 **修正後（現行）** ／
> 実装は `filler_filter.py`

**新規 `filler_filter.py`。既定で有効（`--filler_filter false` で切れる）。**

#### 上限測定の数値がそのまま実装の数値になった

**19本すべてでセグメント集合が完全一致した。**

| 対照 | 結果 |
|------|------|
| `ten-t075` + 実装 対 `ten-f1`（10本） | **10本 identical / 0 differing** |
| `clip-prodloop` + 実装 対 `clip-f1`（9本） | **9本 identical / 0 differing** |

**つまり [#20](#20-定型句フィルタは-06pt-効くが見送った) の
16.1% → 15.5%（ten・−0.6pt）と 19.3% → 19.0%（clip・−0.3pt）は、
再採点せずにこの実装の数値として確定する。**

検出数も記録と一致。**`clip-prodloop` 15件 / `ten-t075` 43件 / `xxl` 11件。**

#### #20 の「安全側に寄せた設計」を全部採った

| 変更 | 実装 |
|------|------|
| 部分一致 → 区間全体の完全一致 | 折り畳み後の長さが句の長さ+2字以内。**部分一致は落とさず `partial` としてログに出す** |
| 0ヒットの3項目を削除 | `チャンネル登録`（単体）/ `高評価とチャンネル登録` / `ご覧いただきありがとうございました` |
| `--filler_filter false` + 全件ログ | **検出は常時。** オフでも `[FILLER]` 行に `keep` として全件出る |

**`最後までご視聴` も削った（#20 の表より1項目多い）。** 唯一のヒットが部分一致
（「最後までご視聴してくださって嬉しかったら…」）なので**区間全体の一致では不活性**、
そして残るのは文の断片で、**実台詞を巻き込む形として一番危険。** 捕獲の損失はゼロ。

#### 誤爆の再確認（参照15本・11,967キュー）

**0ヒット。** そのうえで**境界の近さ**を測った（リストに入っていない断片）。

| 実参照でのヒット | 断片 |
|---|---|
| 18 | ありがとうございます |
| 15 | ありがとうございました |
| 3 | 登録 |
| **0** | ご視聴 / 視聴 / チャンネル / 高評価 |

**だからリストは句の全長を保つ。** どれか1つでも上の断片へ短くすると台詞を消し始める。
単体テストにこの3件（「今日はありがとうございましたじゃあまたあしたな」
「手伝ってくれてありがとうございました」「納品登録を」）を保持側として入れてある。

#### 韓国語の同型を足した（実運用の出力が根拠）

**[#25](#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02) で見つかった。**
言語判定が韓国語に外れると、**同じ定型句クラスが韓国語で出る。**
第三羽1本で **8件**、全部が区間全体の一致。

```
  [FILLER] 8 detected, 8 dropped
           00:00:28   2.9s drop exact: ns=0.95 다음 영상에서 만나요.
           00:02:56   0.2s drop exact: ns=0.98 영상편집 및 자막이 도움이 되셨다면 구독, 좋아요, 댓글 부탁드립니다.
           00:11:12   0.1s drop exact: ns=0.93 시청해주셔서 감사합니다.
```

**日本語の台詞と衝突しない**（文字種が重ならない）。
`FILLER_KO` として別グループにしてあるので、言語が増えても平坦なリストにならない。

#### `no_speech_prob` を測れるようにした（3つめの否定的結果）

**#20 が「唯一手つかず」と書いていた信号。** セグメントに
`no_speech_prob` と `avg_logprob` を保存し、`[FILLER]` 行に `ns=` として出す。
**判定には使わない。** 測ったら使えないことが分かったため。

| | n | 分布 |
|---|---|---|
| 定型句8件 | 8 | **min 0.74 / 中央値 0.91 / max 0.98** |
| 残ったセグメント | 103 | min 0.030 / 中央値 0.614 / **p75 0.879** |

**向きは合っている**（定型句は全部 0.74 以上）**が、分離しない。**

| ゲート | 定型句の捕獲 | 巻き込む実セグメント |
|-------|------------|-------------------|
| 0.74 | **8 / 8** | **40件（出力の38.8%）** |
| 0.90 | 6 / 8 | 11件（10.7%） |

**単独では動作点が無い。** 確信度（#20）・音量（#20）に続いて3つめの否定的結果で、
**固定リストが残る理由がこれで揃った。**

> **この測定の限界。** 1本・定型句8件で、しかも**言語判定が失敗した壊れた出力**の上で
> 測っている（残った103件には上記の不可能キュー42件が含まれる）。
> **健全な素材での分布は未測定。** 現行の既定はこのフィールドを常に保存するので、
> **回すたびに溜まる。**

#### 副産物：`--help` が exit 1 で死んでいた（公開中の 0.9.2 でも）

**`--vad_method` の help に未エスケープの `%` が4つあった**
（`19.3% -> 16.1%` など）。argparse は help を必ず `% params` に通すので、
**`whisp-carrier.exe --help` が生のトレースバックと
`[PYI-*:ERROR] Failed to execute script` を出して exit 1 になる。**

**公開中の 0.9.2 exe で再現を確認した**（rc=1・stdout 0バイト）。
**[リリース前チェックリスト](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)の
14項目が `--version` は見るが `--help` を見ていない**ので通り抜けた。
**`--help` は利用者が最初に叩くものなので、項目を1つ足すこと**
（[運用ルール](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)の
「不具合を1件直したら、それを検出する項目を1つ足す」に該当）。

修正後、**85アクション全部を `% params` に通して 0 bad、`--help` は rc=0・16,188バイト。**

### #27. モデル軸を4候補で閉じた（large-v3 が全勝・2026-09-03）

> **採点条件** 出典 `_eval/score-models5.txt` ／ **4本**（公女殿下 #10・死亡遊戯 #09・
> ニンジャラ ガムッチ・アイプリ）・参照 14,260字・`--exclude-songs` ／
> 世代 **修正後（現行）** ／ **VAD は全モデル共通で TEN 0.75**
> （`--config ten-clip`。Qwen は同じ区間を JSON 経由で食わせた）／
> 対照の `ten-f1` は[定型句フィルタ込みの現行既定](#26-定型句フィルタを実装した20-の見送りを撤回2026-09-02)

**large-v3 が4本すべてで1位。** VAD を共通にしてあるので認識器同士の比較。

| モデル | デコーダ層 | 全文CER | coverage | precision | 長さ比 | ループ | 実時間比 |
|--------|----------|--------|----------|-----------|--------|-------|---------|
| **whisp-carrier large-v3** | **32** | **19.7%** | **83.6%** | **88.6%** | 0.94 | 3件23字 | 4.5〜6.6% |
| `zh-plus/faster-whisper-large-v2-japanese-5k-steps` | **32** | 24.4% | 80.3% | 84.9% | 0.95 | **11件117字** | 7〜10% |
| `Qwen/Qwen3-ASR-1.7B-hf` | （Qwen3 LLM） | 25.5% | 80.5% | 84.2% | 0.96 | 7件292字 | **1.5〜3.6%** |
| Faster-Whisper-XXL r245.4 | 32 | 26.6% | 77.2% | 86.8% | 0.89 | 3件23字 | 12% |
| **`kotoba-tech/kotoba-whisper-v2.0-faster`** | **2** | **37.9%** | 67.3% | 79.6% | **0.84** | 2件21字 | 2.2% |

素材別。

| 素材 | large-v3 | jp-large-v2 | Qwen | XXL | kotoba-v2 |
|------|---------|-------------|------|-----|-----------|
| 公女殿下 #10 | **8.0%** | 9.7% | 9.7% | 10.8% | 31.3% |
| 死亡遊戯 #09 | **28.8%** | 34.0% | 31.8% | 35.1% | 35.4% |
| ニンジャラ | **26.7%** | 35.8% | 33.6% | 30.3% | 43.3% |
| アイプリ | **22.9%** | 26.8% | 35.5% | 39.9% | 42.9% |

#### 蒸留デコーダは閾値として効く（#4 の原因が切り分けられた）

**[#4](#4-anime-whisper-は-large-v3-に負けたモデルカードと逆) と
[#22](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持) は
「anime-whisper が負ける」までで、原因を特定できていなかった。**
**ベースモデルの kotoba-v2 を測ったら同じ壊れ方をした**ので、
**負けの原因はファインチューンではなくベースの蒸留構造。**

| | 全文CER | 長さ比 | coverage |
|---|--------|--------|----------|
| anime-whisper（9本・[#4](#4-anime-whisper-は-large-v3-に負けたモデルカードと逆)） | 41.8% | 0.80 | — |
| **kotoba-v2（4本・本項）** | **37.9%** | **0.84** | 67.3% |
| large-v3（同4本） | 19.7% | 0.94 | 83.6% |

**どちらも「出力が足りない」型で一致する。** kotoba-v2 は最良の素材（公女殿下）でも
31.3%・長さ比 0.87 で、**large-v3 の 8.0% に 23.3pt 負ける。**

**そして層数は単調に効く軸ではない。** 32層同士（large-v3 19.7% 対 jp-large-v2 24.4%）は
**同じ層数で 4.7pt 差**なので、そこは学習内容の差。
**2層が壊れるという閾値**であって「多いほど良い」ではない。

**Whisper ファミリーは large-v1/v2/v3 すべて 32層が最大なので、この軸に上は無い。**
上を求めると CTranslate2 の外（Qwen3-Omni-30B クラス）になり、
配布 exe・単一コンシューマGPU・実時間比の想定から外れる。**モデル軸は閉じたと判断する。**

#### 日本語特化もドメイン特化も勝てない

**`zh-plus/...` はフルデコーダの日本語ファインチューンで、素材も日本語アニメ。**
それでも 4.7pt 負ける。しかも**きれいな素材では並ぶのに難素材で崩れる**という形。

| 素材 | large-v3 | jp-large-v2 | 差 |
|------|---------|-------------|-----|
| 公女殿下（会話劇） | 8.0% | **9.7%** | +1.7 |
| アイプリ | 22.9% | 26.8% | +3.9 |
| 死亡遊戯 | 28.8% | 34.0% | +5.2 |
| **ニンジャラ**（叫び・早口） | 26.7% | **35.8%** | **+9.1** |

**公女殿下では `len ratio 1.00` / coverage 92.5% でほぼ互角。**
崩れる方向も分かっていて、**ループが 11件117字**（large-v3 は3件23字）。
`お願いお願いお願い淡かかに` のような出力が出る。
**ファインチューンで安定性を落としている**ので、
[`condition_on_previous_text=False` を選んだ判断](#15-condition_on_previous_texttrue-は死亡遊戯を破壊した仮説の否定)と同じ
「精度より安定性」の論点に当たる。

#### Qwen3-ASR は別アーキテクチャでも同水準（速度だけが本物）

**タイムスタンプを出さないので VAD 区間を単位に食わせた**（区間は `_eval/vad-ten.json`、
30秒窓を超える区間は29秒で無重複分割。faster-whisper も `clip_timestamps` で
窓ごとに切るので、この分割は片側だけの不利ではない）。**単語タイムスタンプは無い**ので
per-block CER は読まないこと（47.1〜54.9% と出るが、
[anime-whisper で 42.2% → 62.6% と20pt開いた](HANDOVER.md#指標)のと同じ粒度の問題）。

**素の greedy・フィルタ0個で 25.5%。** 後処理（`loop_filter` / `filler_filter` は
下流がバックエンド非依存なので流用可能）を足せば縮むが、**縮むのは precision 側**で、
負けの主成分は coverage 3.1pt + precision 4.4pt の**両方**。
**アイプリの 12.6pt 差は `len ratio 1.00` の状態で precision 75.4%** なので、
削れば coverage が落ちる。**後処理で 1〜2pt、5.8pt は埋まらない**という読み。

**速度は本物。** 実時間比 **1.5〜3.6%** で large-v3 の 4.5〜6.6% より速く、
重みは 4.08GB fp16。**弱いGPU向けの選択肢としてのみ意味が残る**
（[`--batched` を追わない理由](HANDOVER.md#1---batched-は追わない判断記録2026-08-22)の
「速度が制約になっていない」が成立しない環境なら、
「精度 5.8pt 落として2倍速」の取引が成立しうる）。

**歌に強い兆候が1つ。** 差分に OP の歌詞が出る（`0:01:58.4 触れてる毎日だから何千回何万回…`）。
**歌唱除外で採点しているので数字には乗っていない。**

#### Qwen3-Omni-30B-A3B は RTX 5090 に載らなかった（実測・2026-09-03）

**「層数の上を見るなら CTranslate2 の外」の先を実際に試した。** 結論は
**32GB のコンシューマGPUでは 4bit でも動かない。** 予測ではなく OOM の実測。

**まず取得量。** `Qwen/Qwen3-Omni-30B-A3B-Instruct` は
**bf16 で 67,256 MB（15 shards）。**

| | サイズ | 量子化 | ライセンス |
|---|-------|--------|-----------|
| `Intel/...-int4-AutoRound` | **25,050 MB** | auto-round 4bit g128 sym | Apache-2.0 |
| `cyankiwi/...-AWQ-4bit` | **26,275 MB** | compressed-tensors pack-quantized | `other` |

**4bit でも 25GB あるのは Talker が同梱されているため**（`config.json` に
`talker_config` と `code2wav_config` が残る）。**書き起こしに使わない音声生成側を払っている。**

**そして shard は均等分割なので、Thinker だけの部分ダウンロードはできない。**

**ロードは両方とも同じ壁で落ちた。**

| 試行 | 結果 |
|------|------|
| Intel / `dtype=float16` | **OOM。30.26 GB 確保して 768 MiB 不足** |
| Intel / `dtype='auto'` | **OOM。30.37 GB** |
| Intel / dtype 指定なし | **OOM。30.37 GB** |
| cyankiwi / `dtype='auto'` | **OOM。30.64 GB** |
| cyankiwi / dtype 指定なし | **OOM。30.64 GB** |

**`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` も効かない**
（未使用の予約は 28〜420 MiB しかなく、断片化ではなく絶対量が足りない）。

**原因は逆量子化。** 25GB の int4 を bf16 に展開すると約60GB で、
32GB のカードでは「30GB 積んだところで力尽きる」— 観測と一致する。
**つまり 4bit カーネルが適用されていない。**
**両形式で同じ結果なので、原因は量子化形式ではなく
transformers 5.16.1 + Qwen3-Omni の組み合わせ側。**

#### 途中で踏んだ罠（環境側）

- **`auto-round` が torch の pin を壊す。** `pip install auto-round` が
  **torch 2.8.0+cu128 をアンインストールして torch 2.14.0 を入れる**ため、
  torchaudio が不整合になり、sm_120 の CUDA ビルドも失われる。
  **隔離 venv だったので主環境は無傷**（`_venv_inass` / `_venv_fsmn` と同じ理由で
  隔離した判断が効いた）。cu128 の pin を `--force-reinstall` で戻せば
  **auto-round・compressed-tensors と torch 2.8.0+cu128 は共存できる**
- **音声しか使わないのに Pillow と torchvision が必要。**
  Omni の `AutoProcessor` はマルチモーダルなので画像プロセッサを必ず構築し、
  無いと `ValueError: Could not load any image processor class` で
  **Processor 自体が作れない。** `torchvision` は cu128 の index から
  torch と版を合わせて入れること（既定 index だと torch がまた入れ替わる）
- **ログの粒度で誤診した。** 最初は「12分ロードが進まない」と読んだが、
  実際は `AutoProcessor` が5秒で例外を投げていた。プロセスの RSS が 13 MB で
  GPU 使用率 1% だったのが手掛かり。**段階ごとにログを吐くようにして5秒で判明した**

#### 残る手段と、それぞれの投資

| 手段 | 結果 |
|------|------|
| **vLLM** | **試して閉じた。** 下記 |
| CPU オフロード | `device_map='auto'` なら載るが速度が桁で落ちる。**エンコーダ同時実行の環境では論外**（未実施） |
| 48GB 以上のGPU | 動作要件そのものの変更。README の「NVIDIA RTX GPU」から外れる |

#### vLLM は Windows で「入るが動かない」（実測）

compressed-tensors の本来のランタイムなので 4bit カーネルが効く見込みがあり、
**駄目で元々で試した。結果は「黙って壊れたものが入る」型だった。**

| 段 | 結果 |
|----|------|
| `pip download vllm --no-deps` | **Windows 用 wheel が無い。** `vllm-0.28.0.tar.gz`（40MB のソース配布）が来る |
| `pip install vllm` | **rc=0、258秒で「Successfully installed」。** wheel も生成される |
| 生成された wheel | **`vllm-0.28.0-cp311-cp311-win_amd64.whl` が 9,106,098 バイト** |
| `import vllm` | **`ModuleNotFoundError: No module named 'vllm._C_stable_libtorch'`** |

**9.1 MB という大きさが答え。** vLLM のネイティブ拡張（`_C` / `_C_stable_libtorch`）は
Windows ではビルド対象から外れ、**Python のソースだけの空の wheel が出来る。**
CUDA カーネルのコンパイルで時間がかかるという予測も外れで、**そもそも
コンパイルされていない**（だから4分で終わる）。

**「pip install は成功し、import が即死する」** —
[`ctranslate2.converters` を excludes に入れた件](HANDOVER.md#運用上の注意)や
[同梱 ffmpeg が使われていなかった件](#j-同梱-ffmpeg-が使われていなかった2026-08-23)、
[罠2](HANDOVER.md#罠2-蒸留モデルの-alignment_heads-はデコーダ層数と合っていない)と同じ、
**このプロジェクトが繰り返し踏んできた「ビルドは通って実行時に全滅」の形。**

**これで Omni の手段は尽きた。** transformers では OOM、vLLM は Windows で動かない。

**#27 の結論は変わらない。** むしろ**実測で補強された** —
「層数の上を見るには CTranslate2 の外」と書いたその外側が、
**このプロジェクトの想定環境では物理的に届かない**ことが数字で出た。

#### 再検討する条件

- **弱いGPU / CPU専用環境からの要望**が来て、実時間比が問題になった場合（→ Qwen）
- **Qwen3-ASR の日本語が更新された**場合。1.7B が large-v3 に負けているので、
  **0.6B を試す意味は今は無い**
- ~~`Qwen3-ForcedAligner-0.6B` 単独~~ → **追わない。** 検討した根拠が3つとも崩れた。
  **(1)** [`--realign` の代替](HANDOVER.md#3---realign-が不安定)としては、
  あれが**推奨設定では必ずスキップされる**ので使われていない機能の置き換えになる。
  **(2)** タイムスタンプを出せないモデル（kotoba-v2 / Qwen3-ASR）の救済は、
  **そのモデルが精度で負けている**ので循環。
  **(3)** [#25](#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02) の修復も、
  **原因が言語判定なので対象が無い**（`-l ja` で 42件 → 2件、言語が正しい素材での
  基本発生率は 3,658区間で2件）。**そして transformers が要るので exe に載らない。**
  検出だけなら下記の `dense` で足りる

#### `dense` 検出器を入れた（参照不要指標が4つになった・2026-09-03）

> **採点条件** 推論なし。既存の `_eval/hyp/*` と `_eval/niwatori-review/*.json` に
> 新しい検出器を当てただけ ／ 実装は `eval/score.py`

**[#25](#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02) が
「既存指標では健全に見える」と書いた穴を塞いだ。**
`thin` は**長くて空**（20秒以上で1字/秒未満）を見るが、
**短くて詰まりすぎている**形を見る検出器が無かった。

**`dense` = 15字/秒超 かつ 20字以上。** 閾値の根拠はコーパス実測で、
中央値 4.5〜5.6・p90 6.6〜7.4 字/秒なので、**15 は最速の正常キューの約2倍。**
字数の下限は、丸めで短くなった一語キューを除くため（20字なら 1.3秒未満が必要）。

**手計算の結果を再現した。**

| 対象 | dense | 率 |
|------|-------|-----|
| 第三羽 自動判定（ko 36.45%） | **39 / 103** | **37.9%** |
| 第三羽 `-l ja`（ja 100%） | **2 / 311** | 0.6% |
| コーパス ten-t075 10本 | **2 / 3,658** | 0.05% |

（アドホック計測は 42/111 = 37.8% だった。差は**定型句フィルタが韓国語の定型句8件を
落としている**ぶんで、率は一致している。）

**副産物。他モデルを同じ指標で監査できるようになった。**
**下の表は #27 と同じ4本**（`_eval/score-models5.txt` の TOTAL）。

| モデル | dense | 文字数 | ピーク密度 | 参考: >30s / thin |
|--------|-------|--------|-----------|------------------|
| **ten-f1（現行既定）** | **1 / 1,356** | 25 | 28 ch/s | 0 / 0 |
| Qwen3-ASR-1.7B | **0 / 302** | 0 | 6 ch/s | 0 / 0 |
| jp-large-v2 | 1 / 1,144 | 195 | 75 ch/s | 0 / 0 |
| **XXL r245.4** | **8 / 1,025** | 209 | 179 ch/s | **7 (361s) / 10** |
| **kotoba-v2** | **7 / 1,349** | 180 | **500 ch/s** | 0 / 0 |

**XXL の構造的な弱さの3つ目。** 同じ4本で `>30s 7件（361秒）` / `thin 10件` に
`dense 8件` が加わる。**セグメント数は最少（1,025）なのに異常は最多。**

> **最初にここへ書いた数値はスコープが混ざっていた**（ten-f1 と xxl は手元にある
> 全ファイル、他3つは4本だけを数えていた）。**上の表は4本で揃えたもの。**
> なお `ten-t075` を**コーパス10本**で見ると **dense 2 / 3,658区間**で、
> どちらの見方でも発生率は 0.05〜0.07% と変わらない。

**そして #27 で読めなかったものが読めるようになった。**
Qwen の per-block CER 47〜54% は **`dense` が0件**なので、
**タイムスタンプの崩壊ではなく純粋に粒度の問題**だと切り分けられる。
（区間の時刻をそのまま使うので密度は崩れず、代わりにセグメントが粗い。）

**`--realign` 相当の修復は入れない。** 上記のとおり対象が無い。

#### 探査して候補外にしたもの

| 候補 | 形式 | デコーダ | 却下理由 |
|------|------|---------|---------|
| `kotoba-whisper-v2.1` / `v2.2` | transformers pipeline | v2.0 と同一 | **音響モデルは v2.0 のまま。** 追加分は句読点付与（`punctuators`）と話者分離（`diarizers`）で、こちら側で別に持っている後処理 |
| `mpasila/faster-whisper-Visual-novel-transcriptor` | CT2 | **distil-large-v2 = 2層** | 蒸留。**加えて非商用限定・NSFW 学習データ** |
| `sky9262/whisper-base-japanese-anime` | transformers | base | 小さすぎる |
| `kotoba-whisper-bilingual-v1.0` | — | 2層 | 蒸留。ja/en 翻訳向けで用途が違う |
| `yky-h/nue-asr` | 独自 | — | CT2 非対応・タイムスタンプなし・専用パッケージ |

#### 副産物：事前変換済み CT2 は罠2 を100%踏む（修正済み）

**`-m kotoba-v2` は4本すべて exit 3221225477（0xC0000005）で即死した。**
トレースバックも `[ERROR]` 行も無く、`[STT] Transcribing` の直後に終了。
**[罠2](HANDOVER.md#罠2-蒸留モデルの-alignment_heads-はデコーダ層数と合っていない)そのもの。**

```
alignment_heads = [[7,0],[10,17],[12,18],[13,12],[16,1],[17,14],[19,11],[21,4],[24,1],[25,6]]
                   → 参照レイヤー 7〜25、実際のデコーダは 2層
```

**罠2 に載っている anime-whisper のリストと完全に同一の値。** kotoba-v2 がベースなので当然。

**HANDOVER はこれを予測して「未検証」と書いていた。**

> HuggingFace 上に第三者が上げている anime-whisper の CT2 変換物は、
> この修復が入っていなければ同じクラッシュをするはず（**未検証**）

**検証された。ただし第三者変換物ではなく公式の kotoba-tech 配布物で。**

**原因は修復の適用範囲。** `_fix_alignment_heads()` は変換時にしか走らず、
`[MODEL] ... is already CTranslate2` の経路では config.json に触らない。
**つまり事前変換済み CT2 は全部この穴を通る。**

**修復できない理由も判明した。** 修復には層数とヘッド数の両方が要る。
事前変換済み CT2 の config.json は
`alignment_heads` / `lang_ids` / `suppress_ids` / `suppress_ids_begin` の**4キーだけ**で、
`ctranslate2.models.Whisper` も構造を公開していない（実測: `n_mels` / `num_languages` /
`is_multilingual` とスレッド数のみ）。**層数は `model.bin` の重み名から取れる**
（`decoder/layer_(\d+)/` の最大値。kotoba-v2 で 2、large-v3 で 32 と正しく出た）が、
**ヘッド数はテンソル形状の解析が必要**で、全ヘッドが範囲外のときの代替リストが作れない。

**採った対処は無効化。** `whisp_models.word_timestamp_guard()` が検出したら
**word timestamps を切って理由を3行出す。** 当てずっぽうのヘッドを書くより安全で、
**テキストは変わらないので全文CERに影響しない**（失うのは単語単位の時刻。
`sanitize_segments()` が分割ではなくクランプになり、整形が擬似単語経路になる）。

**走査コストはキャッシュした。** large-v3 で 3.3秒かかるので既定モデルで毎回払うのは無駄。
`_models/whisp-carrier-ct2-layers.json` にサイズ+mtime をキーに記録し、
**実測 cold 3〜4秒 → warm 0.00秒。**

**測定を1回無駄にしたミスも記録しておく。** 最初の kotoba-v2 実行は
`--config ext-clip` で回してしまい、**あれは `silero_v5`**（TEN は `ten-clip`）。
成功していても `ten-f1` と比較できない数字だった。

### #28. 定型句フィルタを残り6本にも当てて、15本を現行既定で採り直した（2026-09-03）

> **採点条件** **推論なし。** 既存の `ten-new` / `ten-long` / `new-clip` に
> **出荷している `filler_filter.py` を import して当てた**（`_eval/_tmp_apply_filler*.py`。
> `eval/strip_loops.py` と同じ方式）／ 世代 **修正後（現行）** ／ 歌唱除外 ／
> 出典 `_eval/score-new4-f1.txt`・`_eval/score-long-f1.txt`・
> `_eval/score-majilumiere-f1.txt`・`_eval/score-new4-vad-f1.txt`、
> 24分もの9本は既存の `_eval/score-fixed9.txt`

**[#26](#26-定型句フィルタを実装した20-の見送りを撤回2026-09-02) が測ったのは
24分もの9本だけだった。** 子供向け4本と一挙2本は
**フィルタなしの数字のまま README と STATUS に載っていた。**
**v1.0 で公開する表が2世代混在になるので、残り6本を埋めた。**

**方法の正当性は #26 が担保している。** あそこで
**実装をあとから当てた結果が、パイプライン内で動かした結果と19本すべてで
セグメント集合まで一致した。** フィルタはセグメントを落とすだけで
テキストも時刻も書き換えないので、既存の出力に後から当てても同じものになる。
**`_eval/_strip_filler.py` ではなく `filler_filter` を import した** —
あれは上限測定用の緩い部分一致リストで、出荷しているルール（区間全体の一致、
`FILLER_JA` 3 + `FILLER_KO` 4）とは別物。

#### 15本すべてが現行既定の数字になった

| 素材群 | 本数 | 参照 | 旧記録（フィルタなし） | **現行既定** | 差 | XXL |
|--------|------|------|---------------------|-------------|-----|-----|
| 24分もの | 9 | 35,036 | 16.1% | **15.5%** | −0.6pt | 20.5% |
| 子供向け | 4 | 16,704 | 21.9% | **21.6%** | −0.3pt | 33.5% |
| ハクメイ一挙 5h22m | 1 | 37,987 | 16.1% | **15.6%** | −0.5pt | 未測定 |
| マジルミエ一挙 4h52m | 1 | 50,255 | 14.1% | **13.8%** | −0.3pt | 未測定 |

**ファイル別では 14本が改善、1本が同値（LIAR GAME #10）、退行は0本。**
カバレッジは**4群すべてで不変**（86.6 / 82.1 / 86.6 / 87.9%）で、
**動いたのは精度だけ**
（90.0 → **90.8** / 86.6 → **87.2** / 89.3 → **89.9** / 91.4 → **91.9%**）。
**落としているのは参照に無い文字だけ**という設計どおりの形。

#### 落とした量

| 素材群 | 検出 | 破棄 | 文字数 | 部分一致（保持） |
|--------|------|------|--------|----------------|
| 子供向け4本 | 14 | 14 | 196 | **0** |
| ハクメイ一挙 | 60 | 60 | 840 | **0** |
| マジルミエ一挙 | 39 | 39 | 546 | **0** |
| 子供向け4本の silero 側 | 33 | 33 | 464 | **0** |

**全部が `ご視聴ありがとうございました` 系の完全一致。** 部分一致は1件も無い。
**一挙で桁が上がるのは尺の効果**（5時間で60件＝12分に1回）で、
**24分もので43件だった密度と整合している。**

**一挙は `thin` も消えた**（ハクメイ 1件 → **0件**）。落とした定型句が
主題歌帯の長いキューだったため。**ピーク密度も 78 → 38ch/s に下がっている。**

#### VAD 比較を同世代に揃えた

**README は「silero → TEN」を 19.3% → 16.1% と書いていたが、
これはどちらもフィルタなしの数字。** 両側にフィルタを当てて採り直した。

| 素材群 | silero（旧既定）+ フィルタ | **TEN（現既定）+ フィルタ** | 差 |
|--------|------------------------|------------------------|-----|
| 24分もの9本 | 19.0% | **15.5%** | **−3.5pt** |
| 子供向け4本 | 30.8% | **21.6%** | **−9.2pt** |

**子供向けの −9.2pt はフィルタなしのとき（31.1 → 21.9）と同じ。**
**定型句は VAD の優劣にほぼ影響しない** — 両方が同じものを同じだけ出している。

> ##  ⚠ 「15本すべてで silero 以上」は現行世代では成立しない
>
> **同世代で採り直すと 12勝1敗になる。** 負ける1本は
> **Summer Pockets #26 で、0.5pt 悪化**（silero 12.9% 対 TEN 13.4%）。
> **フィルタなしでは同値だった**（13.8% 対 13.8%）ので、
> **順位が入れ替わったのは定型句フィルタが silero 側により多く効いたため**
> （13.8 → 12.9 で −0.9pt、TEN 側は 13.8 → 13.4 で −0.4pt）。
>
> | 素材 | silero + フィルタ | TEN + フィルタ | 差 |
> |------|-----------------|--------------|-----|
> | 公女殿下 #10 | 8.5% | **8.0%** | −0.5 |
> | LIAR GAME #10 | 11.1% | **9.7%** | −1.4 |
> | 桃源暗鬼 | 11.9% | **11.6%** | −0.3 |
> | **Summer Pockets #26** | **12.9%** | 13.4% | **+0.5** |
> | 終末ツーリング #12 | 16.6% | **13.6%** | −3.0 |
> | クレバテス #06 | 16.7% | **15.5%** | −1.2 |
> | ニンジャラ「ガムッチ」 | 28.9% | **26.7%** | −2.2 |
> | 死亡遊戯 #09 | 41.7% | **28.8%** | −12.9 |
> | アイプリ（フェス） | 40.9% | **22.9%** | −18.0 |
> | ニンジャラ「花火」 | 21.1% | **16.0%** | −5.1 |
> | ぷにる #20 | 29.0% | **25.0%** | −4.0 |
> | 名探偵プリキュア | 34.6% | **22.6%** | −12.0 |
> | アイプリ（トップ） | 44.2% | **23.4%** | −20.8 |
>
> **README と README_en から「15本すべて」を外した。**
>
> **追記（同日）。一挙2本の silero 側も同世代で採り直せた**ので、
> **15本の勝敗は 14勝1敗で確定した。**
>
> | 素材 | silero + フィルタ | TEN + フィルタ | 差 |
> |------|-----------------|--------------|-----|
> | ハクメイ一挙 5h22m | 17.6% | **15.6%** | −2.0 |
> | マジルミエ一挙 4h52m | 15.7% | **13.8%** | −1.9 |
>
> 出典 `_eval/score-long-vad-f1.txt` / `_eval/score-majilumiere-vad-f1.txt`。
> **1本の採点に20分近くかかるので一度打ち切ったが、裏で走り切っていた。**
>
> **README は「13本で12勝1敗」のまま公開する。** 数として正しく、
> **1.0.0 のアーカイブは既に固めてあるので、より強い言い方に差し替えるだけのために
> 固め直す価値はない**（[発動条件](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)）。
> **次に版を上げるときに「15本で14勝1敗」へ差し替える。**
>
> **結論の向きは変わらない。** 群の合計ではどの群でも TEN の勝ちで、
> 差は −3.5pt と −9.2pt。**変わったのは「全勝」と言えるかどうかだけ。**

#### ファイル別（15本・現行既定）

| 素材 | CER | 素材 | CER |
|------|-----|------|-----|
| 公女殿下 #10 | **8.0%** | クレバテス #06 | 15.5% |
| LIAR GAME #10 | 9.7% | ハクメイ一挙 | 15.6% |
| 桃源暗鬼 | 11.6% | ニンジャラ「花火」 | 16.0% |
| Summer Pockets #26 | 13.4% | 名探偵プリキュア | 22.6% |
| 終末ツーリング #12 | 13.6% | おねがいアイプリ（フェス） | 22.9% |
| マジルミエ一挙 | 13.8% | おねがいアイプリ（トップ） | 23.4% |
| | | ぷにる #20 | 25.0% |
| | | ニンジャラ「ガムッチ」 | 26.7% |
| | | 死亡遊戯 #09 | **28.8%** |

**幅は 8.0〜28.8%。** 旧記録（8.7〜30.5%）から**両端が縮んだ**ので、
**「最悪でも30%程度」は「最悪でも30%未満」に書き換えられる。**

**ニンジャラは同じ番組で 16.0% と 26.7% に割れる。**
層で語るときに番組名を1つ挙げるだけでは足りない例。

### #29. 取りこぼしを分解した。現行パイプラインの伸びしろは2pt未満（否定的結果4件・2026-09-05）

> **採点条件** **推論なし。** 既存の hyp キャッシュと TEN VAD の再パスだけで出している。
> 参照は **plain 正規化後・music cue 除外**（`score.py` の既定と同じ分母）、
> 世代は **現行**。**CER は出していない** — 測っているのは「その参照キューに
> 一致するテキストが出たか」で、単位はキューまたは文字。
> probe は `_eval/_ten_miss_where.py` / `_inside_miss_where.py` / `_miss_kind.py` /
> `_arib_floor.py` / `_floor_composition.py`、出力は同名の `.txt`。
> **`eval/` は1行も変更していない。記録値は再現可能なまま。**
> **対象本数が節ごとに違う**（9本 = 24分もの、8本 = 他系統の出力が無い
> ニンジャラ「花火」を除く、3本 = 族外モデルを回した素材）。**混ぜて引かないこと。**

**きっかけは「精度が上がる望みが薄い」の裏を取ること。**
[#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) でモデル軸が閉じ、
[#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果)・[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)・[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) で
VAD 空間が閉じている。**残っているのは「取りこぼし 13.4% の中身が何か」だけで、
それは一度も分解されていなかった。** カバレッジはファイル単位・ブロック単位の
指標なので、どの行がなぜ落ちたかは原理的に出てこない。

#### (1) VAD の外は参照の 0.56%。取りこぼしの主戦場ではない

TEN の確率トラックを取り出し、参照キューと区間の重なりを見た。
**1msも重ならないキュー**を数えると、9本で **203c / 36,463c = 0.56%**。

**公表カバレッジは 86.6%（取りこぼし 13.4%）。つまり取りこぼしの9割前後は
区間の中で起きている。** VAD は音声を渡していて、テキストが出ていない。

**確率の内訳（生のキュー本文で計上、9本）。**

| | 文字 | cues | 意味 |
|---|------|------|------|
| A: peak ≥ 0.75 | 101c | 15 | トリガーは引けている |
| B: 0.60 ≤ peak < 0.75 | 257c | 31 | 閾値だけが止めている |
| C: peak < 0.60 | 357c | 51 | 床より下（[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果) と同じ） |

**A の原因は不具合ではなく `min_speech_ms`。** `run>=thr` が16〜48ms（1〜3フレーム）で、
区間は開くが `current["end"]` が最初に 0.60 を割った位置なので長さが 250ms に届かず、
`> min_speech_samples` で捨てられる。**`min_speech` は「4つ振り終えた」
（threshold / min_silence / speech_pad / neg_threshold）に入っていない5つめの knob で、
両モデルで一度も測っていない。** ただし到達しうる量は A+B で 358c しかないので、
**振る価値はほぼ無い。**

**あわせて `ten` の閾値 0.75 の出自を確認した。** `_eval/score-ten-sweep.txt` の
1行目が `only 2026030522`、つまり**死亡遊戯1本・pre-#19・定型句フィルタ前**。
[#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) が
0.90 について「修正前の指標なので必要なら9本で測り直すこと」と書いているのと
同じ条件が 0.60 にも当てはまる。**本節の結果からは測り直す理由が無い**が、
出自が薄いことは記録しておく。

#### (2) 重なり発話は9本すべてで0件

同時発話でどちらかが勝つ形を疑ったが、**参照キューが時間的に過半かぶる例が
1件も無かった**（判定は短い側の窓の50%超。単なる隣接は数えない）。
**この軸は開く前に閉じた。**

#### (3) 多系統で届かない床が、参照の 9.15%

**VAD を替えても実装を替えても出てこない参照を数えた。**
5系統（TEN / silero / 内蔵 / **VADなし** / **XXL r245.4**）、8本。

| 数え方 | 文字 | 参照比 |
|--------|------|--------|
| どの系統も出さないキュー（4字以下は部分文字列一致） | 1,475c | 4.66% |
| 同・LCSのみの緩い判定 | 1,144c | 3.61% |
| **どの系統も一致させられなかった文字** | **2,896c** | **9.15%** |

**最後の行だけが CER と同じ数え方で、公表カバレッジから引ける唯一の量。**
上2つはキュー単位の二値なので、CER への寄与としては過大になる
（`は… はい` 対 `はい` は score.py では欠落1文字で、キュー全損ではない）。

**素材別（未一致文字 / 公表CER）。**

| 素材 | 未一致文字 | 公表CER |
|------|-----------|---------|
| 死亡遊戯 #09 | 20.95% | 28.8% |
| ニンジャラ「ガムッチ」 | 13.28% | 26.7% |
| クレバテス #06 | 10.47% | 15.5% |
| Summer Pockets #26 | 8.60% | 13.4% |
| 桃源暗鬼 | 7.65% | 11.6% |
| LIAR GAME #10 | 7.36% | 9.7% |
| 終末ツーリング #12 | 7.24% | 13.6% |
| 公女殿下 #10 | 4.22% | 8.0% |

**順位がほぼ一致し、各素材の CER の 50〜76% を占める。**
ただし**これは部分的に同語反復**で、未一致文字は CER の欠落成分そのもの。
**独立した予測ではなく分解。** 言えるのは「素材別のばらつきは、
発話の難しさより参照に何が入っているかで決まっている」まで。

**注意。未一致文字は系統の union（オラクル）で、単独でそこに届く系統は無い。**
欠落成分だけで、CER には挿入・置換も含まれる。`pad 2.0s` にも依存する。

#### (4) 族外モデルを入れても床は動かない。anime-whisper が最も拾えない

**5系統すべて faster-whisper なので、Whisper 一族の共通の弱点が床に見える。**
そこで **Qwen（族外）** と日本語モデル3つを足して3本で測り直した。

| 系統 | 拾えた |
|------|-------|
| qwen-1.7b | **89.96%** |
| ten-f1（既定） | 89.48% |
| novad | 88.06% |
| jp-large-v2 | 86.94% |
| xxl | 86.36% |
| kotoba-v2 | 72.88% |
| **anime-whisper** | **68.18%** |

**床は 7.06%（未一致文字 8.64%）。5系統構成の同3本より 1〜2pt しか下がらない。**
アーキテクチャを跨いでも残る。

**anime-whisper が決定的な対照になった。** アニメ台詞で微調整したモデル、つまり
「あっ」「ははい」を書きそうな第一候補が、**7系統中で最も拾えない**（単独到達 9c）。
**ドメイン適応で床に届く、という筋は否定された。**
qwen が既定を 0.5pt 上回るのは単独到達 27c ぶんで、
[#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) の CER では大きく負けている
（拾う量を増やして精度で払う形）。

**全系統の単独到達を合計しても 172c = 1.58%。**
8本構成の「既定だけが落として他が拾えたぶん」も 518c = 1.64%。
**実装とモデルで買える差の上限がここ。**

#### (5) 記法の吸収では床は動かない（0.28pt）

**床の生テキストに ARIB の記法が見えた**ので、正規化で吸収する案を検討した。
長音（`に～い～さ～ん`）、どもり（`り… リィネ`）、語尾促音（`大好きっ`）。
**両側に対称に当てて測った**（片側だけ緩めると全系統が無料で得をする）。

| | 未一致文字 | 参照比 | 差 |
|---|-----------|--------|-----|
| 現行 | 1,475c | 4.66% | |
| R1 `っ/ッ` 削除 | 1,432c | 4.52% | −0.14pt |
| R2 連続同字→1 | 1,437c | 4.54% | −0.12pt |
| R3 両方 | 1,387c | 4.38% | **−0.28pt** |

**0.28pt。15本の記録値を無効にする対価に届かない。正規化は触らない。**

**床320キューの内訳（重複あり）。** かなのみ 272 cues / 1,117c（床の75.7%）、
4字以下 219 cues / 596c、語尾 `っ` 106 cues / 330c、`～` を含む 71 cues / 334c、
どもり記法 13 cues / 77c。**記法は見えているのに吸収では届かない。**

**理由。緩和しても内容が無い。** `あっ` を `あ` にしても、
周辺の仮説が `誰が返すかよ` なら `あ` はどこにも無い。
**床は表記の不一致ではなく、その発話が一度も文字になっていないこと。**
**どの緩和でも届かないのが 1,236c、床の 83.8%**（`うん` `ウフフ` `くっ`
`うわぁ` `ひゃあ` `フン` `ウオー` `ガオー` `やっ`）。

**ただし床は全部が不可避ではない。** 実台詞も混ざる
（`痛い痛い` `やめてぇ` `こっちだほら行くぞ` `違うだろだからオレが言ってやったんだ`）。

#### 途中で2回読み間違えた（同じ型）

**どちらも「窓の広さ」と「比率」を量として読んだ誤り。記録しておく。**

**1. 窓の被覆率で判定して、床の46%を「トリガーは引けているのに落ちている」に
入れてしまった。** `arib_vtt.speech_budget` は 1.2秒/文字・p95・最低2秒という
**意図的に緩い**見積もりで、docstring も「過大に取るほうが安全な方向」と書いている。
**その窓に対して被覆率50%で切ると、正常に検出された発話が未検出に見える。**
**ゼロ被覆（1msも重ならない）に変えたら 46% → 14% に落ちた。**

**2. 「B が床の35.9%だからスイープに進む」と自動判定を書いた。**
**1.5% の 35.9% は小さい。** [#8](#8---vad_threshold-は取りこぼしの-knob-ではなかった否定的結果) が
「字数だけ見て『効いた』と読むと間違える（実際に一度読み間違えた）」と
書いてあるのと同じ穴。**判定は絶対量で切るように直した。**

**あわせて、生のキュー本文で数えていたぶん初期の数値が膨らんでいた。**
`strip_markup` は `PAREN_RE` で括弧を落とすので、`（銃声）` `（ノック）` は
**plain で空になり、元々 CER の参照文字数に入っていない。**
取り返せない代わりに charge もされていない。

#### 副産物：`--vad_neg_threshold` のヘルプが間違っている

ヘルプは `Silero backends only.` だが、`vad.get_speech_segments()` の docstring は
`neg_threshold only reaches the silero and TEN backends` で、
**コードも `get_speech_segments_ten()` に渡している。** ヘルプが誤り。
**挙動は正しいので実害は無いが、TEN で効かないと読める。**

#### 結論。現行パイプラインの伸びしろは1〜2pt

| 軸 | 状態 |
|----|------|
| VAD パラメータ | **閉じた。** 区間外は参照の 0.56%。`min_speech` は未測定だが到達量 358c |
| 重なり発話 | **閉じた。** 9本で0件 |
| デコード側の閾値（`no_speech_threshold` ほか） | **筋が悪い。** 出力ゼロの中身が非言語音声（`フン` `ウオー` `ハハハ`）で、閾値の問題ではない |
| モデル | **閉じた。** [#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) に加えて族外・ドメイン適応も既定を超えない |
| 採点指標（正規化） | **触らない。** 記法の吸収は 0.28pt |
| 残る余地 | **実装・モデルの差の合計で 1.6% 程度** |

**#19 との違いを明示しておく。** あれは**非対称な欠陥**で、
歌を書き起こす設定だけが不当に罰せられていたので**順位が動いた**。
だから直すしかなかった。**今回の床は対称**で、XXL 側にも同じ層が出ている。
**絶対値が悲観的なだけで、比較としては公平。** だから直すのは義務ではない。

**残っている選択肢は採点範囲の話だけ。** `--exclude-songs` と同じ形で
非言語音声キューを opt-in で外せば CER が「台詞をどれだけ拾えたか」に近づくが、
**「非言語」の定義はこちら側の判断になる。**
**やるなら既定 off で、README には床を併記する。**

**数えてはいけないもの。** 本節は「どの ASR でも不可能」を証明していない。
族外は qwen 1つだけで、判定は `pad 2.0s` と部分文字列規則に依存する。
**言えるのは「VAD・実装・モデルの差では動かない」まで。**

---

## 作業記録

実装の経緯と、そう判断した根拠。**新しく読む人は飛ばしてよい**が、
同じ罠を踏み直さないための記録として残している。

### ffmpeg を GPL v3 から公式 LGPL v3 に差し替えた

`whisp_carrier.spec` が探索候補の2番目に、ローカルの Faster-Whisper-XXL
配布フォルダをハードコードで指していた。**そこの ffmpeg.exe は
gyan.dev の essentials build（FFmpeg 6.1.1、`--enable-gpl --enable-version3`）
＝ GPL v3。** MIT のこのプロジェクトに同梱すると配布物全体が GPL 条件下に入る。
環境変数と PATH より優先される位置にあったので、ビルドが通れば必ず入る状態だった。

公式の LGPL ビルド（BtbN/FFmpeg-Builds、n8.1.2）に差し替え、**spec が同梱直前に
configure 行を実際に読んで `--enable-gpl` / `--enable-nonfree` があれば
ビルドを止める**ようにした。ハッシュのピン留めも併用（ピンだけだと ffmpeg 更新の
たびに更新が必要で、古いピンが「検査なし」に劣化する）。

ビルドログに出る形。

```
[spec] ffmpeg: ...\_tools\ffmpeg\official\ffmpeg.exe
[spec] ffmpeg sha256: 8ee152edc79f7ba99969b7fb590cfde438b46cb383952dec87e754db83788572
[spec] ffmpeg verified (known LGPL build): ffmpeg version n8.1.2-44-g7c533d0f86-20260820
```

zip は配布元の公開ハッシュと3経路で照合済み（ローカル実測 / 配布元の
`checksums.sha256` / GitHub Releases API の digest）。出所と検証記録は
`_tools/ffmpeg/PROVENANCE.txt`、同梱物の一覧は `THIRD-PARTY-NOTICES.md`
（Pythonパッケージ、NVIDIA CUDA/cuDNN、Intel OpenMP、libsndfile、Silero VAD）。

### 設定ファイルが再ビルドで消える罠

COLLECT が `dist/whisp-carrier` を丸ごと削除するので `whisp-carrier.yaml` も
消えていた。**黙って内蔵VAD経路（CER 27.1%）に戻るので気付けない。**
spec が COLLECT 前に読んで post-COLLECT で書き戻すようにした（`LIVE_CONFIG_PATH`）。

```
[spec] preserving existing config: ...\dist\whisp-carrier\whisp-carrier.yaml
[spec] copied config sample to:    ...\whisp-carrier.yaml.example
[spec] copied ffmpeg licence to:   ...\LICENSE.ffmpeg.txt
[spec] copied LICENSE to:          ...\LICENSE
[spec] restored config:            ...\dist\whisp-carrier\whisp-carrier.yaml
```

**再ビルド後の exe を実際に起動して確認済み**（長らく未確認だった項目）。

```
[CONFIG] ...\dist\whisp-carrier\whisp-carrier.yaml | profile=anime | override=on
[CONFIG]   beam_size = 10
[CONFIG]   best_of = 10
[CONFIG]   language = 'ja'
[CONFIG]   standard_asia = True
[CONFIG]   vad_method = 'silero_v5'
  [VAD] 1 segments via clip_timestamps (clips are zero-padded to 30s)
  [STT] Done in 1.0s | lang=ja (100.00%) | 3 segments
  [FMT] width=16 lines=2 sentence=True gap=3.0 | 3 -> 3 cues
EXIT=0
```

設定の自動検出（`whisp_config.base_dir()` が `sys.executable` の親を返す経路）が
凍結ビルドで動くこと、`override: true` が効くこと、`vad_method: silero_v5` が
外部VAD + clip 経路に入ることが、これで同時に確認できている。

なお exe の出力には torchaudio の
`In 2.9, this function's implementation will be changed to use
torchaudio.load_with_torchcodec` という UserWarning が混ざる。動作に影響はないが、
ログとしては雑音なので気になるなら抑制する。

### 終了コードの修正（ファイル処理の失敗を非ゼロにした）

`main()` のファイルループが例外を捕まえて `[ERROR]` を出すだけで、終了コードに
反映していなかった。`main()` は何も返さないので **どのファイルが失敗しても
常に exit 0**。しかも `[ERROR]` の直後に `All done` と表示するので、
**バッチ処理では失敗に気付けない。**

```python
# 修正前
for i, f in enumerate(files, 1):
    try:
        process_single_file(...)
    except Exception as e:
        print(f"  [ERROR] {f}: {e}", file=sys.stderr, flush=True)
print("\n[whisp-carrier] All done.", flush=True)   # ← 失敗しても必ずこれ
```

失敗したファイルを集めて、1つでもあれば一覧を stderr に出して `sys.exit(1)`
するようにした。**`All done` は全件成功のときだけ出す**（成功と誤読される
のを防ぐのが主眼）。1本失敗してもバッチ全体は止めない従来の挙動は維持。

終了コードは既存の割り当てに合わせて **1**（`2` は設定・モデル・VADパッチの
起動前エラーに使われている。`--list_models` 等は 0）。

実測3ケース。不正な wav（テキストファイルを `.wav` にしたもの）に
`--ff_loudnorm` を当てて ffmpeg を失敗させた。

| ケース | 出力 | exit |
|--------|------|------|
| 正常1本 | `All done` | **0**（回帰なし） |
| 失敗1本 | `Failed: 1 of 1 file(s).` + ファイル名 | **1** |
| 正常1本 + 失敗1本 | 正常側は `[OUT]` を出し、末尾に `Failed: 1 of 2 file(s).` | **1** |

`process_single_file` の `try` には `except` が無く `finally` だけなので
（一時ファイルの削除用）、フィルターの例外はそのまま `main()` まで上がる。
飲まれていないことをコードで確認済み。

**残っているのは[既知の問題 2-b](HANDOVER.md#2-b---ff_lowhighpass-は今のパイプラインではほぼ何もしていない実測)のほう。**
今回直したのは「例外が上がったときに終了コードへ反映されない」問題だけで、
ffmpeg が 0 を返しつつ壊れた出力を書くケースの検出は別途必要。

### ログの文字化け：原因は自分の出力エンコーディングだった（解決済み）

**この節は一度誤った結論を出している。** 最初は「Amatsukaze が子プロセスの出力を
再エンコードしているので、こちらからは制御できない」と判断して `console_safe()` で
ASCII 以外を無条件エスケープする回避策を入れた。**それは症状の隠蔽で、原因は
こちら側にあった。** Amatsukaze のソースで裏を取って直したので、経緯を残す。

#### 症状

```
00:06:34  13.9s 248 chars over 1 distinct: ????????????????????????...
00:12:24   2.1s '???b?N' x4 spanning 12 chars: \u30ed\u30c3\u30af ...
```

2行目が切り分けの鍵だった。**同じ行の中で、コロンの後ろは `\uXXXX` で正しく出て
いるのに、引用符の中だけが `???b?N` になっている。** つまり `console_safe()` は
効いていて、**通していない箇所が1つあった**という意味。

#### 原因1：`console_safe()` の通し忘れ

`loop_filter.reason()` の3つめの条件が `f"{unit!r} x{count} spanning {span} chars"`
で組まれていて、**`repr()` は非ASCIIをエスケープしない**（PEP 3138）。
そのため `why` は生の日本語を含むのに、`whisp_carrier.py` は `text` 側だけを
`console_safe()` に通していた。条件1（`N chars over M distinct`）は数値だけなので
化けない。**症状がログの行ごとに違ったのはこれが理由。**

#### 原因2（本体）：パイプに cp932 を書いていた

`ロック` の化け方が答えを持っていた。`???b?N` の `b` と `N` は偶然ではない。

```
ロ = 83 8D   ッ = 83 62   ク = 83 4E     ← cp932
                  ↑ 'b'          ↑ 'N'   ← trail バイトが ASCII 範囲なので生き残る
```

**cp932 のバイト列を UTF-8 として解釈した時の特徴。** 手元で再現させたら
`'???b?N' x4 spanning 12 chars` がログと1バイト違わず一致した。

なぜ cp932 だったか。実測するとこうなっている。

| 出力先 | `sys.stdout.encoding` |
|---|---|
| コンソールに直接 | **utf-8**（Python 3.6+ は Windows のワイド API を使う） |
| パイプ経由（Amatsukaze） | **cp932** |
| ファイルへリダイレクト | **cp932** |

つまりコンソールでは正しく、**リダイレクトされた時だけロケール依存に落ちる。**
`console_safe` を入れる前の「ローカルの encoding で書けなければエスケープ」という
判定が無効だったのも同じ理由で、**cp932 は `ロック` を表現できるので判定は通る。**
書けるが、読む側が別の解釈をする。

#### Amatsukaze 側の契約（ソースで確認）

**Amatsukaze は whisper の出力を無条件に UTF-8 として読む。**

- `Amatsukaze/TranscodeManager.cpp` — 同期実行・並列実行の**両経路とも
  `param.isUtf8Log = true` を直接代入**している。GUI の設定項目ではなく固定値
- `Amatsukaze/Subtitle.h` — `WhisperProcessParam` の既定値も `isUtf8Log(true)`
- `Amatsukaze/Subtitle.cpp` — `new StdRedirectedSubProcess(cmd, 10, param.isUtf8Log, ...)`
- `Amatsukaze/ProcessThread.cpp` — `if (isUtf8) line = utf8ToString(ptr, len);`
- `Amatsukaze/StringUtils.cpp` — `utf8ToString()` は
  `char_to_wstring(..., CP_UTF8)` → `wstring_to_string(..., CP_ACP)`。
  **UTF-8 → UTF-16 → cp932**

`StringLiner::AddBytes(MemoryChunk utf8)` と引数名まで UTF-8 前提。
**エンコーディングは最初からこちらの責任だった。**

#### 直したこと

1. **`_setup_console_encoding()` で stdout / stderr を UTF-8 に固定。**
   `PYTHONIOENCODING` が設定されていれば尊重する（`eval/run.py` がこれを使う）。
   凍結ビルドで `sys.stdout` が `None` になる場合と `reconfigure` 不在も回避
2. **`console_safe()` を条件付きに戻した。** ただし判定は「この encoding で
   書けるか」ではなく **「UTF-8 か」**。両者は違い、その違いが今回のバグそのもの
   （cp932 は書けてしまう）。UTF-8 以外なら `\uXXXX` にする
3. **`console_safe(why)` の通し忘れを修正**

**副産物が大きい。** ストリーム側を直したので、`console_safe()` を通していない
行も全部直った。**入力パス（`Processing:`）、`[OUT]` の出力パス、
`[STT]` の逐次プレビュー、`[WARN] File not found`** はどれも素材由来の文字列を
生で出していて、日本語ファイル名では化けていたはず。個別にラップしていたら
一生気付かない類の穴だった。

#### 実測（パイプのバイト列を直接確認し、Amatsukaze のデコードを再現）

| 条件 | パイプ上のバイト | UTF-8デコード | 表示 |
|------|-----------------|--------------|------|
| **既定**（Amatsukazeの条件） | `27 e3 83 ad e3 83 83 e3 82 af 27` | **成功** | `'ロック' x4 spanning 12 chars: ロックロックロックロック` |
| `PYTHONIOENCODING=cp932` | `27 5c 75 33 30 65 64 ...` | **成功** | `'\u30ed\u30c3\u30af' x4 ...` |

**どちらの経路でも `?` が出ない。** 2つめは `console_safe` のフォールバックが
効いた形で、読みにくいが情報は失われず復元できる。

なお**この2つめのケースは修正の途中で一度取りこぼした。** 「書けるか」で判定して
いた版では cp932 が `ロック` を書けてしまうためエスケープが発動せず、化けが再現した。
**「UTF-8 か」で判定する形にして塞いだ。**

### Amatsukazeがどこで呼ぶか（ログで確定）

長く「未確認」だった呼び出し位置が実行ログで確定した。
**CMカット後・出力ファイル単位・中間AACに対して呼ばれる。**

`[音声出力]` 段が trim 済みタイムラインのAACを吐き、直後の
`[字幕ファイル生成]` → `[Whisper起動]` が1ファイルずつ渡す。

```
音声1-0出力 -> .../a0-0-0-0-main.aac   ← 150フレーム = 5.005秒
音声0-0出力 -> .../a0-1-0-0-main.aac   ← 約24分（本編）
```

元TSは54205フレーム=30.1分、TrimAVSの合計が約43.7kフレーム=24.3分なので
CMは落ちた後。5秒の方はCM解析の `53922 54071 :Nologo(cut)`（150フレーム）が
そのまま1出力ファイルになったもの。

ここから分かること。

- **参照VTTと同じ時間軸に対して呼ばれている。** `sample/` の素材はまさに
  この段の出力なので、既存の測定はそのまま実運用の数字として読める
- **`--vad_method` と `--language` を渡してこない。** ここが効くので
  下の[実運用の呼び出し設定](#実運用の呼び出し設定amatsukazeを触らずに直せる)に分けた

  > **訂正（2026-08-23）。当初ここに「Amatsukazeが渡すのは `--model` `--output_dir`
  > `-f` `--beam_size` `--best_of` だけ」と書いていたが、それは
  > GUI の設定が反映された結果でソフト固定の挙動ではない。**
  > model を `未指定` にし whisper-option 欄を空にしたログでは、渡ってきたのは
  > **`--output_dir` と `-f srt vtt` の2つだけ**だった
  > （[XXL の実運用失敗](#実運用での裏付けamatsukaze-から素の-xxl-を呼ぶと落ちる2026-08-23)のログ）。
  > **beam 10 の出どころを[訂正した](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)のと同じ構図**で、
  > 「Amatsukaze が渡してくる」と書く前に GUI 側を確認すること。
- **1録画で複数回呼ばれる。** 分割出力ごとに別プロセスで、モデルも毎回
  ロードし直す。5秒の断片にも large-v3 を丸ごとロードして呼ぶ形になる
- **音ズレは無関係。** Amatsukazeの報告が平均6.13ms / 最大10.63ms
- **生TSは渡ってこない。** かつて検討していた「TS（CM込み）での測定」は
  不要になった。同時に「CM境界がループや30秒超を誘発するか」という軸も消える
- **トラック選択もAmatsukaze側で済んでいる。** 渡ってくるのは単一トラックの音声で、
  実運用でも `extract_audio` は通らない

**注意：この呼び出し位置を確定したログは旧版（`whisper-xl` 名義）のもの。**
banner が `whisper-xl 0.1.0` で `[CONFIG]` `[VAD]` `[FIX]` 行が無い。
Amatsukaze側の段取りは版に依存しないのでそこは使えるが、
**Whisper側の出力（5秒ファイルが `lang=nn` で0セグメント、など）を
現行版の挙動として読まないこと。**

### 実運用の呼び出し設定（Amatsukazeを触らずに直せる）

現行CLIは Amatsukaze の引数を全部受けるので互換の問題は無い。**効くのは
渡していない側。**

- **`--vad_method` 無指定 → 既定の `silero_v5_fw`＝内蔵VAD経路。** eval の
  `builtin` そのもので、**測定で最良だった `ext-clip` ではない**
  （全文CER 27.1% / カバレッジ74.1% 対 24.3% / 80.4%）。実運用に載せるなら
  `--vad_method silero_v5` を足す。`--vad_segment_mode` は既定が `clip` なので
  指定は要らない
- **`--language` 無指定 → 自動判定のまま。** 分割で出る短いファイルは誤検出
  しうる（旧版ログの5秒ファイルが `nn` 61%）。24分の本編側も `ja` 57.6%で、
  eval素材の92〜99%より低い

どちらも `whisp-carrier.yaml`（`override: true`）に書けば**Amatsukaze側の
設定を触らずに入る。設定ファイル機構を足した動機そのものがこれ。**

### Amatsukaze連携テストの結果（現行版で初めて通した）

**Amatsukaze から exe を呼んで24分の本編が通り、設定ファイル・外部VAD・
ループ抑制・字幕整形がすべて実運用で動いた。**

```
"...\dist\whisp-carrier\whisp-carrier.exe" "...\a0-1-0-0-main.aac"
    --output_dir "...\whisper" -f srt vtt --model large-v3
    --ff_loudnorm --ff_lowhighpass --beam_size 10 --best_of 10
```

```
[CONFIG] ...\whisp-carrier.yaml | profile=anime | override=on
[CONFIG]   language = 'ja'
[CONFIG]   standard_asia = True
[CONFIG]   vad_method = 'silero_v5'
  [VAD] 35 segments via clip_timestamps (clips are zero-padded to 30s)
  [STT] 00:23:11 | 290 segments processed...
  [STT] Done in 265.2s | lang=ja (100.00%) | 292 segments
  [LOOP] 292 -> 287 segments | dropped 5 (333 chars of repetition)
  [FMT] width=16 lines=2 sentence=True gap=3.0 | 287 -> 325 cues
[whisp-carrier] All done.
```

**確認できたこと。**

- **設定ファイルの自動検出が凍結ビルドで動く**（`whisp_config.base_dir()` が
  `sys.executable` の親を返す経路が実運用で通った）
- **`vad_method: silero_v5` が効いた。** `via clip_timestamps` が出ているので
  外部VAD + clip 経路。**yaml に書かなければ内蔵経路（CER 27.1%）のままだった**
- **ループ抑制が実運用で初めて発動した。** 5セグメント333字を落とし、うち1つは
  **13.9秒で248字が1種類の文字**という典型的なループ。字幕として致命的な
  キューが消えた。測定では9本中3本でしか発動しなかったが、実素材でも起きる
- 進捗ハートビートが機能（30秒ごと、長尺で無音にならない）
- 字幕整形 287 → 325 キュー、終了コード 0

**見つかった問題は3つ。** 1と2は修正済みで、3が[次の着手 A](#a-amatsukaze連携テストの再実行フィルターを外した条件)。

1. `[LOOP]` の落としたテキストが `?` の羅列になった → 出力を UTF-8 に固定して解決
   （最初は `console_safe()` で隠蔽していた。[該当節](#ログの文字化け原因は自分の出力エンコーディングだった解決済み)）
2. exe が古く `[FF]` の段計測が出ていなかった（ターミナル再利用のせいで
   `audio_filter.py` の修正を含むビルドが実行されていなかった）→ 再ビルド済み
3. **追加オプションに `--ff_loudnorm --ff_lowhighpass` が入っていた。**
   測定条件（フィルターなし）と違う。`--ff_lowhighpass` は16kHz化後なので
   実質何もしていないうえ、`[STT] 265.2s` は測定時の131秒の2倍かかっている
   （`[VAD] 35 segments` も測定の19より多く、loudnorm でレベルが上がって
   VADの拾い方が変わった可能性）

### コンテナ等価性：MKV直渡しでバイト一致

**MKVを直接渡した出力は `clip-fixed` と完全に同一だった。**
クレバテス #06 で実測（`_eval/hyp/container-mkv-direct/`、ログ同梱）。

```
[whisp-carrier] Processing: ...\sample\mkv\...クレバテス... #06 [字].mkv
  [VAD] 19 segments via clip_timestamps
  [STT] Done in 131.3s | lang=ja (99.22%) | 355 segments
```

この JSON の MD5 が `_eval/hyp/clip-fixed/` の同ファイルと一致する
（`B4A05427A37AE9E15FEB14AF2715089B`、579,383バイト）。JSONにはパスが
入らず `duration` / `language` / `segments` だけなので、**セグメント境界も
テキストも全部同一**という意味になる。

つまり**既存の全測定は、コンテナ経由でもそのままの数字。**
ついでに推論の決定性も取れた（`clip-fixed` は2日前の実行）。

差分がどこにも出なかった理由。`--ff_*` が1つも渡らないので
`audio_filter.preprocess()` は走らず、`extract_audio` も通らない
（`whisp_carrier.py` の `has_ff` 判定）。残る違いは
`prep.py` の `ffmpeg -ar 16000 -ac 1 -c:a pcm_s16le` と
`decode_audio()`（PyAV）のリサンプラだけで、**そこも結果を変えなかった。**
外部VAD側は `vad.py` が `.wav` 以外の入力に ffmpeg でテンポラリWAVを作るので、
測定時と同じ経路を通る。

**残っている毛羽。** 実運用の入力は MKV ではなく中間AAC。ただし
`sample/mkv/` の音声トラックは**Amatsukazeがその中間AACをそのまま多重化した
もの**なので、ビットストリームは同一でデマルチプレクサだけが違う。
気になるなら `ffmpeg -i sample.mkv -c:a copy out.aac` で剥がして1本流せば
片付く（推論2分）。優先度は低い。

### フルビルドの結果（`WHISP_CARRIER_FULL=1`）

一度も通していなかったこのビルドを初めて通した。**ビルドは成功し、
`--realign` は動く。`--ff_vocal_extract` は動かない。**

```powershell
$env:WHISP_CARRIER_FULL='1'
python -m PyInstaller whisp_carrier.spec --noconfirm --distpath dist-full --workpath build-full
```

`--distpath` / `--workpath` を分けたのは、検証済みの通常ビルドを
上書きしないため。ビルド時間は約2分40秒（通常は約3分半だが、こちらは
`--clean` なし）。

| | 通常 | フル |
|---|---|---|
| サイズ | 4.78 GB | **5.06 GB**（+280MB） |
| 転写（`-m tiny`） | exit 0 | **exit 0** |
| `--realign` | 未同梱 | **動作（exit 0）** |
| `--ff_vocal_extract` | 未同梱 | **失敗**（audio-separator を除外したため） |
| `-m anime-whisper` | exit 2 で拒否 | **exit 2 で拒否** |

**`--realign` が動いたのは初の確認。** stable-ts の transcribe が走り、
`[REALIGN] Done` まで到達した。ただし[既知の問題 3](HANDOVER.md#3---realign-が不安定)のとおり
モデルを2つ目としてロードするコストは変わっていない。

**それでも配布は通常ビルドにする。** フルビルドが増やすのは `--realign`
だけで、**その `--realign` は推奨設定では必ずスキップされる。**
つまりフルビルドを配ると、+280MB を払って「推奨設定では動かない機能」が
1つ増えるだけになる。`--realign` が要る場合はスクリプト版を使う。

配布物としての検証量にも差がある。通常ビルドは転写・ループ抑制・終了コード・
フィルタ検査を通しているが、フルビルドは7秒の音声で `--realign` を1回
走らせただけ。

### audio-separator を全ビルドから除外した

フルビルドで `--ff_vocal_extract` を走らせたら、同梱には成功しているのに
実行すると scipy が壊れていると報告された。

```
[ERROR] test_speech.wav: The `scipy` install you are using seems to be broken,
        (extension modules cannot be imported), please try reinstalling.
```

**scipy は壊れておらず、再インストールしても直らない。** 凍結環境で拡張モジュールが
解決できていない（PyInstaller の scipy 収集の問題）。同梱したままにすると
**ユーザーを無関係な作業に送り込む**ので、外す方が良いと判断した。

**CERへの影響が無いことが除外の判断根拠。** `eval/run.py` は `--ff_*` を
一切渡さないので、このプロジェクトが出したすべての精度数値はフィルター経路を
通っていない。`audio_separator` を使うのは `--ff_vocal_extract mdx_kim2 /
mb-roformer` の2箇所だけで、主経路からは到達しない。

変更点は3つ。

- `audio_filter.py` に `_load_separator()` を追加。`vad.py` の
  `_missing_backend` と同じ作法で、凍結ビルドなら「この exe では
  `--ff_vocal_extract` は使えない。スクリプト版を使え」と言う
- `whisp_carrier.spec` の `excludes` に `audio_separator` を**常時**追加。
  FULL 分岐からも外した
- `WHISP_CARRIER_FULL=1` は **`--realign`（stable-ts）専用**になった

**実測結果。**

| 確認項目 | 結果 |
|---------|------|
| `audio_separator` の消滅 | 消えた。連鎖して **`librosa` `soxr` `scikit-learn` `msgpack` も消えた** |
| サイズ | 5.08GB → **5.06GB**（−20MB だけ） |
| 転写（`-m tiny`） | 3セグメント / 0.5s / exit 0。無影響 |
| `--realign` | **動作継続**（`[REALIGN] Done`） |
| `--ff_vocal_extract` | **新しいメッセージで停止** |

**サイズはほとんど減らなかった（−20MB）。** 理由は
`cv2`（112MB）`onnx` `pandas` `torchvision` `numba` `llvmlite`（115MB）
`sentencepiece` `tiktoken` `sqlalchemy` `scipy` が**残った**こと。
audio-separator を外しても、これらは別の何か（stable-ts / openai-whisper、
あるいは PyInstaller の torch フック）が参照していて落ちない。

**つまり除外の価値はサイズではなく、(a) 誤誘導するエラーが消えたこと、
(b) LGPL の `soxr` が配布物から消えたこと**の2点に絞られる。
`soxr` は**同梱物の中で唯一 LGPL だった Python パッケージ**なので、
copyleft の表記対象が ffmpeg と libsndfile だけになった。

サイズを本気で削るなら次に見るのは `cv2` と `llvmlite` で、合計227MB。
どちらも何が引いているのか未調査。

### score.py の dense 例表示を動確した（2026-09-03）

> **採点条件** 推論なし。既存の `_eval/hyp/*` に現行の `score.py` を当てただけ ／
> `[score] 4 file(s) scored | min gap 20s | block 30s | music cues excluded from CER | songs (instrumental + printed lyrics) excluded`
> ／ レポートは `_eval/score-models5.txt`

**[#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) で足した `dense` 検出器のうち、
例表示だけが未検証で残っていた。** 件数と集計は実測済みだったが、
**直前まで `dense` は種類フィルタに弾かれて1件も表示されず、それを直した直後**の
状態で止まっていた。STATUS に置いてあった確認コマンドをそのまま回した。

**通った。`dense` の例行がタイムスタンプ付きで出る。**

```
    kotoba-v2   loops 0 (0 chars) | >30s 0 (0s) | thin 0 | dense 5 (139 chars) | longest 16s | peak 500ch/s
      dense    0:02:38.0-0:02:39.9 17ch/s  あいえリンスターの赤ワインと代表この人がフェリシアさんのお父さんか
      dense    0:02:52.5-0:02:53.5 21ch/s  無理な条件での貨物がついているものですので
      dense    0:10:03.1-0:10:03.2 483ch/s そう考えると言いない人が会社を立て直したからじゃないかとね
```

| 確認項目 | 結果 |
|---------|------|
| `dense` の例行が出るか | **出る。** レポート全体で10行 |
| 4種類すべてが表示側を通るか | **通る。** 例行の種類別は loop 21 / thin 4 / over30s 3 / **dense 10** |
| 件数が期待値と合うか | **合う。** `kotoba-v2` の公女殿下 `dense 5`、`xxl` の死亡遊戯 `dense 7`。TOTAL は **xxl 8 / kotoba-v2 7（ピーク 500ch/s）対 既定 `ten-f1` 1** |
| **記録レポートの再現** | **`_eval/score-models5.txt` と 529行・54,082バイトでバイト一致**（`difflib` の差分0）。**記録は修正後のコードで作られている** |

**バイト一致が付いてきたのが収穫。** 「表示が直った」だけでなく
**「記録に残っているレポートが今のコードから再生成できる」**まで確認できた。
[#19](#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) で
18本の測定が無効になった件があるので、**採点コードとレポートの対応が取れていることは
それ自体が確認項目**になる。

**CLI の他の経路も叩いた。すべて期待どおり。** `--help` exit 0 ／ `--hyp` 未指定 exit 2 ／
存在しないディレクトリ exit 2 ／ 設定名の重複 exit 2 ／ `--only` で0件 exit 2 ／
単一設定では `paired diff` を出さない ／ `--report -` はファイルを書かない ／
`--keep-music` がヘッダに反映される ／ `--only` の後に `--exclude` が効く ／
参照VTTの無い素材は `reference MISSING` と TOTAL の一覧に出る。

**その副産物で不具合が2件出た。**
[既知の問題 6](HANDOVER.md#6-evalscorepy-の-cli-に不具合が2件記録2026-09-03) に置いた。
`--examples 0` が anomalies の例を1行出すこと（off-by-one）と、
**`--ref` が既定の `sample` を置換せず追加すること**（argparse の
`action="append"` + `default=[...]`）。**どちらも記録された数値には影響していない** —
`--examples` と `--ref` を使ったコマンドが `*.md` に1本も無い。

**教訓。件数を出す検出器を足したら、例の表示も同じ回で確認すること。**
`dense` は「見に行け」が仕事の検出器なので、**件数だけ出て場所が出ないなら
半分しか動いていない。** #27 はそこを次回送りにしていた。

### 1.0.0の初回ビルドが既定の実行で全部落ちた（Path(None)・2026-09-03）

**exe 限定。スクリプト版では出ない。** [#27](#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) で足した
`word_timestamp_guard()` の呼び出しが `Path(model_dir)` で包んでいて、
**`model_dir` が `None` のとき `TypeError` になる。**

```
File "whisp_carrier.py", line 1366, in main
    guard = whisp_models.word_timestamp_guard(resolved.path, Path(model_dir))
TypeError: expected str, bytes or os.PathLike object, not NoneType
[PYI-56120:ERROR] Failed to execute script 'whisp_carrier' due to unhandled exception!
```

**`--word_timestamps` は既定で有効なので、既定の実行が100%落ちる。**
`Model loaded` の直後に生のトレースバックで exit 1。

**なぜスクリプト版で出なかったか。** `model_dir` の決め方が

```python
exe_dir = Path(getattr(sys, "_MEIPASS", Path(sys.argv[0]).parent))
candidate = exe_dir / "_models"
model_dir = str(candidate) if candidate.exists() else None
```

で、**スクリプト版はリポジトリ直下を見るので `_models/` が実在して `str` が入る。
exe は `_MEIPASS`（展開先の一時ディレクトリ）を見るので `_models` が無く `None` のまま。**
**#27 の測定は `eval/run.py` 経由＝スクリプト版だったので、一度も踏んでいない。**

**修正は呼び出し側の1行。** `local_ct2_dir()` は `cache_root=None` を
「faster-whisper の既定」として正しく扱うので、包まずに渡す。

```python
guard = whisp_models.word_timestamp_guard(
    resolved.path, Path(model_dir) if model_dir else None)
```

**`cache_root` を渡す形にはしなかった。** `local_ct2_dir()` は
`download_model(cache_dir=...)` に流すので、**モデルを実際にロードしたときの
`download_root`（= `model_dir`）と同じ値でないと、照合先がずれる。**

> ## ⚠ これは[リリース前チェックリスト](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)の
> ## 項目4が捕まえた。**表そのものの妥当性の証拠になる**
>
> **項目15（`--help`）・項目16（`-m kotoba-v2`）と完全に同じ型。**
> **「開発機／スクリプト版では通る経路が、配布物では通らない」。**
> J は PATH の ffmpeg、N は CUDA Toolkit、これは `_models/` の実在。
> **3件とも「開発環境に在るものが、配布物の不備を隠す」形をしている。**
>
> **チェックリストに新しい項目は足さない。** 項目4がそのまま検出していて、
> **既定の実行で字幕が出ることを見る**という形が既に正しい。
>
> **追記（2026-09-05）。** 当初ここには「足すべきものがあるとしたら pytest 側で、
> `model_dir=None` を渡す純粋関数のテストになる」と書いていたが、
> **[pytest は入れないと決めた](HANDOVER.md#8-pytest-は入れない判断記録2026-09-05)。**
> **この件が検出手段の不足ではなく、既に在る項目4で捕まる例だった**ことが理由の1つ。
> 再検討するときの最初のケースが `model_dir=None` であることは変わらない。

---

## 検証済みのこと

- スクリプト版・exe版の両方で文字起こしが通る（`test_speech.wav`、7秒TTS）
- 設定ファイルの自動検出、`override` の両モード、プロファイル切り替え、
  型変換、不正キーのエラー化（合成データによる網羅テスト実施）
- 字幕整形の幅・行数上限、禁則処理、擬似単語経路、テキストの欠落なし（同上）
- `--batched`、`-f text` の拡張子表示、`--realign` のガード
- **exe の隣に置いた `whisp-carrier.yaml` の読み込み。** 再ビルド後の exe を
  起動して `[CONFIG]` 行と `vad_method = 'silero_v5'` の反映まで確認済み
  （[該当節](#設定ファイルが再ビルドで消える罠)）
- `whisp_vad_patch.py` の**配線**。合成データでクランプ・ソート・重複マージ・
  空区間の除去、パッチが `transcribe` 側の名前に当たって `vad` 側は無変更の
  ままであること、`with` を抜けたら復元されること。`test_speech.wav` で
  5通り（内蔵 / collect / clip / `--vad_onnx` 差し替え / 存在しない ONNX）を
  実行し、前4つは同一のタイムスタンプ、最後は exit 2 とエラーメッセージを確認
- `eval/` 一式。`arib_vtt.py` は実素材3本でルビ分離・DRCS・消去キュー・
  ギャップ抽出を確認。`normalize.py` は編集距離の既知ケースと、
  実キャプション文字列に対する3段階の変換を確認。`prep.py` は10本の調査と
  WAV抽出（duration のずれは全ファイル ±0.03秒以内）。`run.py` + `score.py` は
  24分もの9本 × 8設定と一挙1本 × 2設定を large-v3 で通した
- **`score.py` の参照不要指標4つの例表示と CLI の分岐。** 4種類（loop / over30s /
  thin / dense）すべてがタイムスタンプ付きで出ること、記録済みレポート
  `_eval/score-models5.txt` が現行コードからバイト一致で再生成できること、
  異常系4つが exit 2 になること（[該当節](#scorepy-の-dense-例表示を動確した2026-09-03)）。
  **`--examples 0` と `--ref` に不具合が残っている**
  （[既知の問題 6](HANDOVER.md#6-evalscorepy-の-cli-に不具合が2件記録2026-09-03)）
- `subtitle_format.sanitize_segments()`。合成データ12ケース（無変更時の
  同一オブジェクト返し、120秒ポーズの分割、3分割とID再採番、単語なしの
  クランプ、`--max_gap 0` でも30秒保証、ポーズ4つの全分割、テキスト欠落なし、
  ログの静音条件）。実素材9本 × 4設定で 30秒超が 0 になることも確認
- `normalize.edit_distance_large()`（全文CER用の帯状DP）。ランダム300件で
  厳密 Levenshtein と完全一致、長さが10倍違うケースと空文字列も一致。
  5000字で2〜13秒。**上限値判定の追加後に再検証**（ランダム300件が
  すべて `exact=True`、帯を意図的に絞った場合に `exact=False` と
  上限値、帯を戻すと厳密解に一致、全幅にクランプされる経路も厳密扱い、
  `CerResult.approximate` への伝播）
- `normalize.lcs_large()`（カバレッジ／精度用の帯状LCS）。既知値、
  ランダム300件と厳密LCSの完全一致、120字ペアと800字ノイズ入りペア、
  共通接頭辞・接尾辞の剥がしが答えを変えないこと、`min(len)` と編集距離との
  整合、そして**帯が拘束するケースの検出**（前後を入れ替えた500字ブロック2つ。
  最適経路が対角線から500ずれるので、帯64では64しか拾えず `exact=False`、
  帯を戻すと厳密に500で `exact=True`）。`CerResult` への伝播も含め14項目
- anime-whisper と large-v3 の比較。9本すべてで large-v3 が上（測定結果 #4）
- **一挙放送（5時間22分）の測定。** clip / collect の2設定を large-v3 で
  実測（各17〜20分）。単語タイムスタンプは5:21:51まで到達して崩れず、
  `sanitize_segments()` も collect 側で54箇所を分割して最長190s→28s。
  採点は両設定とも厳密解（帯の上限値ではない）
- **全文CERの2つの実装欠陥の修正と再採点。** ブロック外の素材の混入と
  帯の打ち切りを直し、24分もの9本4設定を再採点。per-block は全設定・
  全ファイルで完全に不変、全文CERのみ0.1〜0.8pt改善。結論は不変（測定結果 #6）
- **`--vad_segment_mode` の既定を `clip` に変更後の動作。** `test_speech.wav` で
  既定（`clip`）と明示 `collect` の両方を実行し、SRTが完全に同一。
  `[VAD]` 行がそれぞれ `via clip_timestamps` と
  `routed through collect_chunks` になることも確認
- **`novad` 対照の実測。** 24分もの9本を `--vad_filter false` で通した
  （1本70〜103秒、合計約15分）
- **VADパラメータのスイープ。** `--vad_threshold` 0.30 / 0.15、
  `--vad_min_silence_duration_ms` 500 / 100、`--vad_speech_pad_ms` 200 を
  それぞれ9本で実測。**どれも既定を上回らず、結論は否定的**（測定結果 #8・#9）
- **`vad.py` の `window_size` の配線。** silero-vad パッケージ経路では
  `window_size_samples` を渡していないことをコードで確認。
  silero-vad 6.2.1 の同引数の既定は512（`inspect.signature` で確認）
- **本家 Faster-Whisper-XXL r245.4 との比較。** 24分もの9本を同一モデル
  （large-v3）で実測し、`eval/xxl_convert.py` で採点パイプラインに載せた。
  あわせて **RTX 5090 では float16/int8 系が cuBLAS で落ち、float32 のみ動く**
  ことを確認（測定結果 #10）
- **`eval/run.py` の進捗ストリーミング。** `test_speech.wav` で出力と
  ログ形式を確認。9本のnovad実測で30秒ハートビートが実際に出ることも確認
- **`loop_filter.py`（測定結果 #12）。** 合成データで検出6件・保持9件を確認
  （`ああああ…` `うっうっ…` `チュー×6` `やったーーー` `あ、あ、あ、…`
  `よーし、よーし、…` を落とし、`きゃあああああ` `そそそそんなわけ` `ええええ!?`
  `うわああああ` `いやいやいや` `それはそれは大変でしたね` `ハァ…ハァ…`
  通常文・空文字列を残す）。`filter_segments()` は削除ゼロなら同一オブジェクトを
  返し、空リストも通る。**実素材9本で採点用検出器との差は4件だけで、
  すべて「実装が残す」向き**（`_eval/_detector_diff.txt` で全件を目視）。
  `process_single_file` への配線は、合成セグメントを注入して
  `[LOOP] 4 -> 2 segments` 行の出力・JSONから該当セグメントが消えること・
  `--loop_filter false` で4件全部残ることを確認
- **`eval/strip_loops.py`（測定結果 #11）。** 落としたセグメント数と文字数が
  `score.py` の集計と一致することを3設定で確認（clip 11本1363字、
  novad 21本3417字、XXL 8本53字）。同一ディレクトリ指定を拒否すること、
  `duration` / `language` がそのまま通ること、削除がゼロなら出力が入力と
  同じ内容になることも確認
- **コンテナ等価性。** MKV直渡しの出力が `clip-fixed` と **MD5一致**
  （[該当節](#コンテナ等価性mkv直渡しでバイト一致)）
- **ffmpeg の出所。** zip の SHA-256 が3経路で一致。サイズも一致。
  configure 行に `--enable-gpl` / `--enable-nonfree` が無いことを確認。
  記録は `_tools/ffmpeg/PROVENANCE.txt`
- **spec のライセンスガード。** spec 本体を実際に exec して3ケース確認
  （PyInstaller が注入する名前をダミーで与え、テスト対象が本番コード
  そのものになるようにした）。公式LGPL版は `verified`、
  XXL の GPL 版を環境変数で指定すると **SystemExit で中止**、
  存在しないパスは次候補へフォールバック。ライセンス文3点が exe の隣に
  配置されることも確認
- **終了コード。** 不正な wav に `--ff_loudnorm` を当てて ffmpeg を失敗させ、
  スクリプト版と exe の両方で確認。正常1本は exit 0、失敗1本は exit 1、
  **正常+失敗の2本では正常側を出力しつつ exit 1**
- **フィルターの順序修正と段検査。** ステレオ素材を `-ac 2` で生成して確認
  （モノラルでは FC が恒等変換になり 2-a が再現しない）。`--ff_fc` は
  `peak=-91.0 dB` を検出して exit 1、`--ff_lc` と `--ff_invert` は
  **修正前 -91dB だったものが通る**。4段チェーンで段ごとの `[FF]` 行が
  出ることも確認
- **出力エンコーディング（UTF-8化）。** パイプ上のバイト列を直接確認し、
  Amatsukaze のデコード（UTF-8 → UTF-16 → cp932）を再現して2条件で検証。
  既定は `e3 83 ad ...` で `'ロック' x4 spanning 12 chars: ロック...` と読め、
  `PYTHONIOENCODING=cp932` では `\u30ed...` にフォールバックする。
  **どちらも `?` が出ない。** Amatsukaze 側が UTF-8 前提であることは
  `TranscodeManager.cpp` / `Subtitle.h` / `ProcessThread.cpp` /
  `StringUtils.cpp` の4ファイルで確認（`isUtf8Log = true` は固定値）
- **`console_safe()` の通し忘れ修正。** `loop_filter.reason()` が `repr()` 経由で
  生の日本語を返すことを確認し、`why` を通す形に修正
- **回帰確認。** `test_speech.wav` を `-m tiny --no_config` で実行し、
  3セグメント / `All done` / exit 0（UTF-8化の前後で不変）
- **設定ファイルの再ビルド耐性。** spec の退避・復元でファイルが残り
  内容が一致し、**その exe を起動して `[CONFIG]` が出ることも確認済み**
- **Amatsukaze連携。** 24分の本編を実運用で通した。`[CONFIG]`
  `[VAD] via clip_timestamps` `[LOOP] dropped 5` `[FMT]` が出て exit 0
- **実運用設定の精度検証（#13）。** beam 10 + best_of 10 + language ja で9本を
  実測し `clip-prodloop`（beam 5）と比較。**9本中8本で同等または悪化**、
  合計 22.0% → 22.3%。時間は同一ファイルで 131.3s → 135.0s（+2.8%）
- **`--language ja` の無影響（#13）。** `prod-lang` と `clip-prodloop` が
  **9本すべてで (start, end, text) 完全一致。** 比較は中身で行うこと
  （`clip-prodloop` は `strip_loops.py` が書いた JSON なのでバイトは全部不一致になる）
- **yaml の CLI 上書き。** whisper-option 欄に `--beam_size 10` が入っている条件で
  `beam_size = 5 (overrides CLI 10)` を実機確認（欄の中身は名残で、空にしても既定が 5）
- **追加素材5本（#14）。** `prep.py` で MISSING も重複警告もなく WAV 抽出、
  記録値と同条件で推論、24分もの4本と一挙を分けて採点
- **一挙2本目（マジルミエ 4h52m）。** 全文CER **17.3%**、カバレッジ85.4%、
  **30秒超0件**、ループ抑制後29字。**長尺劣化が無いことの2例目**
- **子供向け4本での XXL 直接比較（#14）。** XXL r245.4 を `-ct float32` で
  当て `xxl_convert.py` で採点。**カバレッジ 69.1% 対こちら 67.7% で、
  取りこぼしが実装非依存であることを確定。** XXL は 30秒超4件185秒・thin 10件・
  ループ482字を出し、構造的欠陥ではこちらが大幅に良い
- **UTF-8化が実パイプラインで機能。** `eval/run.py` 経由のログに
  `'ロック' x4 spanning 12 chars: ロック ロック ロック ロック` と日本語で出た
  （修正前は `'???b?N'`）
- **`condition_on_previous_text=true`（#15）。** 死亡遊戯 #09 に当てて実測。
  VADは両条件とも51区間で、変えたのはこの1つだけ。
  **カバレッジ 59.7% → 21.3%、推論 65.4s → 384.2s、出力が 18:16 で止まる**
  （既定は 27:05 まで出す。尺30:00）。
  `loop_filter` の落とし件数は0（縮退がセグメントをまたぐため対象外）。
  遅延が温度フォールバックでないことは `--temperature` の既定がスカラー0
  であることをコードで確認
- **実運用条件7本の再採点（推論なし）。** 現行既定（`clip-prodloop`）で
  全文CER **15.6%** / カバレッジ 86.8% / 精度 91.2% / per-block 17.2%。
  ループ抑制前（18.4% / 86.8% / 87.8% / 20.4%）から**カバレッジは完全に不変**で
  精度だけが上がる。判定も `both` から `MISSED SPEECH dominates` に変わった。
  レポートは `_eval/score-nosong-prodloop.txt`
- **健全6本に novad を当てたコスト（推論なし）。** `novad-prodloop` を
  `strip_loops.py --detector production` で生成して直接採点。
  **全文CER 13.2% → 15.5%、per-block 14.6% → 15.9%、無字幕域への出力が2.7倍。**
  損失は2本に集中し（終末ツーリング +12.2pt、クレバテス +6.6pt）、
  4本はむしろ改善する。**終末ツーリングでは production 検出器が1件も落とせない**
  ことも確認（`strip_loops` の出力で dropped 0）。
  レポートは `_eval/score-novad-clean6.txt`。
  **なお終末ツーリングの損失は #16 で歌唱由来と判明し、符号が反転した**
- **`normalize.is_sung()` と `--exclude-songs`（#16）。** 参照15本すべてに当てて
  誤爆を確認（落ちるのは参照の1.6%、健全な会話劇は0字、誤爆は長尺2本の
  引用台詞で合計0.02%）。**終末ツーリングは0字で330.7秒のブロックが落ちる**
  ことも確認。除外は `build_blocks()` がブロックを落とし
  `score_cer_whole()` が生き残ったブロックからしか仮説を拾わないので**対称**。
  既定はオフなので**既存の記録値はすべて再現できる**
- **歌唱除外での3者比較（#16、推論なし）。** clip 19.7% / novad **19.3%** /
  XXL 21.4%、per-block 20.9% / 20.8% / 25.4%。
  ファイル別で clip が XXL に **8勝1敗**、novad が **7勝2敗**。
  レポートは `_eval/score-nosing.txt`
- **TEN VAD バックエンド（#18）。** `--vad_method ten` として追加。
  区間の組み立ては silero の後処理を移植した `segments_from_probabilities()` で、
  **silero 側のコードには触っていないので記録値は動かない。**
  死亡遊戯1本での閾値スイープ（0.45 / 0.60 / 0.75 / 0.90）と、
  4本での clip / novad / xxl 対照を実測。**ten 0.75 が CER・カバレッジ・
  per-block のすべてで最良**（4本合計 24.1% 対 既定 27.2% 対 XXL 27.6%）。
  **既定は変えておらず `requirements.txt` にも入れていない**
- **`precomputed` バックエンドと `eval/ext_vad_dump.py`（#18）。**
  主環境に入れられないVADを別venvで走らせ、区間をJSON経由で受け取る経路。
  **区間は16msフレーム格子にラスタライズして他バックエンドと同じ集約を通す。**
  inaSpeechSegmenter 0.8.0 と FSMN-VAD（funasr-onnx 0.4.2）で実測し、
  4本で **fsmn 25.4% / inass 50.5%**（ten 24.1% / silero 27.2%）。
  **INASS はアイプリの78%を music と判定するのでこの素材には使えない**
- **`--vad_neg_threshold` の配線と実測（#17）。** 両経路（`vad.py` の silero
  バックエンドと faster-whisper の `VadOptions`）に通した。
  受け付け可否は署名で確認済み（silero-vad 6.2.1 と `VadOptions` の両方に
  `neg_threshold` がある）。**指定しないときは送らないので既定の挙動は不変。**
  4本 × 3値を実測し、**全振り幅 0.4pt・非単調で否定的。**
  区間数の応答（健全側は下限で3〜4割まとまるのに死亡遊戯だけ動かない）から
  **死亡遊戯の取りこぼしは検出側の失敗**と確定。
  レポートは `_eval/score-negthr.txt`

## 未検証のこと

- `vad.py` の凍結時エラーメッセージ（`_missing_backend`）— コード読みのみ。
  そもそも `auditok` と `webrtcvad` は未インストールなので実行経路自体が未確認
- `--batched` の速度・品質評価（動作はする）
- **anime-whisper の単語タイムスタンプの長尺での精度。** 2層デコーダの
  1層から取った値なので、5時間スケールで崩れないかは未確認。
  `sanitize_segments()` がこの値に依存して分割するので、崩れると分割位置も狂う。
  large-v3 については一挙で確認済み（崩れなかった）
- **`--vad_onnx` に実際に別バージョンの ONNX を渡した場合。** 検証は同梱の
  v6 を明示指定して「差し替え機構が動く」ところまで。v5 の ONNX は未入手
- **exe 版での `--vad_onnx` のパス解決**（`whisp_config.base_dir()` 経由、
  コード読みのみ）
- **一挙での anime-whisper・内蔵VAD・novad。** 一挙は clip / collect の
  2設定しか回していない。3経路の比較とモデル比較、novad対照は24分ものだけ
- **推奨設定同士での本家比較。** 測定結果 #10 は既定同士のみ。
  ただし**子供向け4本では実施済み**（#14。こちらが2.2pt良い）
- **AACコンテナを直接渡した経路。** MKV直渡しは等価と確認済みで、
  実運用のAACは同じビットストリームをコンテナから剥がしただけなので差は
  出ないはずだが、実際には流していない
- **音声フィルターを外した条件でのAmatsukaze連携**（[次の着手 A](#a-amatsukaze連携テストの再実行フィルターを外した条件)）
- **`novad` を発動条件付きで既定に混ぜた条件。** 片側ずつは測り終えた
  （狙う2本で大きく勝ち、健全6本では合計 +2.3pt 負ける。
  [次の着手 C](#不要になったc-死亡遊戯の取りこぼし)）。
  **残っているのは混成の9本合計**で、`novad-prodloop` は生成済みなので
  2本だけ差し替えて採点すれば推論なしに出る
- **TEN VAD の残り5本・長尺・exe同梱**（[次の着手 E](#済みe-ten-vad-の検証を広げる)）。
  測ったのは4本のみ。**`ten 0.90` を4本に当てた条件**も死亡遊戯1本しか無い
- **混成を混成として採点した数字。** [#16](#16-歌唱区間を除外したら構図が変わったexclude-songs)の
  18.4% は参照文字数での加重計算で、レポートとして出したものではない。
  **ただし [#18](#18-ten-vad-が-silero-を置き換えた取りこぼしは実装で直せる) で混成そのものが不要になる見込みなので優先度は下がった**
- **推奨設定同士での本家比較。** [#16](#16-歌唱区間を除外したら構図が変わったexclude-songs)も
  こちら側は整形なし・XXL側は既定のまま。`--standard_asia` を入れた条件、
  XXL側の `--sentence` / `--ff_rnndn_sh` を入れた条件は未測定
- **`neg_threshold` × `min_silence_duration_ms` の2次元。**
  1次元は振り終えて否定的（[#17](#17---vad_neg_threshold-も-knob-ではなかったvad空間は本当に打ち止め否定的結果)）。
  `min_silence` 3000ms が終話判定を支配してヒステリシスの余地を潰している
  可能性は残るが、`min_silence` を下げる方向は単独で悪化する（[#9](#9-vadパラメータ空間は打ち止め既定が最良だった否定的結果)）
- **clip 経路の発話秒数のログ。** 発動条件に使う発話率が運用ログから読めない。
  上の発話率はすべて出力占有からの近似値で、VADの検出発話そのものではない
- **死亡遊戯のループが他の素材で再発するか。** ニンジャラは別話で再発を確認したが
  （#14）、死亡遊戯型は1本しかない
- **`--vad_window_size_samples` を実際に効かせた条件。** 通常経路では配線
  されていないので、torch.hub フォールバックを強制しないと試せない。
  silero既定512で動いている現状が悪いという証拠は無いので優先度は低い

**この節から落とした項目**（解決したもの）。

- ~~問題の3本に局や番組属性の共通項があるか~~ → **内容側で確定**（#14）。
  局のメタデータを調べる必要はなくなった
- ~~ニンジャラのループが他の素材で再発するか~~ → **別話で再発した**（444字、#14）
- ~~一挙が1本しかない~~ → **2本目を測った**（マジルミエ 4h52m、17.3%）
- ~~実運用設定（beam 10）の精度~~ → **測った。悪い**（#13）
- ~~死亡遊戯で `condition_on_previous_text=true` を試した条件~~ →
  **測った。破壊的**（カバレッジ 59.7% → 21.3%、#15）
- ~~`--vad_neg_threshold` を露出した条件~~ → **露出して振った。否定的**（#17）。
  **これでVADパラメータは4つすべて振り終えた**

---

## 解決済み（旧版からの変更点）

- **プロジェクト名の不統一** — spec の `pathex` がハードコードされた旧パスを
  指していたのを `SPECPATH` に変更（フォルダ名変更に追従する）。
  git remote を `whisp-carier` → `whisp-carrier` に修正。
  `.bat` の Python フルパス決め打ちを、環境変数 → `%LOCALAPPDATA%` →
  `py -3.11` → `python` のフォールバックに変更。
- **`--initial_prompt` のデフォルト** — 文字列 `"auto"` が実際にプロンプトとして
  渡っていた問題を修正。
- **字幕整形の未実装** — `--standard` / `--standard_asia` / `--sentence` /
  `--max_line_width` / `--max_line_count` / `--max_gap` はプリセットが値を
  代入するだけで writer 側が読んでいなかった。`subtitle_format.py` として実装。
  プリセットは明示指定された値を上書きしないようにしたので、
  `--standard_asia --max_line_width 20` は 20 が残る。
- **多行キューによる出力破壊** — 整形で行内に改行が入るようになったため、
  TSV は列崩れ、LRC は行崩れを起こす。`one_line()` で TXT / TSV / LRC を
  1行に畳むようにした（SRT / VTT は改行をそのまま使う）。
- **spec の ffmpeg パス** — Downloads 配下の決め打ちパスが消えると
  `if exists()` で無言にスキップされ、ffmpeg 未同梱のexeができる状態だった。
  環境変数 → 決め打ち → PATH の順に探し、見つからなければビルドを止める。
  その後 GPL 版を引いていた問題も含めて[該当節](#ffmpeg-を-gpl-v3-から公式-lgpl-v3-に差し替えた)で解決。
- **`--batched` が必ず TypeError になる問題** — `batch_size` を
  `WhisperModel.transcribe()` に渡していた。同メソッドに `batch_size` は無く
  `**kwargs` も受け取らない。`BatchedInferencePipeline` でモデルを包む形に変更。
  なお batched はVADチャンクのまとめ方が違うため、セグメント数が変わる
  （テスト音声で 3 → 1）。速度差と品質差の評価は未実施。
- **宣言だけで配線されていなかったオプション** —
  `--max_new_tokens` / `--chunk_length` / `--hotwords` を `transcribe_kwargs`
  に配線した。`--hallucination_silence_th_temp` と `--model_preload` は
  faster-whisper 側に対応する機能が無い、あるいは常時プリロード済みで
  意味を持たないため、Faster-Whisper-XXL のコマンドライン互換のために
  受け取って無視することをヘルプに明記した。削除すると既存のAmatsukaze
  オプション文字列が壊れるため残した。
- **`[OUT]` の拡張子表示ずれと重複書き込み** — 表示が
  `base_out + f".{fmt}"` で組まれていたため `-f text` が `.text` と出ていた。
  `FORMAT_EXTENSIONS` を単一の真実の源にし、`write_outputs()` が実際に
  書いたパスを返してそれを表示するようにした。あわせて `-f txt text` や
  `-f all srt` のような指定で同じファイルを二度書く問題も解消。
- **`--postfix` のデッドコード** — if/else が両方同じ処理だったので削除。
- **`--realign` が字幕整形を破棄する問題** — 整形オプションが有効なときは
  realign をスキップして理由を表示する。SRT未出力時も無言ではなく理由を出す。
- **exe に設定ファイルのサンプルが入らない問題** — `datas` 経由だと onedir では
  `_internal` 配下に入ってしまい、設定の自動検出が見る「exeの隣」に届かない。
  spec の COLLECT 後に exe の隣へコピーするようにした。
- **exe が起動しない問題** — `whisp_carrier.spec` で新規ビルドしたら解決した。
  原因は現存しない `whisper_xl.spec` 側にあった（[既知の問題 4 の details](HANDOVER.md#4-exe化しても一部機能は動かない)）。
- **30秒超セグメント（既定経路の既存バグ）** — `sanitize_segments()` で修復。
  詳細は[測定結果 #1](#1-既定経路に30秒超セグメントの欠陥があった既存バグ修正済み)。
- **`--vad_segment_mode` の既定が測定結果と食い違っていた問題** — `collect` を
  既定にした根拠（ゼロ埋めが幻覚を誘発する）が実測で逆向きに否定されたので
  `clip` に戻した。ヘルプと `[VAD]` 行の文面も、否定された主張を
  述べないように直した（[測定結果 #6](#6-一挙放送で-clip-が勝ち既定を-clip-に戻した)）。
- **ループ出力が本家に対する唯一の負け要因だった問題** — `loop_filter.py` を足し、
  `process_single_file` で字幕整形の前に通すようにした（既定有効、
  `--loop_filter false` で無効）。全文CER 24.3% → 22.0%、per-block
  26.0% → 23.4% で、**本家XXL（23.7% / 27.8%）を上回った。**
  健全6本の出力は1文字も変わらない（[測定結果 #11](#11-ループを潰した場合の上限既定に載せるだけで本家を抜く)・[#12](#12-実装した検出器は上限にそのまま到達した誤爆ゼロで)）。
- **フィルターのチャンネル選択が無音を作っていた問題** — `extract_audio()` に
  統合して順序を保証し、段ごとの計測と検査を入れた（[既知の問題 2](HANDOVER.md#2-音声フィルターが実用レベルに達していない)）。
- **ファイル処理の失敗が exit 0 になっていた問題** —
  [該当節](#終了コードの修正ファイル処理の失敗を非ゼロにした)。
- **実運用が測定より悪いデコード設定で動いていた問題** —
  Amatsukaze の whisper-option 欄に入っていた `--beam_size 10 --best_of 10` は
  9本中8本で beam 5 と同等または悪化していた
  （合計 22.0% → 22.3%、時間は+2.8%）。**yaml で 5 に戻した**（`override: true`
  なので欄は無変更）。`whisp-carrier.yaml.example` も揃えた。
  詳細は[測定結果 #13](#13-実運用設定beam-10は測定設定beam-5よりわずかに悪い)。
  **訂正（2026-08-22）。「Amatsukaze が渡してくる」と書いていたが、
  欄に入っていたのは以前の実験の名残で、ソフトが強制していたわけではない。
  欄を空にすれば既定の 5 が入るので、本来は yaml 側の対処も不要。**
- **ログの日本語が文字化けする問題** — パイプに cp932 を書いていたのが原因で、
  Amatsukaze は UTF-8 でデコードする（ソースで確認）。`_setup_console_encoding()`
  で stdout / stderr を UTF-8 に固定し、`console_safe()` は「UTF-8 以外の時だけ
  エスケープ」に変更、`console_safe(why)` の通し忘れも修正。
  **入力パスや `[OUT]` など `console_safe()` を通していない行も同時に直った。**
  [該当節](#ログの文字化け原因は自分の出力エンコーディングだった解決済み)。
- **再ビルドで `whisp-carrier.yaml` が消える問題** — spec が退避・復元する。
  [該当節](#設定ファイルが再ビルドで消える罠)。
- **全文CERがブロック外の素材を課金していた問題（eval）** — `score_cer_whole()` が
  「最初のブロック開始〜最後のブロック終了」で仮説を拾っていたため、一挙の
  話間47分43秒（OP・ED・次回予告・提供）が挿入として計上され、CERが14pt
  膨らんでいた。ブロックに重なるセグメントだけを拾う形に変更。
- **帯状DPの打ち切りが黙って上限値を返していた問題（eval）** —
  幅を上限にクランプし、返り値を `(距離, 厳密か)` に変え、
  `CerResult.approximate` 経由でレポートに `UPPER BOUND` を出す。
  `max_band` は8192→32768。
- **CERが取りこぼしと誤認識を区別できなかった問題（eval）** — 帯状LCS
  （`normalize.lcs_large`）を足して カバレッジ＝LCS/参照長、
  精度＝LCS/仮説長を出すようにした。判定を一言添え、帯が拘束した場合は
  `LOWER BOUND` と表示する。`score.py` に `--only` / `--exclude` も足した。
- **`eval/run.py` が長尺で無音になる問題** — `capture_output=True` で
  子プロセスの出力を溜め込んでいたため、5時間22分のファイルでは
  `[VAD]` 行から `[STT] Done` まで約20分間なにも表示されなかった。
  `Popen` で行単位に読みながら表示とログ書き込みを行う形に変更し、
  30秒間なにも出ていなければ直近の行をハートビートとして出す。
  stderr は stdout にマージするのでログの並びが実際の時系列になる。

---

## テスト結果サマリ

| 素材 | オプション | セグメント | 時間 | 備考 |
|------|-----------|-----------|------|------|
| サイバーフォーミュラ（25分） | `--beam_size 10 --best_of 10` | 513 | 465s | 会話多い |
| ワールドイズダンシング（30分） | `--beam_size 10 --best_of 10` | 200 | 55s | ダンスシーン多い |
| テスト音声（7秒TTS） | デフォルト | 3 | 1.4s | 正常動作確認 |
| テスト音声（7秒TTS） | `sentence` + `width=12` + `count=2` | 3キュー | 1.1s | 字幕整形の動作確認。SRTが2行組みになり、TSV/TXTは1行に畳まれる |
| テスト音声（7秒TTS） | `-m anime-whisper` | 1 | 0.6s | 変換7.6s（HFキャッシュ済みの状態）。単語タイムスタンプ正常 |
| テスト音声（7秒TTS） | `-m anime-whisper --standard_asia` | 1→3キュー | 0.4s | `…` と `。` で分割され、各行16字以内 |
| テスト音声（7秒TTS） | `--batched` | 1 | 0.6s | 動作するがVADチャンクのまとめ方が変わりセグメントが粗くなる（3→1） |
| テスト音声（7秒TTS） | `--vad_method silero_v5_fw`（内蔵・基準） | 3 | 1.5s | tinyモデル。区切りは 0.000 / 1.620 / 4.660 |
| テスト音声（7秒TTS） | `--vad_method silero_v5 --vad_segment_mode collect` | 3 | 2.2s | 外部VAD 1チャンク 7.1s を collect_chunks 経路へ。基準と同一 |
| テスト音声（7秒TTS） | `--vad_method silero_v5`（既定 = `clip`） | 3キュー | 2.0s | 明示 `collect` とSRTが完全に同一。`[VAD]` 行は `via clip_timestamps` |
| テスト音声（7秒TTS） | `--vad_onnx <同梱v6>` | 3 | 0.9s | 差し替え機構の動作確認。基準と同一 |
| — | `--vad_onnx does_not_exist.onnx` | — | — | exit 2 とパス付きエラー |
| テスト音声（7秒TTS） | `-m tiny`（ループ抑制の素通り確認） | 3 | 0.6s | `[LOOP]` 行が出ないこと＝落とすものが無ければ無音であることの確認 |
| 合成セグメント4件 | 既定（`--loop_filter true`） | 4→2 | — | `[LOOP] 4 -> 2 segments` と `dropped 2 (36 chars of repetition)`、理由行2行 |
| 合成セグメント4件 | `--loop_filter false` | 4→4 | — | `[LOOP]` 行なし。全件保持 |
| テスト音声（7秒TTS） | exe版 `-m tiny` | 3 | 0.9s | 区切り 0.000/1.620/4.660。ffmpeg 同梱・ライセンス4点の配置・exit 0 |
| テスト音声（7秒TTS） | exe版 + 設定ファイル（`override: true`） | 3キュー | 1.0s | `[CONFIG]` 6行・`vad_method: silero_v5` の反映・字幕整形・exit 0 |
| テスト音声（7秒TTS） | exe版 `-m anime-whisper` | — | — | exit 2。スクリプト版での変換手順を案内して停止（transformers 未同梱） |
| テスト音声（7秒TTS） | **フルビルド** `-m tiny` | 3 | 0.5s | `WHISP_CARRIER_FULL=1`。5.06GB |
| テスト音声（7秒TTS） | **フルビルド** `--realign` | 3 | — | stable-ts が動作。`[REALIGN] Done` / exit 0。**初の確認** |
| テスト音声（7秒TTS） | **フルビルド** `--ff_vocal_extract mb-roformer` | — | — | `_load_separator()` が「この exe では使えない、スクリプト版を使え」と説明して停止 |
| 不正wav（テキストを`.wav`に） | `--ff_loudnorm`（ffmpegを失敗させる） | — | — | `Failed: 1 of 1 file(s).` + ファイル名を stderr に出して **exit 1**。修正前は `All done` / exit 0 |
| 不正wav + 正常音声の2本 | `--ff_loudnorm` | 3（正常側のみ） | — | 正常側は `[OUT]` を出し、末尾に `Failed: 1 of 2 file(s).` で **exit 1**。バッチは止まらない |
| テスト音声（7秒TTS） | `--ff_loudnorm`（段計測の確認） | 3 | 0.6s | `extract: peak=-1.2dB mean=-20.4dB` → `loudnorm: peak=-1.5dB mean=-16.9dB`。手測値と一致 |
| **ステレオ**（`-ac 2` で生成） | `--ff_fc` | — | — | `peak=-91.0 dB` を検出して **exit 1**。「FCは5.1系にしか無い」と原因表示。**修正前は無音を黙って転写していた** |
| **ステレオ** | `--ff_lc` | 3 | 0.5s | `peak=-4.2dB` で通る。**修正前は -91dB** |
| ステレオ（左右非対称に生成） | `--ff_invert` | 3 | 0.5s | `peak=-1.1dB` で通る。**修正前は -91dB** |
| ステレオ | `--ff_fc --ff_lc` | — | — | 排他エラーで **exit 1**（別チャンネルの同時選択） |
| テスト音声（7秒TTS） | `--ff_gate --ff_speechnorm --ff_loudnorm --ff_lowhighpass` | 3 | 0.5s | 4段すべて計測。`lowhighpass` は mean を0.1dBしか動かさず、`gate` は完全に不変 |
| **MKV直渡し**（クレバテス #06） | `--config ext-clip` 相当 | 355 | 131.3s | **`clip-fixed` の同ファイルとMD5一致。** コンテナ等価性の確認 |
| **Amatsukaze 実運用（24分本編・中間AAC）** | Amatsukaze の引数 + yaml | 292→287→325キュー | 265.2s | **連携テスト初回。** `[CONFIG]` `[VAD] via clip_timestamps` `[LOOP] dropped 5 (333 chars)` `[FMT]` すべて出て exit 0 |
| パイプ経由（Amatsukazeの条件を再現） | 既定（UTF-8化後） | — | — | パイプ上が `e3 83 ad ...`＝UTF-8。Amatsukaze のデコードを再現して `'ロック' x4 spanning 12 chars: ロックロックロックロック` と**日本語で読める** |
| 同・`PYTHONIOENCODING=cp932` | `console_safe()` のフォールバック | — | — | `\u30ed\u30c3\u30af` にエスケープされ、**`?` にならない**。「書けるか」判定だった版ではここが化けた |
| テスト音声（7秒TTS） | `-m tiny --no_config`（UTF-8化の回帰確認） | 3 | 0.9s | `All done` / exit 0。正常系は不変 |

この表の `-m anime-whisper` は英語TTS音声に対する動作確認（日本語専用モデルなので
カタカナで書き起こされる）。**アニメ実素材での CER 比較は測定結果 #4 にある。**
モデル解決の各分岐（エイリアス→CT2そのまま渡し / ビルトインサイズ /
ローカル変換済みディレクトリ / 未知のHFリポジトリからの変換）はすべて通した。

**VADの表の各行が同じタイムスタンプになっているのは想定どおり**で、
7秒では発話が1チャンクに収まりパディングの影響が出ないため。
**この表が示しているのは配線の正しさだけで、`collect` と `clip` の品質差ではない。**
品質の比較は実素材で実施済み（測定結果 #2・#6）。

---

---

### 0.9.0 公開までの経緯（2026-08-23 の記録）

**これは 0.9.0 を公開した時点の記録。** 現在地は [STATUS.md](STATUS.md) を見ること。
この直後のセッションで [J](#j-同梱-ffmpeg-が使われていなかった2026-08-23) と [K](#k-torch-を外して配布サイズを削る2026-08-23) が見つかり、0.9.1 になった。

1. **[配布アーカイブを公開した](#配布アーカイブ2分割で作った2026-08-23)。**
   4900 MiB → 1999 MiB の2分割で、両方とも GitHub の2GB上限未満。
   **3回固め直している**（1回目は古い docs 込み、2回目は版を 0.9.0 に上げたため破棄）。
   SHA-256 は該当節にある
2. **[版を 0.1.0 → 0.9.0 にした](HANDOVER.md#なぜ-10-ではなく-090-なのか)。**
   精度と機能は出揃っているが **1.0 は「CLI 表面を維持する」約束**なので、
   現行 exe の第三者確認が無い段階では早いと判断した。
   **1.0 の条件は「現行 exe が他人の環境で1回動くこと」**
3. **[ドキュメントを再編した](HANDOVER.md#ドキュメント構成readme-は日本語がトップ開発版は-handover-に寄せた)。**
   `README.md` が日本語（**exe 版のマニュアル**）、`README_en.md` が英語。
   **スクリプト版のコマンドは README から全部落として HANDOVER に寄せた**
   （配布物を解いた利用者に `pip install` は実行できない指示だから）
4. **運用ルールを2つ決めた。**
   [再パックの発動条件](HANDOVER.md#再パックの発動条件ドキュメントだけの変更では固め直さない)（ドキュメントだけの変更では固め直さない）と、
   [版番号とタグの運用](HANDOVER.md#版番号とタグの運用スクリプト版だけの修正では版を上げない)（版は exe に紐づく。
   スクリプト版だけの修正では版を上げない）。**どちらも1日で3回パックし直した反省から**
5. **[参考プロジェクト表と THIRD-PARTY-NOTICES の欠落を直した](HANDOVER.md#参考プロジェクト表と-third-party-notices-の欠落を直した2026-08-23)。**
   **既定なのに TEN VAD がクレジット表から抜けていた**（Apache-2.0 で表記義務がある）。
   **PyInstaller のブートローダーが notices に無かった** — exe に組み込まれるが
   `_internal/` にも PYZ にも現れないので、再生成手順では原理的に拾えない
6. **[anime-whisper を現行パイプラインで再検証した（#22）](#22-anime-whisper-を現行パイプラインで再検証した4-の結論は維持)。**
   **結論は維持で 23.3pt 負け**（27.4% 対 50.7%）。
   **「#4 の敗因はループだから抑制が入った今なら救われる」という仮説は否定された** —
   製品版検出器は旧出力から9本で2セグ427字しか落とさない。実際の故障は誤認識
7. **[XXL が実運用経路で落ちることの裏付けが取れた](#実運用での裏付けamatsukaze-から素の-xxl-を呼ぶと落ちる2026-08-23)。**
   Amatsukaze からオプション皆無で呼ぶと `CUBLAS_STATUS_NOT_SUPPORTED` で停止し、
   **録画は完成して字幕だけ付かない**。素材・VRAM・モデルサイズに依存しない
8. **[作者への訂正が必要になった](HANDOVER.md#訂正して伝える必要がある説明sm_120-フォールバック説)。**
   8月に伝えた「sm_89 へサイレントフォールバックして 20〜40% 低下」は実測と違う
   （落ちる／float32 なら動くが約2倍遅い）。**その説明が Amatsukaze のドキュメントに
   反映されている**ので、訂正と exe 版の検証依頼を送った。**その反応待ちが現在地**

**前回までの内容（TEN VAD を既定にする一連の測定）は[現状](STATUS.md#現状)と
[測定結果](#測定結果)に移した。** 15本で silero に 15勝0敗、
実運用と測定が同一素材で一致、という到達点はそこで読める。

---

## AMD ROCm 事前確認（2026-08-30）

公式 CTranslate2 v4.8.1 の `rocm-python-wheels-Windows.zip`（137,399,104 bytes）を
GitHub Releases から一時領域へ取得し、cp312 wheel を展開して確認した。取得物と
添付の調査スクリプトはリポジトリへコピーしておらず、Git 管理外。

| 確認項目 | 結果 |
|---|---|
| wheel 内 `ctranslate2.dll` | 290,646,528 bytes |
| `ctranslate2.dll` の直接依存 | `hipblas.dll`, `amdhip64_7.dll`（ほかは VC runtime / Intel OpenMP / Windows） |
| `hipblas.dll` の推移依存 | `rocblas.dll`, `rocsolver.dll`, `libhipblaslt.dll` と2系統のカーネル data tree |
| この機の `amdhip64_7.dll` | `C:\Windows\System32\amdhip64_7.dll`、`LoadLibrary` 成功 |
| HIP/BLAS ランタイム | AMD 公式 `rocm_sdk_libraries_custom` 7.2.1 wheel を隔離 venv に導入 |
| tiny float16 | 9.98秒、1.9秒（5.3x）、単語タイムスタンプ42個 |
| large-v3 float16（通常） | 119.71秒、90.92秒（1.32x）、単語タイムスタンプ492個 |
| large-v3 float16（batched=8） | 119.71秒、4.61秒（25.97x）、単語タイムスタンプ492個 |
| 実 AAC / large-v3 float16（batched=8） | 1464.7秒、TEN VAD 5.1秒、推論44.1秒、全体49.045秒、exit 0 |

配布設計は CUDA / ROCm を別パッケージにした。両 wheel の
`ctranslate2.dll` は同名で置換関係にあり、単一 `_internal` に安全に同居できない。
AMD 版は `whisp-carrier-amd.exe`、CUDA 版は従来どおり `whisp-carrier.exe`。
AMD 版には hipBLAS とライセンス通知を入れ、`amdhip64_7.dll` はドライバから読む。

短尺 large-v3 の転写文は合成した原文と一致。長尺 batched は内容語を保ち、原文との差は
各反復の読点1個（12 / 660字、約1.8%）だった。ただしこれは合成音声の自己一致であり、
CUDA 版との CER 比較ではない。通常推論は速度条件を満たさず、batched は十分満たすため、
AMD 配布版だけ batched を既定にする判断だった。未確定なのは既存 eval 素材での CUDA 版との CER。

追記（2026-09-05）：実音声 `F:\temp\test.aac` / large-v3-turbo の比較を受け、
AMD も通常推論を既定に変更した。CUDA 通常推論315字幕に対しAMD通常推論も315字幕、
本文313件が完全一致し、推論は43.6秒。旧AMDバッチ推論は65字幕まで結合されていた。
`without_timestamps=False` を明示するとバッチ推論も301字幕まで改善したが、
CUDA の結果に近い通常推論を採用。バッチ推論は `--batched` 指定時のみ使う。
これは単一素材での出力比較であり、正解字幕に対する CER 評価ではない。

同日の終了処理修正：通常推論は `All done.` とSRT出力後もプロセスが残った。
既存の `os._exit()` でも回避できず、出力ファイルのcloseと標準出力のflush後に
Windows `TerminateProcess(GetCurrentProcess(), status)` を呼ぶ形へ変更した。
これはDLLのprocess-detach処理を避ける対策（個々のDLL内の停止箇所までは未特定）。
再ビルドしたAMD exeで同じAACを無指定の通常推論で処理するとexit 0で終了し、
SRTのSHA-256は変更前と一致（`2b8d09e63723fda12be5832344bfc017cfafe0493cd63d1898a1b4e01ef9ab0c`）。
無効なAAC入力もexit 1で終了し、プロセスは残らなかった。

最終入力に指定された `X:\temp\sample\_test.aac` は実在せず、同名相当として見つかった
`X:\temp\sample_test.aac`（46,823,914 bytes）を使用した。TEN VAD の39領域は batched
用に30秒以下の64 clipへ分割され、JSONは64セグメント、最終セグメントは
1439.900–1463.420秒。SRT/JSONは `X:\temp\whisp-carrier-amd-final-test-v4` に生成した。

この実素材で batched API の `clip_timestamps` が通常推論と異なる型を要求すること、
30秒超の辞書 clip は残りを自動処理せず切り捨てることが判明した。辞書配列への変換と
30秒分割を実装し、元の39領域を64 clipとして全範囲処理した。また Windows ROCm は
実推論後のモデル破棄で `All done` の後に停止するため、出力を閉じて flush した後だけ
終了コードを保ったまま明示終了する。CUDA / CPU の終了経路は変更しない。

### CUDA 配布版の回帰ビルド（2026-09-05）

`WHISP_CARRIER_BACKEND` を未指定にし、既定値 `cuda` でPyInstallerのslim版を
`dist-slim/whisp-carrier` へビルドした。ビルド環境はPython 3.12.10、標準
CTranslate2 4.8.1、PyTorch 2.8.0+cu128。ホストにはCUDA Toolkit 12.9が入っているが、
配布物はPyTorchの `torch/lib` から13個、1,870 MBのCUDA DLLを収集する。

完成物は755ファイル、2,327,776,110 bytes。backend markerは `cuda`、ROCm DLLは
混入していない。実行時はPATHを `C:\Windows\System32;C:\Windows` のみにし、
`CUDA_PATH` も除去した状態で `--version` / `--checkcuda` がともにexit 0、RTX 2070を
1台認識した。したがってインストール済みToolkitではなく同梱CUDA DLLで起動している。

最終入力をローカル `F:\temp\sample_test.aac` へコピーしてlarge-v3 / float16 /
CUDA版既定の非batchedで全編処理した。1464.7秒に対してTEN VAD 39領域、推論95.4秒、
全体100.259秒、JSON/SRT 366セグメント、最終時刻1463.420秒、exit 0。

torch==2.8.0
torchaudio==2.8.0
# 1.2.1 以降は内蔵VADが silero v6 になる。README/HANDOVER はこれを前提にしている。
faster-whisper>=1.2.1
# Windowsスクリプト版でCTranslate2 DLLのCUDA/ROCmバックエンドを判定するために使用。
pefile>=2023.2.7; platform_system == "Windows"
# Linuxスクリプト版でCTranslate2のELF依存先を調べるために使用。
pyelftools>=0.31; platform_system == "Linux"
# transformers 形式のモデル（-m anime-whisper 等）を CTranslate2 に変換するときだけ
# 必要。変換は初回のみで、変換済みモデルを使う場合は無くても動く。
# ctranslate2 4.8 系のコンバータは from_pretrained に dtype= を渡すため 4.56 以上。
# 5.15.0 / safetensors 0.8.0 で動作確認。
transformers>=4.56
safetensors>=0.4
audio-separator>=0.44.0
# 既定のVAD（--vad_method ten）。Apache-2.0、1.3MB。wheel に prebuilt な
# ten_vad.dll が入っていて ctypes で読む形なので、exe では spec が
# ten_vad/lib/Windows/x64 に配置している。
# 9本で silero に対し全文CER 19.3% → 16.1%、カバレッジ 82.6% → 86.6%。
ten-vad==1.0.6.8
# --vad_method silero_* 用。既定ではないが切り替え先として残している。
silero-vad>=5.1.0
stable-ts>=2.16.0
ffmpeg-python==0.2.0
tqdm>=4.65.0
# 設定ファイル（whisp-carrier.yaml）の読み込み用。6.0.3 で動作確認。
PyYAML>=6.0,<7
